# Ex1号機照射計画 仕様書

> **ファイル**: `xlms/Ex1号機照射計画.xlsm`
> **種別**: マクロ有効ブック (.xlsm)
> **用途**: 1号機の照射計画立案・記録・印刷を行うEXメニューファイル（照射管理システムを補完）
> **最終データ日付**: 2021-04-08（シート上のスナップショット）

---


## 凡例（本仕様書の表記ルール）

本仕様書では、保守時の判別を容易にするため、以下の表記ルールを使用します。

### 用語規約

| 用語 | 意味 |
|---|---|
| EXメニュー | ExRicSys フォルダに配置される VBA ファイル群（Ex*.xlsm）の総称 |
| `ExRicSys` | 配置フォルダ名。初出時は〈EXメニュー配置フォルダ〉と注記 |

### 表記規則

| 種別 | 表記 | 例 |
|---|---|---|
| モジュール（.bas / .cls） | **太字** | **スタート処理.bas** |
| ユーザーフォーム（.frm） | **太字** | **SoutiSenntaku.frm** |
| プロシージャ / イベント | `コード体()` | `生産情報開始処理()` |
| シート名 | 「」 | 「未処理品一覧」 |
| セル参照 | `コード体` | `$W$3` |
| 名前付き範囲 | `コード体` | `Amari` |
| DB テーブル / カラム | `コード体` | `ExKeikakuX` / `kakunin` |
| ユーザー操作 | （操作名） | （処理方法実行 Click） |
| 主要マーク | ✓ | ✓ = 保守時に最初に確認すべき項目 |

### データフロー 場所マーク（9章）

9章のデータフロー（テーブル・ツリー図）では、処理が行われる場所を以下のアイコンで区別します。

| アイコン | 種別 | 意味 |
|---|---|---|
| 📊 | シート操作 | ワークシート上のセル書込み・読取り・表示変更 |
| 🖥️ | 画面操作 | ユーザーフォーム（.frm）の表示・入力・操作 |
| 🗄️ | DB操作 | Oracle DB への SELECT / INSERT / UPDATE / DELETE |
| 📄 | VBA内部処理 | 変数計算・条件分岐など、画面・シートに直接関与しない処理 |

### ✓（主要マーク）の判定基準

✓ は **保守時に最初に確認すべき項目** を示します。
判定基準は対象の種類ごとに以下のとおりです。

| 章 | 対象 | ✓ の判定基準 |
|---|---|---|
| 1.1 | シート | ユーザーが直接操作する、または VBA が動的に表示/非表示を切り替える |
| 1.2 | ユーザーフォーム | ユーザー入力を受け付ける、または業務フローの起点となる |
| 1.3 / 6.0 | VBA モジュール | ① ユーザー操作の起点 ② DB I/O を含む ③ 他モジュールから呼び出される ④ コード行数上位 25%　のいずれか |
| 2 | セル / 名前付き範囲 | VBA から `Range()` で代入または参照され、業務ロジックに直結する |
| 3 | 名前付き範囲 | VBA から `Range()` で代入または参照され、業務ロジックに直結する |
| 5 | ボタン / コントロール | DB 更新・画面遷移・計算実行など副作用のある操作を起動する |
| 6.0（全プロシージャ） | プロシージャ | ① ユーザー操作の起点（Click イベント等） ② DB I/O を実行 ③ 他モジュールから呼び出される Public　のいずれか |
| 7 | フォームコントロール | ユーザー入力を受け付ける、またはイベントで業務処理を起動する |
| 8.2 | DB テーブル | INSERT / UPDATE / DELETE の対象（参照のみのテーブルは ✓ なし） |

---

## 目次

1. [ファイル構成](#1-ファイル構成)
2. [シート詳細](#2-シート詳細)
3. [名前付き範囲](#3-名前付き範囲)
4. [数式・条件付き書式](#4-数式条件付き書式)
5. [ボタン・マクロ対応](#5-ボタンマクロ対応)
6. [VBA モジュール仕様](#6-vba-モジュール仕様)
7. [ユーザーフォーム仕様](#7-ユーザーフォーム仕様)
8. [DB接続・外部連携](#8-db接続外部連携)
9. [データフロー](#9-データフロー)
10. [セキュリティ・注意事項](#10-セキュリティ注意事項)

---

## 1. ファイル構成

### 1.1 シート一覧

| ✓ | # | シート名 | コードネーム | max_row | max_col | 用途 |
|---|---|---------|-------------|---------|---------|------|
| ✓ | 1 | 「照射計画」 | Sheet4 | 177 | 293 (KG) | 照射中製品・計画予定をガントチャート形式で表示。ユーザーが直接操作し計画線を入力する主画面 |
| ✓ | 2 | 「未計画品一覧」 | Sheet5 | 204 | 14 (N) | 未照射品の在庫一覧を表示。ダブルクリックで計画対象として「照射計画」シートに転記 |
|   | 3 | 「設定値」 | Sheet1 | 26 | 14 (N) | セット場所マスタ、表示文字定義、稼働時間等のシステム定数を格納 |
|   | 4 | 「使用方法」 | Sheet2 | 27 | 4 (D) | 操作手順の説明テキスト |

### 1.2 フォーム一覧

| # | フォーム名 | 用途 |
|---|-----------|------|
| — | （なし） | 本ファイルにはユーザーフォームは含まれない |

### 1.3 モジュール一覧

| ✓ | # | モジュール名 | 種別 | プロシージャ数 | 行数（概算） | 概要 |
|---|---|-------------|------|---------------|-------------|------|
| ✓ | 1 | **Ex共通変数定義** | .bas | 0（宣言のみ） | 10 | Public 変数・定数の宣言（DB テーブル名、モード、中断カウンタ等） |
| ✓ | 2 | **ThisWorkbook** | .cls | 2 | 20 | ブック起動時の初期化（`Ex表示起動` 呼出）、閉じる前の未記録チェック |
| ✓ | 3 | **Ex印刷設定** | .bas | 1 | 55 | 印刷日数を指定してガントチャートを印刷プレビュー |
| ✓ | 4 | **Ex最新データ表示起動** | .bas | 1 | 60 | 全データ再読込のメインエントリ。未照射品・照射中・計画データを順次取得し画面を再構成 |
| ✓ | 5 | **中断・空き設定** | .bas | 4 | 140 | 計画線に「中断」「空」マークを挿入し、後続セルを右シフトする操作 |
| ✓ | 6 | **Sheet4**（「照射計画」） | .cls | 3 | 115 | ダブルクリックで計画記号を記入、セット場所変更時の自動ソート番号付与 |
| ✓ | 7 | **Exサブルーチン** | .bas | 1 | 12 | セット場所によるソート処理 |
| ✓ | 8 | **Ex計画表示** | .bas | 4 | 240 | DB から計画データを読み込み、ガントチャートに記号を配置 |
| ✓ | 9 | **Ex計画記録** | .bas | 1 | 170 | ガントチャート上の計画線を DB テーブルに INSERT/UPDATE |
| ✓ | 10 | **Ex最新照射中表示** | .bas | 2 | 215 | 照射管理サーバから照射中データを取得し残時間を計算、照射線を描画 |
| ✓ | 11 | **Ex計画エリア整え** | .bas | 3 | 130 | 画面クリア・条件付き書式設定・罫線描画・印刷範囲設定 |
| ✓ | 12 | **Sheet5**（「未計画品一覧」） | .cls | 1 | 12 | ダブルクリックで `計画指定()` を呼出 |
| ✓ | 13 | **Ex未照射品** | .bas | 2 | 105 | 未照射在庫と予約データを DB から読み込み一覧表示。`計画指定()` で照射計画シートに転記 |
| ✓ | 14 | **SQL_Execution** | .bas | 5 | 190 | ADODB によるDB接続・SQL実行・シート書込み・配列取得の汎用ライブラリ |
| ✓ | 15 | **ExSubFunc** | .bas | 4 | 75 | 日付変換 (`ExchengeDATE`, `ExchengeDay`)、記録有無判定 (`Kiroku`)、制御文字除去 (`CleanChar`) |
| ✓ | 16 | **Ex照射予定指定削除** | .bas | 1 | 55 | 予定行を選択して削除。DB レコードも同時に DELETE |
| ✓ | 17 | **ErrCheck** | .bas | 1 | 8 | エラーメッセージ表示の共通ユーティリティ |
|   | 18 | **Sheet1**（「設定値」） | .cls | 0 | 1 | コードのみ（`Option Explicit`） |
|   | 19 | **Sheet2**（「使用方法」） | .cls | 0 | 1 | コードのみ（`Option Explicit`） |

---

## 2. シート詳細

### 2.0 可視性一覧

| シート名 | 初期状態 | VBA による動的制御 |
|---------|---------|-------------------|
| 「照射計画」 | 表示 | `画面クリア()` で行 3-18 非表示、列 B-F・R 非表示（DebugFlg 空時） |
| 「未計画品一覧」 | 表示 | なし |
| 「設定値」 | 表示 | なし |
| 「使用方法」 | 表示 | なし |


### 2.0b 非表示行・列一覧

| シート | 非表示行 | 非表示列 |
|---|---|---|
| 「照射計画」 | 3〜18, 21〜22, 73〜84 | B〜F, R |
| 「未計画品一覧」 | 1, 3, 5 | C〜D |

### 2.1 「照射計画」シート

**目的**: 照射中製品・計画予定をガントチャート形式で表示し、ユーザーが直接操作して計画線を入力する主画面。最大293列×177行（1列＝3時間、1日8列で約34日分）。

#### ヘッダー領域（行1〜22）

| 行 | 列範囲 | 内容 |
|----|--------|------|
| 1 | `A1` | `DebugFlg`（デバッグフラグ。名前付き範囲） |
| 2 | `A2` | "Ex2"（ファイル識別子） |
| 2 | `P2:T2`（結合） | `MSGArea`：照射モード表示（"貯蔵中" / "固定照射中" / "昇降中" / "移動照射中" 等） |
| 2 | `W2:AA2`（結合） | "高速回転数(rpm)" → `MoterRPM`（`AB2`） |
| 2 | `AD2:AH2`（結合） | "処理箱数(L:40cm換算)" → `Hakosuu`（`AI2`） |
| 2 | `AK2:AN2`（結合） | "処理時間(h)" → 数式 `=IF(OR(AB2="",AI2=""),"",(AI2*40+1200)/(AB2*11.324))` |
| 3 | `C3` | `NewData`（名前付き範囲） |
| 4 | `F4:KG4` | 列番号（1〜293に対応する連番。ただし ID 列以降 209-216 の8値を繰り返し） |
| 5 | `A5:KG5` | セル通し番号（1〜293） |
| 19 | `A19:U19` | 列ヘッダー（No / 移動有無 / 未計画数 / 場所ソート用 / Ful受付番号 / 受付No / 会社名 / 線量 / 入荷数 / 未照射数 / 納期 / 出荷日 / 備考 / セット数 / 照射時間 / セット場所 / 管理番号 / 残時間 / 照射 / メモ） |
| 19 | `V19` 以降（8列結合で繰返） | 日付（`NowTime` = `=NOW()` 起点で +1日ずつ） |
| 20 | `V20:KG20` | 3時間刻みの時刻（0, 3, 6, 9, 12, 15, 18, 21 の繰返し） |
| 21 | `B21` | `=COUNTA($F21:$IC21)` — 計画行の使用数 |
| 21 | `D21` | `=IF(Q21="","",VLOOKUP(Q21,SetTbl,2,FALSE))` — セット場所のソート値変換 |
| 21 | `F21` | `Jikoku` = `=INT(HOUR(NowTime)/3)+1` — 現在時刻の3時間ブロック番号 |

#### データ領域（行23〜72、50行 = 最大50製品分）

| 列 | 内容 | 備考 |
|----|------|------|
| `A` | No（行番号） | |
| `B` | 移動/中断/空 有無 | 数式: `=COUNTIF(Q23,IdouMoji)+COUNTIF(V23:KG23,AkiMoji)+...` |
| `C` | （内部使用） | |
| `D` | 未計画数 | 数式: `=K23-SUM(V23:KG23)` |
| `E` | 場所ソート用 | VBA が 80/90/99 を書込み |
| `F` | Ful受付番号 | DB から取得 |
| `G` | 受付No（4桁） | DB から取得 |
| `H` | 会社名 | DB から取得 |
| `I` | 指定線量 | DB から取得 |
| `J` | 入荷数 | DB から取得 |
| `K` | 未照射数 | DB から取得 |
| `L` | 納期 | mm/dd 形式に変換 |
| `M` | 出荷日 | mm/dd 形式に変換 |
| `N` | 備考 | 制御文字除去済 |
| `O` | セット数 | ユーザー入力 |
| `P` | 照射時間 | ユーザー入力 |
| `Q` | セット場所 | 南コン / 北コン / 南固 / 北固 / 特１ / 特2 / 移動 |
| `R` | 管理番号 | DB の keikakuno |
| `S` | 残時間 | VBA が計算 |
| `T` | 照射状況 | "中" / "中断" / "予定" |
| `U` | メモ | DB から取得 |
| `V:KG` (22〜293) | ガントチャート領域 | 計画記号・照射中記号・中断・空・移動が入る |

#### フラグ行（行73）

| 列 | 内容 |
|----|------|
| `A73` | "移動照射フラグ"（ラベル） |
| `B73` | `=SUM(B23:B72)` — 移動/中断有無の合計 |
| `V73:KG73` | `=COUNTIF(V23:V72,IdouMoji)` — 各時間帯の移動照射件数 |

#### セット場所マスタ（行77〜84）

| 行 | 列A | 名前付き範囲 |
|----|-----|-------------|
| 77 | 北固 | `SetIti` (`$A$77:$A$84`) |
| 78 | 北コン | |
| 79 | 南固 | |
| 80 | 南コン | |
| 81 | 移動 | |
| 82 | 特１ | |
| 83 | 特2 | |
| 84 | 特殊 | |

### 2.2 「未計画品一覧」シート

**目的**: 未照射品の一覧をDBから取得して表示し、計画未設定の製品を確認する。

| 行 | 列範囲 | 内容 |
|----|--------|------|
| 2 | `E2` | "未照射品一覧"（タイトル） |
| 4 | `B4:M4` | 列ヘッダー（No. / 受付No / 会社名 / 製品名 / 線量 / 入荷数 / 未照射数 / 納期 / 出荷日 / 備考） |
| 6〜105 | `B6:M105` | データ行（`Misyousya` 名前付き範囲）。行番号は `B6` から連番。DB より自動取得 |

### 2.3 「設定値」シート

**目的**: セット場所マスタ・表示文字定義・照射パラメータなど、計画画面の動作設定値を格納する。

| セル範囲 | 内容 |
|---------|------|
| `B3:D3` | ヘッダー（セット場所 / No:ソート用 / DB登録値） |
| `B4:D13` | セット場所マスタ（10件: 北固〜予約）。`SetTbl` = `$B$4:$C$13`、`BayoTB` = `$C$4:$D$13` |
| `E4:E11` | 場所別の照射中表示文字列 |
| `G3:J5` | 計画・照射中の表示文字定義 |
| `G4:H4` | 計画表示: "計画" / "｜" (`HyoujiMoji1`) |
| `G5:H5` | 計画完了: "計画完了" / "↑" (`Hyoujimoji2`) |
| `G6:H6` | 計画移動時: "計画移動時" / "・" (`KeiIdou`) |
| `I4:J4` | 照射中: "照射中" / "｜" (`HyoujiMoji3`) |
| `I5:J5` | 照射完了: "照射完了" / "↑" (`HyoujiMoji4`) |
| `K4` | 移動列表示文字: "移動" (`IdouMoji`) |
| `L4` | 中断文字: "中断" (`TyuudanMoji`) |
| `M4` | 空き文字: "空" (`AkiMoji`) |
| `G9:H9` | "１日の照射時間" = 20（`KadouJikann`） |

### 2.4 「使用方法」シート

**目的**: 操作手順を8項目で記述するテキストシート（VBA なし）。項目: 起動、未照射品の計画、計画記録、印刷設定、予定行削除、中断設定、空設定、中断/空取消。

---

## 3. 名前付き範囲一覧

| ✓ | 名前 | 参照先 | 用途 |
|---|------|--------|------|
| ✓ | `AkiMoji` | `設定値!$M$4` | "空" 表示文字 |
| ✓ | `BayoTB` | `設定値!$C$4:$D$13` | セット場所⇔DB登録値の変換テーブル |
| ✓ | `DebugFlg` | `照射計画!$A$1` | デバッグモードフラグ |
| ✓ | `Hakosuu` | `照射計画!$AI$2` | 処理箱数表示セル |
| ✓ | `HyoujiAria` | `照射計画!$E$23:$KG$72` | データ表示+ガントチャート全域（クリア対象） |
| ✓ | `HyoujiMoji1` | `設定値!$H$4` | 計画線の中間文字 "｜" |
| ✓ | `Hyoujimoji2` | `設定値!$H$5` | 計画線の終端文字 "↑" |
| ✓ | `HyoujiMoji3` | `設定値!$J$4` | 照射中の中間文字 "｜" |
| ✓ | `HyoujiMoji4` | `設定値!$J$5` | 照射中の終端文字 "↑" |
| ✓ | `IdouMoji` | `設定値!$K$4` | 移動照射表示文字 "移動" |
|   | `IdouUMu` | `照射計画!$B$73` | 移動照射有無フラグ行 |
| ✓ | `Jikoku` | `照射計画!$F$21` | 現在時刻の3時間ブロック番号 |
| ✓ | `KadouJikann` | `設定値!$H$9` | 1日の照射稼働時間（20h） |
| ✓ | `KeiIdou` | `設定値!$H$6` | 計画中の移動文字 "・" |
| ✓ | `KeikakuAria` | `照射計画!$V$23:$IC$72` | ガントチャート領域（条件付き書式設定対象） |
| ✓ | `MisyoriUno` | `未計画品一覧!$E$6:$E$105` | 未処理受付番号列 |
| ✓ | `Misyousya` | `未計画品一覧!$C$6:$M$105` | 未照射品一覧データ範囲（クリア対象） |
| ✓ | `MoterRPM` | `照射計画!$AB$2` | 高速回転数表示セル |
| ✓ | `MSGArea` | `照射計画!$P$2` | 照射モード表示セル |
| ✓ | `NewData` | `照射計画!$C$3` | 新規データフラグ |
| ✓ | `NowTime` | `照射計画!$V$19` | 現在日時（`=NOW()`） |
| ✓ | `SetIti` | `照射計画!$A$77:$A$84` | セット場所リスト |
| ✓ | `SetTbl` | `設定値!$B$4:$C$13` | セット場所⇔ソート番号の変換テーブル |
| ✓ | `SotrArea` | `照射計画!$D$23:$IC$72` | ソート対象範囲 |
|   | `TeisaiMoto` | `照射計画!$ID$20:$IK$72` | 定彩元データ（予備領域） |
| ✓ | `TyuudanMoji` | `設定値!$L$4` | "中断" 表示文字 |


### 3.1 データの入力規則

| シート | セル | 種別 | 制約 |
|---|---|---|---|
| 「照射計画」 | `A1` | 整数 | 1〜9 |
| 「照射計画」 | `G8:G18` | 整数 | >=INT($V$19) |
| 「照射計画」 | `Q7:R18 U7:U18` | リスト | リスト: `#REF!` |
| 「照射計画」 | `Q23 Q25:Q72` | リスト | リスト: `SetIti` |
| 「照射計画」 | `U23:U72` | テキスト長 | <=30 |


---

---

## 4. 数式一覧

全410件の数式は以下のパターンに分類される。

| ✓ | パターン | 件数 | 代表セル | 数式 | 用途 |
|---|---------|------|---------|------|------|
| ✓ | 処理時間計算 | 1 | `AO2` | `=IF(OR(AB2="",AI2=""),"",(AI2*40+1200)/(AB2*11.324))` | 回転数と箱数から処理時間を算出 |
| ✓ | 現在日時 | 1 | `V19` | `=NOW()` | `NowTime` — ガントチャートの基準日時 |
| ✓ | 翌日計算 | 1 | `AD19` | `=INT(NowTime)+1` | 翌日の日付 |
| ✓ | 日付連番 | 32 | `AL19` 等 | `=AD19+1` | 2日目以降の日付ヘッダー |
| ✓ | 行使用数 | 1 | `B21` | `=COUNTA($F21:$IC21)` | 計画行の使用セル数 |
| ✓ | 場所変換 | 1 | `D21` | `=IF(Q21="","",VLOOKUP(Q21,SetTbl,2,FALSE))` | セット場所名→ソート番号 |
| ✓ | 時刻ブロック | 1 | `F21` | `=INT(HOUR(NowTime)/3)+1` | 現在時刻の3時間ブロック番号 |
| ✓ | 移動/中断カウント | 50 | `B23:B72` | `=COUNTIF(Q23,IdouMoji)+COUNTIF(V23:KG23,AkiMoji)+...` | 各行の中断・空・移動の有無を数値化 |
| ✓ | 未計画数 | 50 | `D23:D72` | `=K23-SUM(V23:KG23)` | 未照射数から計画済みセル数を差引 |
| ✓ | 移動フラグ合計 | 1 | `B73` | `=SUM(B23:B72)` | 全行の移動/中断合計 |
| ✓ | 列別移動カウント | 272 | `V73:KG73` | `=COUNTIF(V23:V72,IdouMoji)` | 各3時間枠での移動照射件数 |

---

## 5. ボタン・マクロ対応

### 5.1 シート上ボタン

#### 「照射計画」シート（vmlDrawing1.vml — 7個 + 描画図形 1個）

| ✓ | # | ボタン表示 | マクロ | 機能 |
|---|---|-----------|--------|------|
| ✓ | — | （A2 図形・テキストなし） | `画面クリア()` | シートのデータ領域をクリアして初期状態に戻す |
| ✓ | 1 | 最新データ | `Ex表示起動()` | DB から最新の照射中・計画データを再読込し画面を再構成 |
| ✓ | 2 | 計画記録 | `Ex計画記録.Ex計画記録()` | ガントチャート上の計画線を DB に INSERT/UPDATE |
| ✓ | 3 | 印刷設定 | `Innsatu()` | 印刷日数を入力しプリンタ選択→印刷プレビュー |
| ✓ | 4 | 中断設定 | `中断設定()` | 選択セルに「中断」マークを挿入し後続を右シフト |
| ✓ | 5 | 空設定 | `空き設定()` | 選択セルに「空」マークを挿入し後続を右シフト |
| ✓ | 6 | 中断/空取消 | `中断空き取消()` | 「中断」「空」マークを削除し後続を左シフト |
| ✓ | 7 | 予定行削除 | `予定行削除()` | 選択行の予定データをクリアし DB からも DELETE |

#### 「未計画品一覧」シート（vmlDrawing2.vml — 1個）

| ✓ | # | ボタン表示 | マクロ | 機能 |
|---|---|-----------|--------|------|
| ✓ | 1 | 選択 | `計画指定()` | 選択行の製品を「照射計画」シートに転記し予定行として追加 |

### 5.2 フォームボタン

本ファイルにはユーザーフォームが存在しないため該当なし。

### 5.x ショートカットキー

| マクロ名 | ショートカット | 処理概要 |
|---|---|---|
| `画面クリア()` | （割り当てなし） | 計画シートのデータクリア |
| `ExAriaCLS()` | （割り当てなし） | 計画エリアのクリア |

### 5.3 CommandBar

VBA コード内に `CommandBar` の定義は存在しない。

### 5.4 ダブルクリックイベント

| ✓ | シート | イベント | 動作 |
|---|--------|---------|------|
| ✓ | 「照射計画」 | `Worksheet_BeforeDoubleClick` | 行23-72、列22以降でダブルクリック → `記号記入()` で計画記号をセルに配置 |
| ✓ | 「未計画品一覧」 | `Worksheet_BeforeDoubleClick` | 行6以降、列19以下でダブルクリック → `計画指定()` で「照射計画」に転記 |

---

## 6. VBA モジュール仕様

### 6.0 全プロシージャ一覧

| ✓ | # | モジュール | プロシージャ | 種別 | DB I/O | 呼出元 | 概要 |
|---|---|-----------|-------------|------|--------|--------|------|
| ✓ | 1 | **ThisWorkbook** | `Workbook_Open()` | Event | — | 自動 | ブック開時に `Ex表示起動()` を呼出 |
| ✓ | 2 | **ThisWorkbook** | `Workbook_BeforeClose()` | Event | — | 自動 | 未記録の計画行がある場合に確認ダイアログを表示 |
| ✓ | 3 | **Ex印刷設定** | `Innsatu()` | Sub | — | ボタン「印刷設定」 | 印刷日数入力→改ページ設定→印刷プレビュー |
| ✓ | 4 | **Ex最新データ表示起動** | `Ex表示起動()` | Sub | 間接 | ボタン「最新データ」/ `Workbook_Open` | 画面クリア後、`Ex未照射品表示` → `Ex照射中製品表示` → `Ex計画表示Main` → `計画画面整形` → `SetBasyoSort` |
| ✓ | 5 | **中断・空き設定** | `中断設定()` | Sub | — | ボタン「中断設定」 | `中断空き処理(TyuudanMoji)` を呼出 |
| ✓ | 6 | **中断・空き設定** | `空き設定()` | Sub | — | ボタン「空設定」 | `中断空き処理(AkiMoji)` を呼出 |
| ✓ | 7 | **中断・空き設定** | `中断空き処理()` | Sub | — | `中断設定` / `空き設定` | 選択セルに中断/空マークを挿入し後続セルを右シフト。「空」セルは入替処理あり |
| ✓ | 8 | **中断・空き設定** | `中断空き取消()` | Sub | — | ボタン「中断/空取消」 | 中断/空マークを削除し後続セルを左シフト |
| ✓ | 9 | **Sheet4**（「照射計画」） | `Worksheet_BeforeDoubleClick()` | Event | — | ダブルクリック | `記号記入()` を呼出 |
| ✓ | 10 | **Sheet4**（「照射計画」） | `記号記入()` | Sub | — | `Worksheet_BeforeDoubleClick` | 照射時間から計画記号数を計算しセルに書込み（移動照射は `IdouMoji`、固定はセット数+中間記号+終端記号） |
| ✓ | 11 | **Sheet4**（「照射計画」） | `Worksheet_Change()` | Event | — | セル変更 | セット場所が "移動" に変更された場合ソート用値を更新 |
| ✓ | 12 | **Exサブルーチン** | `SetBasyoSort()` | Sub | — | `Ex表示起動` | `SotrArea` を場所(E列)→管理番号(R列)で並替え |
| ✓ | 13 | **Ex計画表示** | `Ex計画表示Main()` | Sub | 間接 | `Ex表示起動` | `Ex計画データ表示` → `照射中の中断` → `移動照射反映` → `計画画面整形` |
| ✓ | 14 | **Ex計画表示** | `移動照射反映()` | Sub | — | `Ex計画表示Main` | 移動照射中に固定照射の時間帯を `IdouMoji` で埋める |
| ✓ | 15 | **Ex計画表示** | `Ex計画データ表示()` | Sub | R | `Ex計画表示Main` | `ExR1Keikaku1` から計画済み製品を SELECT し、ガントチャートの列に記号を配置 |
| ✓ | 16 | **Ex計画表示** | `照射中の中断()` | Sub | R | `Ex計画表示Main` | `ExR1Keikaku2` から中断記録を SELECT し、該当列に中断記号を挿入 |
| ✓ | 17 | **Ex計画表示** | `Ex計画記号表示()` | Sub | R | `Ex計画データ表示` | `ExR1Keikaku2` から記号データを SELECT し、セルに配置 |
| ✓ | 18 | **Ex計画記録** | `Ex計画記録()` | Sub | W | ボタン「計画記録」 | 行23-72を走査し、照射中/予定の計画線を `ExR1Keikaku1` / `ExR1Keikaku2` に INSERT/UPDATE。記録前に既存レコードを DELETE |
| ✓ | 19 | **Ex最新照射中表示** | `Ex照射中製品表示()` | Sub | R | `Ex表示起動` | `syouk1`, `zaiko`, `ExkeikakuX` を JOIN し照射中製品を取得。`sengnr1` から直近の照射開始イベントを取得し残時間を計算 |
| ✓ | 20 | **Ex最新照射中表示** | `SyousyaIti()` | Function | — | `Ex照射中製品表示` | 照射位置コード→ソート用場所番号の変換 |
| ✓ | 21 | **Ex計画エリア整え** | `画面クリア()` | Sub | — | デバッグ用 | DebugFlg に応じて行・列の表示/非表示を切替え、`ExAriaCLS()` を呼出 |
| ✓ | 22 | **Ex計画エリア整え** | `ExAriaCLS()` | Sub | — | `Ex表示起動` / `画面クリア` | `HyoujiAria`, `Misyousya`, `MoterRPM`, `Hakosuu`, `MSGArea` をクリア |
| ✓ | 23 | **Ex計画エリア整え** | `計画画面整形()` | Sub | — | 多数 | 条件付き書式（南コン→薄い赤、北コン→薄い黄色、固定→薄い紫）設定、罫線描画、印刷範囲設定、行高さ自動調整 |
| ✓ | 24 | **Sheet5**（「未計画品一覧」） | `Worksheet_BeforeDoubleClick()` | Event | — | ダブルクリック | `計画指定()` を呼出 |
| ✓ | 25 | **Ex未照射品** | `Ex未照射品表示()` | Sub | R | `Ex表示起動` | `zaiko`, `ExkeikakuX`, `sehmst` を JOIN し未照射品一覧を取得。`ExYoyakuX` から予約データも追加取得 |
| ✓ | 26 | **Ex未照射品** | `計画指定()` | Sub | — | ボタン「選択」/ ダブルクリック | 未計画品一覧から選択行のデータを「照射計画」シートの予定行に転記 |
| ✓ | 27 | **SQL_Execution** | `Open_oraconDB()` | Sub | 接続 | 各 DB 操作 | ADODB で Oracle DB に接続 |
| ✓ | 28 | **SQL_Execution** | `SQL_Exe()` | Sub | 実行 | 各 DB 操作 | SQL 文を Execute |
| ✓ | 29 | **SQL_Execution** | `SQL_INSERT_UPDATE()` | Sub | W | `Ex計画記録` | キー検索 → 存在すれば UPDATE、なければ INSERT |
| ✓ | 30 | **SQL_Execution** | `SQL_Delete()` | Sub | W | `Ex計画記録` / `予定行削除` | WHERE 条件で DELETE |
| ✓ | 31 | **SQL_Execution** | `Disp_Sheet()` | Sub | R | `Ex計画データ表示` / `Ex未照射品表示` | SQL 結果をシートに `CopyFromRecordset` で一括書込み |
| ✓ | 32 | **SQL_Execution** | `Set_Array()` | Sub | R | `照射中の中断` / `Ex計画記号表示` / `Ex照射中製品表示` | SQL 結果を VBA 配列に格納 |
| ✓ | 33 | **ExSubFunc** | `ExchengeDATE()` | Function | — | 複数 | 日付文字列を "mm/dd" or "yyyy/mm/dd" 形式に変換 |
| ✓ | 34 | **ExSubFunc** | `ExchengeDay()` | Function | — | `Ex計画記録` | 日付値を "mmdd" or "yyyymmdd" 文字列に変換 |
| ✓ | 35 | **ExSubFunc** | `Kiroku()` | Function | — | `ThisWorkbook` | 予定行で管理番号未設定の行を検出（未記録行の有無判定） |
| ✓ | 36 | **ExSubFunc** | `CleanChar()` | Function | — | 複数 | 文字列から制御コード（ASC < 32）を除去 |
| ✓ | 37 | **Ex照射予定指定削除** | `予定行削除()` | Sub | W | ボタン「予定行削除」 | 予定行のセルクリア + DB から `ExR1Keikaku1` / `ExR1Keikaku2` を DELETE |
| ✓ | 38 | **ErrCheck** | `ErrCheckMsg()` | Sub | — | 複数 | `mpErrDes` が非空の場合 MsgBox でエラー表示 |

---

## 7. ユーザーフォーム仕様

本ファイルにはユーザーフォームは含まれない。

---

## 8. DB 接続・外部連携

### 8.1 ODBC 接続

| 項目 | 値 |
|------|-----|
| 接続方式 | ADODB (Microsoft ActiveX Data Objects) |
| 接続文字列 | `DSN=ricdb;UID=ric;PWD=t6101` |
| 接続先 | 照射管理システム（Oracle DB） |
| ドライバ | ODBC 経由 Oracle |
| 接続タイミング | 各 SQL 実行時に毎回 Open → 実行 → Close |
| カーソル位置 | `adUseClient` |
| トランザクション | `SQL_INSERT_UPDATE` / `SQL_Delete` で `BeginTrans` → `CommitTrans` |

### 8.2 テーブル一覧

| ✓ | # | テーブル名 | 用途 | SELECT | INSERT | UPDATE | DELETE |
|---|---|-----------|------|--------|--------|--------|--------|
| ✓ | 1 | `ExR1Keikaku1` | 1号機計画表示部（製品情報+計画期間） | ✓ | ✓ | ✓ | ✓ |
| ✓ | 2 | `ExR1Keikaku2` | 1号機計画データ部（時間帯別記号データ） | ✓ | ✓ | ✓ | ✓ |
| ✓ | 3 | `syouk1` | 照射管理テーブル（照射中製品情報） | ✓ | — | — | — |
|   | 4 | `zaiko` | 在庫テーブル（未照射品情報） | ✓ | — | — | — |
|   | 5 | `ExkeikakuX` | 共通計画テーブル（出荷日・備考） | ✓ | — | — | — |
|   | 6 | `sehmst` | 製品マスタ（会社コード→製品名変換） | ✓ | — | — | — |
|   | 7 | `sengnr1` | 線源利用データ（照射開始/停止イベント） | ✓ | — | — | — |
|   | 8 | `ExYoyakuX` | 予約テーブル（予約品情報） | ✓ | — | — | — |

> テーブル名の定数定義: `mpExKeikaku1 = "ExR1Keikaku1"`, `mpExKeikaku2 = "ExR1Keikaku2"`（**Ex共通変数定義** モジュール）

### 8.3 SQL 文一覧

#### SELECT 文

| # | 実行元 | SQL 概要 | テーブル | WHERE 条件 | 主なカラム |
|---|--------|---------|---------|-----------|-----------|
| 1 | `Ex計画データ表示()` | 計画済み製品情報の取得 | `ExR1Keikaku1` | `keikakuno>1000 AND keieday>={現在日数値} AND kakuninn={有効フラグ} AND kainame IS NOT NULL` | UNO, KAINAME, SITEISN, NYUKASU, MISYOUSU, NOUKI, SYUKKABI, BIKOU, SETSUU, SYOZI, SYOITI, KEIKAKUNO, ZANJIKANN, SYOUJYO, MENO |
| 2 | `Ex計画記号表示()` | 計画記号データの取得 | `ExR1Keikaku2` | `keikakuno={管理番号}` | keiday, keihr, keikaku |
| 3 | `照射中の中断()` | 中断記録の取得 | `ExR1Keikaku2` | `yuukou={有効フラグ}` + 管理番号ごとにループ | keiday, keihr, keikaku |
| 4 | `Ex照射中製品表示()` | 照射中製品情報の取得 | `syouk1 s, zaiko z, ExkeikakuX e, ExR1Keikaku1 k` | `s.uno=z.uno AND s.uno=e.uno(+) AND s.syokind<>{有効フラグ} AND TO_NUMBER(s.slotno)=k.keikakuno(+)` | uno, kainame, siteisn, nyukasu, misyousu, nouki, syukkabi, bikou1, syosuu, syotime, syoichi, slotno, htimer, syokind, syostat, meno |
| 5 | `Ex照射中製品表示()` | 直近の照射開始イベント取得 | `sengnr1` | `(event='1' OR event='3') AND sdate>='{照射中最古日}' ORDER BY sdate DESC, stime DESC` | event, sdate, stime, timer |
| 6 | `Ex未照射品表示()` | 未照射在庫一覧の取得 | `zaiko z, ExkeikakuX e, sehmst s` | `z.uno=e.uno(+) AND z.misyousu*1>0 AND z.syouso='1' AND z.kaisyacd=s.kaisyacd AND z.sehncd=s.sehncd` | uno, kainame, seiname, siteisn, nyukasu, misyousu, nouki, syukkabi, bikou1 |
| 7 | `Ex未照射品表示()` | 予約データの取得 | `ExYoyakuX` | `souti='1' AND yuukou='1'` | yoyakuno, uno, kainame, siteisn, nyukasu, nouki, syukkabi, bikou |
| 8 | `SQL_INSERT_UPDATE()` | レコード存在確認 | 対象テーブル | `{動的キー条件}` | COUNT(*) |

#### INSERT / UPDATE 文

| # | 実行元 | テーブル | 対象カラム |
|---|--------|---------|-----------|
| 1 | `Ex計画記録()` | `ExR1Keikaku1` | KEIKAKUNO, UNO, KAINAME, SITEISN, NYUKASU, MISYOUSU, NOUKI, SYUKKABI, BIKOU, SETSUU, SYOZI, SYOITI, ZANJIKANN, SYOUJYO, MENO, keisday, keieday, KAKUNINN（18項目） |
| 2 | `Ex計画記録()` | `ExR1Keikaku2` | keikakuno, edano, keiday, keihr, keikaku, yuukou（6項目） |

#### DELETE 文

| # | 実行元 | テーブル | WHERE 条件 |
|---|--------|---------|-----------|
| 1 | `Ex計画記録()` | `ExR1Keikaku1` | `keikakuno>=0 AND kakuninn='2'` |
| 2 | `Ex計画記録()` | `ExR1Keikaku2` | `keikakuno>=0 AND yuukou='2'` |
| 3 | `予定行削除()` | `ExR1Keikaku1` | `keikakuno={管理番号} AND kakuninn='2'` |
| 4 | `予定行削除()` | `ExR1Keikaku2` | `keikakuno={管理番号} AND yuukou='2'` |

### 8.4 外部ファイル参照

VBA コード内に外部ファイルパスの参照は存在しない。Excel の外部リンクも検出されていない。

---

## 9. データフロー

### 9.1 データフローテーブル

| ✓ | # | データ | 入力元 | 処理 | 出力先 | 場所 |
|---|---|--------|--------|------|--------|------|
| ✓ | 1 | 照射中製品情報 | 🗄️ `syouk1` + `zaiko` + `ExkeikakuX` | `Ex照射中製品表示()` で SELECT & 残時間計算 | 📊「照射計画」行23〜 | 📊🗄️ |
| ✓ | 2 | 線源イベント | 🗄️ `sengnr1` | `Ex照射中製品表示()` で直近の照射開始日時・タイマー値取得 | 📊「照射計画」残時間計算に使用 | 🗄️ |
| ✓ | 3 | 照射モード | 🗄️ `sengnr1` event列 | 1=固定, 3=移動 → 表示文字変換 | 📊 `MSGArea` (P2) | 📊🗄️ |
| ✓ | 4 | 計画データ（製品） | 🗄️ `ExR1Keikaku1` | `Ex計画データ表示()` で SELECT | 📊「照射計画」行24〜 | 📊🗄️ |
| ✓ | 5 | 計画データ（記号） | 🗄️ `ExR1Keikaku2` | `Ex計画記号表示()` で SELECT → セル配置 | 📊「照射計画」V列〜KG列 | 📊🗄️ |
| ✓ | 6 | 未照射在庫 | 🗄️ `zaiko` + `ExkeikakuX` + `sehmst` | `Ex未照射品表示()` で SELECT | 📊「未計画品一覧」行6〜 | 📊🗄️ |
| ✓ | 7 | 予約データ | 🗄️ `ExYoyakuX` | `Ex未照射品表示()` で SELECT | 📊「未計画品一覧」行6〜（在庫の後に追加） | 📊🗄️ |
| ✓ | 8 | 計画指定（転記） | 📊「未計画品一覧」選択行 | `計画指定()` でセルコピー | 📊「照射計画」予定行 | 📊 |
| ✓ | 9 | 計画記号入力 | 🖥️ ダブルクリック | `記号記入()` でセット数・照射時間から記号数計算 | 📊「照射計画」V列〜 | 📊🖥️ |
| ✓ | 10 | 中断/空挿入 | 🖥️ ボタン操作 | `中断空き処理()` で Insert+右シフト | 📊「照射計画」V列〜 | 📊🖥️ |
| ✓ | 11 | 計画記録（保存） | 📊「照射計画」行23-72 | `Ex計画記録()` で DELETE → INSERT/UPDATE | 🗄️ `ExR1Keikaku1` + `ExR1Keikaku2` | 📊🗄️ |
| ✓ | 12 | 予定行削除 | 🖥️ ボタン操作 | `予定行削除()` でセルクリア + DELETE | 📊 クリア + 🗄️ DELETE | 📊🗄️🖥️ |
| ✓ | 13 | 設定値参照 | 📊「設定値」 | 名前付き範囲で参照 | 📊「照射計画」条件付き書式・記号文字 | 📊 |
| ✓ | 14 | 印刷 | 📊「照射計画」 | `Innsatu()` で印刷範囲設定 | 📄 プリンタ出力 | 📊📄 |

### 9.2 データフローツリー図

```
🖥️ ユーザー操作
├── [起動 / 最新データボタン] → Ex表示起動()
│   ├── ExAriaCLS()                          📊 画面クリア
│   ├── Ex未照射品表示()                      📊「未計画品一覧」
│   │   ├── SELECT zaiko+ExkeikakuX+sehmst   🗄️ → 📊
│   │   └── SELECT ExYoyakuX                 🗄️ → 📊
│   ├── Ex照射中製品表示()                    📊「照射計画」行23〜
│   │   ├── SELECT syouk1+zaiko+ExkeikakuX   🗄️ → 📊
│   │   └── SELECT sengnr1                   🗄️ → 残時間計算
│   ├── Ex計画表示Main()
│   │   ├── Ex計画データ表示()               🗄️ ExR1Keikaku1 → 📊
│   │   │   └── Ex計画記号表示()             🗄️ ExR1Keikaku2 → 📊
│   │   ├── 照射中の中断()                   🗄️ ExR1Keikaku2 → 📊
│   │   ├── 移動照射反映()                   📊 内部処理
│   │   └── 計画画面整形()                   📊 書式・罫線
│   └── SetBasyoSort()                       📊 ソート
│
├── [未計画品ダブルクリック / 選択ボタン] → 計画指定()
│   └── 📊「未計画品一覧」 → 📊「照射計画」予定行
│
├── [計画セルダブルクリック] → 記号記入()
│   └── 📊 セット数・照射時間 → 📊 計画記号配置
│
├── [中断設定ボタン] → 中断設定() → 中断空き処理()
│   └── 📊 選択セル → Insert → 右シフト
│
├── [空設定ボタン] → 空き設定() → 中断空き処理()
│   └── 📊 選択セル → Insert → 右シフト
│
├── [中断/空取消ボタン] → 中断空き取消()
│   └── 📊 選択セル → Delete → 左シフト
│
├── [計画記録ボタン] → Ex計画記録()
│   ├── DELETE ExR1Keikaku1 (kakuninn='2')   📊 → 🗄️
│   ├── DELETE ExR1Keikaku2 (yuukou='2')     📊 → 🗄️
│   ├── INSERT/UPDATE ExR1Keikaku1           📊 → 🗄️
│   └── INSERT/UPDATE ExR1Keikaku2           📊 → 🗄️
│
├── [予定行削除ボタン] → 予定行削除()
│   ├── DELETE ExR1Keikaku1                  📊 → 🗄️
│   ├── DELETE ExR1Keikaku2                  📊 → 🗄️
│   └── セルクリア                            📊
│
└── [印刷設定ボタン] → Innsatu()
    └── 📊 → 📄 印刷プレビュー
```

---

## 10. セキュリティ注意事項

| # | 区分 | 内容 | リスク |
|---|------|------|--------|
| 1 | DB 認証情報のハードコード | 接続文字列 `DSN=ricdb;UID=ric;PWD=t6101` が **SQL_Execution** モジュール内に平文で記述されている | パスワード漏洩リスク。環境変数またはレジストリ等への外部化を推奨 |
| 2 | SQL インジェクション | `Ex計画記録()` 等でセル値を直接 SQL 文に文字列連結している。クォート (`'`) のエスケープ処理がない | セルに悪意ある文字列が入力された場合に SQL インジェクションが成立する可能性 |
| 3 | エラーハンドリング | `On Error Resume Next` が多用されており、DB エラーが握りつぶされる箇所がある（`Open_oraconDB`, `SQL_Exe` 等） | データ不整合の検出遅延リスク |
| 4 | DELETE の全件削除 | `Ex計画記録()` 内で `WHERE keikakuno>=0 AND kakuninn='2'` により、有効フラグ `'2'` の全レコードを DELETE した後に再 INSERT する設計 | 処理途中でエラーが発生した場合にデータ消失リスク（トランザクションが個別 SQL 単位） |
| 5 | シート保護 | `UserInterfaceOnly:=True` で UI からの編集のみ保護するが、保護後すぐに `Protect` で上書きしている箇所あり（`Ex表示起動`）。パスワード未設定 | VBA からの保護解除が容易 |
| 6 | 有効フラグ定数 | `mpYukou = "'2'"` がハードコードされており、値の意味（2 = 有効）がドキュメント化されていない | 保守時の誤解リスク |
| 7 | olevba 検出事項 | `Workbook_Open` (AutoExec)、`Chr` (文字列操作)、`Open` (ファイルオープン可能性) が検出されているが、本ファイルでは正当な業務用途 | 外部監査ツールでフラグされる可能性あり |
