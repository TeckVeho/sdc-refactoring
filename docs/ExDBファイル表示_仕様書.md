# ExDBファイル表示 仕様書

> **ファイル種別**: .xlsm（マクロ付き）
> **用途**: Oracle DB の任意のテーブルをユーザーが選択・条件設定してデータを抽出・表示する汎用DBブラウザツール（EXメニューの1ファイルとして照射管理システムを補完）。テーブル定義は Access DB（`テーブル一覧.accdb`）で管理する
> **VBA プロジェクト**: モジュール 14 本（.bas 8 / .cls 6 / .frm 0）
> **外部連携**:
> - Oracle DB（ODBC `DSN=ricdb`）：各業務テーブルのデータ抽出
> - Access DB（`\\163.59.144.156\ExRicSys\テーブル一覧.accdb`〈EXメニュー配置フォルダ〉）：テーブル定義・項目情報の管理
> **解析日**: 2026-08-20（表記統一）

---


## 凡例（本仕様書の表記ルール）

本仕様書では、保守時の判別を容易にするため、以下の表記ルールを使用します。

### 用語規約

| 用語 | 意味 |
| --- | --- |
| EXメニュー | ExRicSys フォルダに配置される VBA ファイル群（Ex*.xlsm）の総称 |
| `ExRicSys` | 配置フォルダ名。初出時は〈EXメニュー配置フォルダ〉と注記 |

### 表記規則

| 種別 | 表記 | 例 |
| --- | --- | --- |
| モジュール（.bas / .cls） | **太字** | **スタート処理.bas** |
| ユーザーフォーム（.frm） | **太字** | **SoutiSenntaku.frm** |
| プロシージャ / イベント | `コード体()` | `生産情報開始処理()` |
| シート名 | 「」 | 「未処理品一覧」 |
| セル参照 | `コード体` | `$W$3` |
| 名前付き範囲 | `コード体` | `Amari` |
| DB テーブル / カラム | `コード体` | `ExKeikakuX` / `kakunin` |
| ユーザー操作 | （操作名） | （処理方法実行 Click） |
| 主要マーク | ✓ | ✓ = 保守時に最初に確認すべき項目 |

### データフロー 場所マーク（9章）

9章のデータフロー（テーブル・ツリー図）では、処理が行われる場所を以下のアイコンで区別します。

| アイコン | 種別 | 意味 |
| --- | --- | --- |
| 📊 | シート操作 | ワークシート上のセル書込み・読取り・表示変更 |
| 🖥️ | 画面操作 | ユーザーフォーム（.frm）の表示・入力・操作 |
| 🗄️ | DB操作 | Oracle DB への SELECT / INSERT / UPDATE / DELETE |
| 📄 | VBA内部処理 | 変数計算・条件分岐など、画面・シートに直接関与しない処理 |

### ✓（主要マーク）の判定基準

✓ は **保守時に最初に確認すべき項目** を示します。
判定基準は対象の種類ごとに以下のとおりです。

| 章 | 対象 | ✓ の判定基準 |
| --- | --- | --- |
| 1.1 | シート | ユーザーが直接操作する、または VBA が動的に表示/非表示を切り替える |
| 1.2 | ユーザーフォーム | ユーザー入力を受け付ける、または業務フローの起点となる |
| 1.3 / 6.0 | VBA モジュール | ① ユーザー操作の起点 ② DB I/O を含む ③ 他モジュールから呼び出される ④ コード行数上位 25%　のいずれか |
| 2 | セル / 名前付き範囲 | VBA から `Range()` で代入または参照され、業務ロジックに直結する |
| 3 | 名前付き範囲 | VBA から `Range()` で代入または参照され、業務ロジックに直結する |
| 5 | ボタン / コントロール | DB 更新・画面遷移・計算実行など副作用のある操作を起動する |
| 6.0（全プロシージャ） | プロシージャ | ① ユーザー操作の起点（Click イベント等） ② DB I/O を実行 ③ 他モジュールから呼び出される Public　のいずれか |
| 7 | フォームコントロール | ユーザー入力を受け付ける、またはイベントで業務処理を起動する |
| 8.2 | DB テーブル | INSERT / UPDATE / DELETE の対象（参照のみのテーブルは ✓ なし） |

---

## 目次

1. [ファイル構成](#1-ファイル構成)
2. [シート詳細](#2-シート詳細)
3. [名前付き範囲一覧](#3-名前付き範囲一覧)
4. [数式一覧](#4-数式一覧)
5. [ボタン・マクロ対応](#5-ボタンマクロ対応)
6. [VBA モジュール仕様](#6-vba-モジュール仕様)
7. [ユーザーフォーム仕様](#7-ユーザーフォーム仕様)
8. [DB 接続・外部連携](#8-db-接続外部連携)
9. [データフロー](#9-データフロー)
10. [セキュリティ注意事項](#10-セキュリティ注意事項)
---

## 1. ファイル構成

### 1.1 シート一覧

> ✓ = ユーザーが直接操作する、または VBA が動的に表示/非表示を切り替えるシート

| ✓ | No | シート名 | 最大行 | 最大列 | 保存時 Visible | VBA による動的切替 |
| --- | --- | --- | --- | --- | --- | --- |
|  | 1 | 使い方 | 25 | 3 (C) | visible | — |
| ✓ | 2 | 項目TB | 224 | 44 (AR) | visible | — |
| ✓ | 3 | 抽出 | 150 | 25 (Y) | visible | — |
| ✓ | 4 | 抽出結果 | 30000 | 140 (EJ) | visible | — |
| ✓ | 5 | Table登録 | 208 | 16 (P) | visible | — |


### 1.2 ユーザーフォーム一覧

> ✓ = ユーザー入力を受け付ける、または業務フローの起点となるフォーム

なし。

### 1.3 VBA モジュール一覧

> ✓ = ユーザー操作の起点 / DB I/O を含む / 他モジュールから呼び出される / コード行数上位 25%

| ✓ | No | モジュール | 種別 | プロシージャ数 | 主な役割 |
| --- | --- | --- | --- | --- | --- |
| ✓ | 1 | **ThisWorkbook.cls** | .cls | 2 | 起動/終了イベント |
| ✓ | 2 | **Sheet1.cls** | .cls | 1 | 「抽出」シートイベント |
|  | 3 | **Sheet3.cls** | .cls | 0 | 「項目TB」シート（コードなし） |
|  | 4 | **Sheet4.cls** | .cls | 0 | 「Table登録」シート（コードなし） |
|  | 5 | **Sheet5.cls** | .cls | 0 | 「使い方」シート（コードなし） |
|  | 6 | **Sheet6.cls** | .cls | 0 | 「抽出結果」シート（コードなし） |
| ✓ | 7 | **テーブル選択.bas** | .bas | 2 | テーブル指定・項目名表示処理 |
| ✓ | 8 | **抽出設定.bas** | .bas | 3 | データ抽出・全て表示・新しいBookへの記録 |
| ✓ | 9 | **テーブル一覧.bas** | .bas | 3 | テーブル一覧表示・修正登録・テーブル登録 |
| ✓ | 10 | **SQL_Execution.bas** | .bas | 6 | Oracle DB への ODBC 接続・SQL 実行共通処理 |
| ✓ | 11 | **SQL_AccessExe.bas** | .bas | 7 | Access DB への OLE DB 接続・SQL 実行共通処理 |
|  | 12 | **シートの保護.bas** | .bas | 1 | 各シートの保護設定 |
|  | 13 | **画面クリア.bas** | .bas | 1 | 全シートの表示クリア |
|  | 14 | **ユーティリティ.bas** | .bas | 2 | 一覧登録・シートデータコピー処理 |

---

## 2. シート詳細

### 2.0 b 非表示行・列一覧

| 種類 | 対象 | 備考 |
| --- | --- | --- |
| 非表示行 | 項目TB 行2 | ヘッダー補助行 |
| 非表示列 | 項目TB C〜D | 内部列 |
| 非表示行 | 抽出 行2〜3 | 設定補助行 |
| 非表示列 | 抽出 L〜M | 内部列 |
| 非表示行 | Table登録 行2 | ヘッダー補助行 |
| 非表示列 | Table登録 I〜J, O〜P | 変更前値・内部列 |

### 2.0 シート可視性一覧


| No | シート | VBA による非表示化 | 表示するタイミング | 非表示にするタイミング | 制御プロシージャ |
| --- | --- | --- | --- | --- | --- |
| 1 | 使い方 | — | — | — | — |
| 2 | 項目TB | — | — | — | — |
| 3 | 抽出 | — | — | — | — |
| 4 | 抽出結果 | — | — | — | — |
| 5 | Table登録 | — | — | — | — |

> 以下の各シートのレイアウト構造表における ✓ = VBA から `Range()` で代入または参照され、業務ロジックに直結するセル

### 2.1 使い方

**目的**: このツールの操作手順（Ⅰ:データ抽出方法、Ⅱ:テーブル設定登録・修正方法）を記載した説明シート。

#### 非表示行・列

なし。

#### 操作概要（セル記載内容）

**Ⅰ データ抽出方法**:
1. 「項目TB」シートでテーブルを選択し「選択」コマンドをクリック → 「抽出」シートに遷移
2. 「抽出」をクリック
3. 表示したい項目は項目名または表示列を右クリックで選択（「する」に切替）
4. 表示不要の場合はC列の表示を「しない」にする
5. 必要に応じて抽出条件を設定（文字数は桁数と一致させること）
6. 最大抽出数以上データがある場合は条件を設定して再度抽出（最大30,000件）
7. 抽出結果を新しいBookに記録する場合は「Bookに記録」を使用

**Ⅱ テーブル設定登録・修正方法**:
1. 「項目TB」のテーブル一覧で新規/修正行を選択
2. 「Table登録」シートで入力して「登録」をクリック
3. 必須: テーブル名、DBテーブル名、DB、関係装置、項目名称、識別名、属性、桁数

---

### 2.2 項目TB

**目的**: Access DB の `テーブル一覧` テーブルから取得したテーブル一覧を表示する。ユーザーがテーブル行を選択して「選択」ボタンを押すと「抽出」シートへ遷移する。

#### 非表示行・列

| 種類 | 対象 | 備考 |
| --- | --- | --- |
| 非表示行 | 2 | ヘッダー補助行 |
| 非表示列 | C〜D | 内部列 |

#### レイアウト構造

| ✓ | No | セル | 名前付き範囲 | 種別 | 実態（値/数式/VBA代入） | 業務的意味 |
| --- | --- | --- | --- | --- | --- | --- |
|  | 1 | B3 | — | ラベル | ラベル「テーブル一覧」 | ラベル「テーブル一覧」 |
|  | 2 | B5〜I5 | — | ラベル | テーブル一覧ヘッダー（No/テーブルNo/関係装置/テーブル名/DBテーブル名/説明/DB） | テーブル一覧ヘッダー（No/テーブルNo/関係装置/テーブル名/DBテーブル名/説明/DB） |
|  | 3 | D6:I85 | `Table` | VBA代入 | テーブル一覧データ（Access DBから取得してVBAが書き込み） | テーブル一覧データ（Access DBから取得してVBAが書き込み） |
|  | 4 | F6:I78 | `KoumokuF` | VBA代入 | テーブル一覧の一部参照範囲 | テーブル一覧の一部参照範囲 |
|  | 5 | F6:F63 | `KoumokuTB` | VBA代入 | テーブル名一覧 | テーブル名一覧 |

（行番号 86〜224 は空白またはその他のデータ）

---

### 2.3 抽出

**目的**: 選択されたテーブルの項目一覧（表示順/表示有無/項目名/説明/識別子/属性/桁数）を表示し、抽出条件・ソートキーを設定する画面。

#### 非表示行・列

| 種類 | 対象 | 備考 |
| --- | --- | --- |
| 非表示行 | 2〜3 | 設定補助行 |
| 非表示列 | L〜M | 内部列 |

#### レイアウト構造

| ✓ | No | セル | 名前付き範囲 | 種別 | 実態（値/数式/VBA代入） | 業務的意味 |
| --- | --- | --- | --- | --- | --- | --- |
|  | 1 | A4 | — | ラベル | タイトル「照射管理システムのDBより各テーブルデータ抽出」 | タイトル「照射管理システムのDBより各テーブルデータ抽出」 |
|  | 2 | A7:B7 | — | ラベル | ラベル「読込むDBテーブル」（結合） | ラベル「読込むDBテーブル」（結合） |
|  | 3 | C7 | `DB_FileName` | VBA代入 | DBテーブル名（VBAが設定） | DBテーブル名（VBAが設定） |
|  | 4 | C8:H8 | `F_Setumei` | VBA代入 | ファイルの説明（VBAが設定、結合） | ファイルの説明（VBAが設定、結合） |
|  | 5 | D7 | `ReadDB` | VBA代入 | テーブル名（VBAが設定） | テーブル名（VBAが設定） |
|  | 6 | E7 | — | ラベル | ラベル「データベース」 | ラベル「データベース」 |
|  | 7 | E8 | `OrderKey` | VBA代入 | ソートキー | ソートキー |
|  | 8 | F7 | `DBName` | VBA代入 | DB名（UID として使用） | DB名（UID として使用） |
|  | 9 | H5 | `KoumokuRow` | VBA代入 | 項目行番号 | 項目行番号 |
|  | 10 | H7 | `T_TableNo` | VBA代入 | テーブルNo | テーブルNo |
|  | 11 | I7 | `TB_Name` | VBA代入 | テーブル名 | テーブル名 |
|  | 12 | J7 | `MaxDataN` | VBA代入 | 最大抽出件数（デフォルト30000） | 最大抽出件数（デフォルト30000） |
|  | 13 | J8 | `DataN` | VBA代入 | 項目数（VBAが設定） | 項目数（VBAが設定） |
|  | 14 | L8 | `KoumokuN` | VBA代入 | 選択項目数 | 選択項目数 |
|  | 15 | M8 | `Sort1` | VBA代入 | ソート1設定数 | ソート1設定数 |
|  | 16 | N8 | `Sort2` | VBA代入 | ソート2設定数 | ソート2設定数 |
|  | 17 | A10:J150 | `TyusyutuTB` | VBA代入 | 項目一覧テーブル（VBAが書き込み） | 項目一覧テーブル（VBAが書き込み） |
|  | 18 | H10:I150 | `Jyoukenn` | 入力 | 抽出条件入力欄（最小値・最大値） | 抽出条件入力欄（最小値・最大値） |

#### 項目一覧の列構造（行10以降）

| 列 | 内容 |
| --- | --- |
| A | 表示順（No） |
| B | 表示設定（する/空） |
| C | 項目名（日本語名、右クリックで「する」切替） |
| D | 説明（属性型） |
| E | 識別子（DBカラム名） |
| F | （内部使用） |
| G | 桁数 |
| H | 抽出条件下限値 |
| I | 抽出条件上限値 |
| J | ソート設定（ソート1/ソート2） |

---

### 2.4 抽出結果

**目的**: DBから抽出されたデータを表示する結果シート。最大30,000行×140列。ファイルサイズが大きい理由はこのシートの大量データが含まれているため。

#### 非表示行・列

なし。

#### レイアウト構造（ヘッダー部）

| ✓ | No | セル | 名前付き範囲 | 種別 | 実態（値/数式/VBA代入） | 業務的意味 |
| --- | --- | --- | --- | --- | --- | --- |
|  | 1 | `$A$1` | — | ラベル | テーブル名称： | ヘッダーラベル |
|  | 2 | `$B$1` | — | VBA代入 | テーブル名 | 抽出対象テーブル名 |
|  | 3 | `$A$2` | — | ラベル | テーブル内容： | ヘッダーラベル |
|  | 4 | `$B$2` | — | VBA代入 | テーブルの説明 | テーブル説明 |
|  | 5 | `$A$3` | — | ラベル | テーブル名　： | ヘッダーラベル |
|  | 6 | `$B$3` | — | VBA代入 | DBテーブル名 | DB 上のテーブル名 |
|  | 7 | `$A$4` | — | ラベル | データベース： | ヘッダーラベル |
|  | 8 | `$B$4` | — | VBA代入 | DB名（UID） | 接続先 DB |
|  | 9 | `$A$5` | — | ラベル | 抽出条件　　： | ヘッダーラベル |
|  | 10 | `$B$5` | — | VBA代入 | SQL WHERE 条件文字列 | 抽出条件 |
|  | 11 | `$A$6` | — | ラベル | 抽出件数　　： | ヘッダーラベル |
|  | 12 | `$B$6` | — | VBA代入 | 抽出されたレコード件数 | 件数 |
| ✓ | 13 | `$B$12:$…$30000` | — | VBA代入 | DB から取得したデータ | 抽出結果本体（最大30,000行） |

（データ行 12〜30000 は概要のみ記載：計X行のデータ）

---

### 2.5 Table登録

**目的**: テーブル定義（テーブル名・DBテーブル名・DB・関連装置・説明）および項目一覧（表示順・項目名称・識別子・キー・属性・桁数・説明）を登録・修正するフォームシート。

#### 非表示行・列

| 種類 | 対象 | 備考 |
| --- | --- | --- |
| 非表示行 | 2 | ヘッダー補助行 |
| 非表示列 | I〜J, O〜P | 変更前値・内部列 |

#### レイアウト構造

| ✓ | No | セル | 名前付き範囲 | 種別 | 実態（値/数式/VBA代入） | 業務的意味 |
| --- | --- | --- | --- | --- | --- | --- |
|  | 1 | B5 | `TableNo` | VBA代入 | テーブルNo（編集対象） | テーブルNo（編集対象） |
|  | 2 | C5 | `TableName` | VBA代入 | テーブル名 | テーブル名 |
|  | 3 | D5:E5 | `TableNameDB` | VBA代入 | DBテーブル名（結合） | DBテーブル名（結合） |
|  | 4 | F5 | `_xlnm.Database` | VBA代入 | （Excel データベース定義） | （Excel データベース定義） |
|  | 5 | G5 | `Souti` | VBA代入 | 関連装置 | 関連装置 |
|  | 6 | H5 | `Setumei` | VBA代入 | 説明 | 説明 |
|  | 7 | P5 | `HenKou1` | VBA代入 | テーブル一覧変更有無 | テーブル一覧変更有無 |
|  | 8 | P6 | `HenKou2` | VBA代入 | 項目一覧変更有無 | 項目一覧変更有無 |
|  | 9 | B5:O5 | `TableHed` | VBA代入 | ヘッダー行（変更前） | ヘッダー行（変更前） |
|  | 10 | B8:O207 | `TableData` | VBA代入 | 項目データ編集領域 | 項目データ編集領域 |

#### Table登録の列構造（行8以降）

| 列 | 内容 |
| --- | --- |
| B | 表示順 |
| C | 項目名称 |
| D | 識別子（DBカラム名） |
| E | キー |
| F | 属性 |
| G | 桁数 |
| H | 説明 |
| I〜O | （変更前の値 ※変更検出用） |
| P | 変更有無フラグ（数式） |

---

## 3. 名前付き範囲一覧

> ✓ = VBA から `Range()` で代入または参照され、業務ロジックに直結する名前付き範囲

| ✓ | No | 名前 | 参照先 | 業務的意味 |
| --- | --- | --- | --- | --- |
|  | 1 | `_xlnm.Database` | Table登録!$F$5 | Excel データベース範囲 |
|  | 2 | `DataN` | 抽出!$J$8 | 項目数（テーブルの全カラム数） |
|  | 3 | `DB_FileName` | 抽出!$C$7 | 選択されたDBテーブル名 |
|  | 4 | `DBName` | 抽出!$F$7 | データベース名（ODBCのUID として使用） |
|  | 5 | `Debug` | 使い方!$A$1 | デバッグフラグ |
|  | 6 | `F_Setumei` | 抽出!$C$8 | テーブルの説明文 |
|  | 7 | `HenKou1` | Table登録!$P$5 | テーブル一覧変更有無 |
|  | 8 | `HenKou2` | Table登録!$P$6 | 項目一覧変更有無 |
|  | 9 | `Jyoukenn` | 抽出!$H$10:$I$150 | 抽出条件入力欄（最小値/最大値） |
|  | 10 | `KoumokuF` | 項目TB!$F$6:$I$78 | テーブル一覧の参照範囲 |
|  | 11 | `KoumokuN` | 抽出!$L$8 | 選択項目数（「する」に設定された項目の数） |
|  | 12 | `KoumokuRow` | 抽出!$H$5 | 項目行番号 |
|  | 13 | `KoumokuTB` | 項目TB!$F$6:$F$63 | テーブル名一覧 |
|  | 14 | `MaxDataN` | 抽出!$J$7 | 最大抽出件数（デフォルト30,000） |
|  | 15 | `OrderKey` | 抽出!$E$8 | ソートキー |
|  | 16 | `ReadDB` | 抽出!$D$7 | 読み込むテーブル名（日本語名） |
|  | 17 | `Setumei` | Table登録!$H$5 | テーブルの説明（入力欄） |
|  | 18 | `Sort1` | 抽出!$M$8 | ソート1の設定数 |
|  | 19 | `Sort2` | 抽出!$N$8 | ソート2の設定数 |
|  | 20 | `Souti` | Table登録!$G$5 | 関連装置（入力欄） |
|  | 21 | `T_TableNo` | 抽出!$H$7 | 選択されたテーブルNo |
|  | 22 | `Table` | 項目TB!$D$6:$I$85 | テーブル一覧データ（D〜I列） |
|  | 23 | `TableData` | Table登録!$B$8:$O$207 | 項目データ編集領域 |
|  | 24 | `TableHed` | Table登録!$B$5:$O$5 | ヘッダー行（変更前値格納） |
|  | 25 | `TableName` | Table登録!$C$5 | テーブル名（入力欄） |
|  | 26 | `TableNameDB` | Table登録!$D$5 | DBテーブル名（入力欄） |
|  | 27 | `TableNo` | Table登録!$B$5 | テーブルNo（入力欄） |
|  | 28 | `TB_Name` | 抽出!$I$7 | テーブル名（別名） |
|  | 29 | `TyusyutuTB` | 抽出!$A$10:$J$150 | 項目一覧テーブル全体 |


### 3.1 データの入力規則

| シート | セル | 種別 | 制約 | 用途 |
| --- | --- | --- | --- | --- |
| 抽出 | `J7` | 整数 | <=30000 |  |
| 抽出 | `J10:J150` | リスト | リスト: `"ｿｰﾄ1,ｿｰﾄ2"` |  |
| Table登録 | `G5` | リスト | リスト: `"1号機,2号機,2/3号機,3号機,EB,共通,(Ex)1号機,(Ex)共通,ビュー"` |  |
| Table登録 | `F5:G5` | リスト | リスト: `"ric,rich,"` |  |
| Table登録 | `E8:E207` | リスト | リスト: `"〇"` |  |


---

---

## 4. 数式一覧

### Table登録シート

| セル | 数式（パターン） | 説明 |
| --- | --- | --- |
| P5〜P207 | `=IF(B5&C5&D5&E5&F5&G5&H5<>I5&J5&K5&L5&M5&N5&O5,1,0)` | 変更前(I〜O列)と現在値(B〜H列)を比較して変更有無を検出（1=変更あり） |

---

## 5. ボタン・マクロ対応

> ✓ = DB 更新・画面遷移・計算実行など副作用のある操作を起動するボタン

### 5.1 シート上のボタン（Form Control）

| ✓ | No | シート | ボタンラベル | 割り当てマクロ | 動作概要 |
| --- | --- | --- | --- | --- | --- |
|  | 1 | 項目TB | 選択 | `テーブル指定()` | 選択行のテーブルを指定し、抽出シートに項目一覧を表示 |
|  | 2 | 項目TB | テーブル設定の登録・修正 | `テーブル一覧修正登録()` | 選択行の定義をTable登録シートに表示（新規/修正） |
|  | 3 | Table登録 | 登録 | `テーブル登録()` | 入力内容をAccess DB（テーブル一覧/項目一覧）に保存 |
|  | 4 | 抽出 | 抽出 | `データ抽出()` | 選択された項目・条件でOracle DBからデータを抽出 |
|  | 5 | 抽出 | 全て表示 | `全て表示_非表示()` | 全項目の表示「する」/「」を一括切替 |

**右クリック操作（Sheet1.cls）**:
- B列（表示欄）: 空→「する」/「する」→空の切替

### 5.2 ショートカットキー

| No | マクロ名 | ショートカット | 処理概要 |
| --- | --- | --- | --- |
| 1 | `AllCls()` | **Ctrl+E** | 抽出結果・抽出設定・Table登録シートを全クリアし初期状態に戻す |
| 2 | `シートデータコピー()` | **Ctrl+B** | アクティブシートのデータをTable登録シートにコピー（通常は `Exit Sub` で無効化） |

### 5.3 ユーザーフォーム上のボタン（サマリ）

なし。

### 5.4 CommandBar に動的追加されるボタン

なし。

## 6. VBA モジュール仕様

### 6.0 全プロシージャ一覧

> ✓ = ユーザー操作の起点（Click イベント等） / DB I/O を実行 / 他モジュールから呼び出される Public

| ✓ | No | モジュール | プロシージャ | スコープ | 種別 | 概要 |
| --- | --- | --- | --- | --- | --- | --- |
| ✓ | 1 | **ThisWorkbook** | `Workbook_Open()` | Private | Event | ブック起動時にシート保護・画面クリア・テーブル一覧表示 |
| ✓ | 2 | **ThisWorkbook** | `Workbook_BeforeClose()` | Private | Event | 保存ダイアログ抑止 |
| ✓ | 3 | **テーブル選択** | `テーブル指定()` | Public | Sub | 選択行のテーブルを指定し項目一覧を表示 |
| ✓ | 4 | **テーブル選択** | `項目名表示()` | Public | Sub | Access DBから項目一覧を取得して表示 |
| ✓ | 5 | **抽出設定** | `データ抽出()` | Public | Sub | Oracle DBに動的SQLを発行し結果をシートに表示 |
|  | 6 | **抽出設定** | `新しいBookに記録()` | Public | Sub | 抽出結果を新規ブックにコピー |
|  | 7 | **抽出設定** | `全て表示_非表示()` | Public | Sub | 全項目の表示/非表示を一括切替 |
| ✓ | 8 | **テーブル一覧** | `テーブル一覧表示()` | Public | Sub | Access DBからテーブル一覧を取得して表示 |
| ✓ | 9 | **テーブル一覧** | `テーブル一覧修正登録()` | Public | Sub | テーブル定義をTable登録シートに読込 |
| ✓ | 10 | **テーブル一覧** | `テーブル登録()` | Public | Sub | Table登録シートの内容をAccess DBに登録 |
| ✓ | 11 | **SQL_Execution** | `Open_oraconDB()` | Public | Sub | Oracle DB接続（ODBC） |
| ✓ | 12 | **SQL_Execution** | `SQL_Exe()` | Public | Sub | SQL実行 |
| ✓ | 13 | **SQL_Execution** | `SQL_INSERT_UPDATE()` | Public | Sub | INSERT/UPDATE汎用処理 |
| ✓ | 14 | **SQL_Execution** | `SQL_Delete()` | Public | Sub | DELETE汎用処理 |
| ✓ | 15 | **SQL_Execution** | `Disp_Sheet()` | Public | Sub | SQL結果をシートに出力 |
| ✓ | 16 | **SQL_Execution** | `Set_Array()` | Public | Sub | SQL結果を配列に格納 |
| ✓ | 17 | **SQL_AccessExe** | `Open_AccessDB()` | Public | Sub | Access DB接続（OLE DB） |
| ✓ | 18 | **SQL_AccessExe** | `SQL_Access_Exe()` | Public | Sub | Access SQL実行 |
| ✓ | 19 | **SQL_AccessExe** | `SQL_Access_INSERT_UPDATE()` | Public | Sub | Access INSERT/UPDATE |
| ✓ | 20 | **SQL_AccessExe** | `SQL_Access_DELETE()` | Public | Sub | Access DELETE |
| ✓ | 21 | **SQL_AccessExe** | `Disp_Access_Sheet()` | Public | Sub | Access結果をシートに出力 |
| ✓ | 22 | **SQL_AccessExe** | `Set_Access_Array()` | Public | Sub | Access結果を配列に格納 |
|  | 23 | **SQL_AccessExe** | `Set_Access_Close()` | Public | Sub | Access接続クローズ |
|  | 24 | **シートの保護** | `シート保護()` | Public | Sub | 各シートに保護を設定 |
| ✓ | 25 | **画面クリア** | `AllCls()` | Public | Sub | 全画面クリア（ショートカット Ctrl+E） |
|  | 26 | **ユーティリティ** | `一覧登録()` | Public | Sub | 項目TBシートをAccess DBに一括登録 |
|  | 27 | **ユーティリティ** | `シートデータコピー()` | Public | Sub | アクティブシートをTable登録にコピー（通常無効化） |
|  | 28 | **Sheet1** | `Worksheet_BeforeRightClick()` | Private | Event | B列の表示/非表示トグル |

### 6.1 ThisWorkbook

#### `Workbook_Open()`

**処理概要**: ブック起動時に各シートを保護し、画面をクリアして最大抽出件数を 30,000 に設定し、テーブル一覧を表示する。

```vba
Private Sub Workbook_Open()
    Worksheets("抽出").Select
    Call シート保護
    Call AllCls
    Range("MaxDataN") = 30000
    Call テーブル一覧表示
End Sub
```

#### `Workbook_BeforeClose()`

**処理概要**: 保存ダイアログを抑止してブックを閉じる。

---

### 6.2 テーブル選択

#### `テーブル指定()`

**処理概要**: 「項目TB」シートでアクティブなセルのテーブルを選択し、対応するテーブル名・DBテーブル名・DB・説明を「抽出」シートの対応セルに設定する。その後 `項目名表示()` を呼び出す。

**処理フロー**:
1. アクティブセルが `Table` 範囲内かチェック
2. `T_TableNo`・`ReadDB`・`DB_FileName`・`DBName`・`F_Setumei` に値を設定
3. `項目名表示()` → Access DB の `項目一覧` から項目を取得して `TyusyutuTB` に表示

#### `項目名表示()`

**処理概要**: Access DB の `項目一覧` テーブルから指定 `テーブルNo` に対応する項目を取得して「抽出」シートに書き込む。

```sql
SELECT 表示順, '', 項目名称, 説明, 識別子, 属性, 桁数
FROM 項目一覧
WHERE テーブルNo = <テーブルNo>
ORDER BY 表示順
```

---

### 6.3 抽出設定

#### `データ抽出()`

**処理概要**: 「抽出」シートの設定（表示項目・条件・ソート）に基づいてOracle DBに動的SQLを発行し、結果を「抽出結果」シートに表示する。

**処理フロー**:
1. 入力検証（項目選択あり・ソート重複なし・DBName 設定あり）
2. Oracle DB接続文字列を設定: `DSN=ricdb;UID=<DBName>;PWD=t6101`
3. `TyusyutuTB`（項目一覧）から抽出条件（H/I列）を読み取り WHERE 句を生成
   - DATE型の場合: `TO_DATE(...,'yyyy/mm/dd hh24:mi:ss')`
   - その他: 文字列比較
4. 表示が「する」の項目の識別子を SELECT 句に追加
5. ソート設定に応じて `ROW_NUMBER() over(ORDER BY ...)` または `ROWNUM` を追加
6. `Disp_Sheet()` でデータを抽出結果シートの12行目から書き込み
7. DATE型カラムに `yyyy/mm/dd hh:mm` 書式を適用
8. 30,000件以上の場合は警告メッセージ
9. 「新しいBookに記録しますか？」の確認

**主要 SQL**:
```sql
-- ソートキーあり
SELECT ROW_NUMBER() over(ORDER BY <識別子1>, <識別子2>) as row_number,
       <識別子1>, <識別子2>, ...
FROM <DBテーブル名>
WHERE <識別子> >= '<下限値>' AND <識別子> <= '<上限値>' AND
      ROWNUM <= 30000

-- ソートキーなし
SELECT ROWNUM, <識別子1>, <識別子2>, ...
FROM <DBテーブル名>
WHERE ROWNUM <= 30000
```

#### `新しいBookに記録()`

**処理概要**: 「抽出結果」シートを新しいブックにコピーしてユーザーに提示する。

#### `全て表示_非表示()`

**処理概要**: ボタンテキストを「全て表示」↔「全て非表示」で切替えながら、`TyusyutuTB` の全行の表示列（B列）を「する」/空 に一括設定する。

---

### 6.4 テーブル一覧

#### `テーブル一覧表示()`

**処理概要**: 起動時に Access DB の `テーブル一覧` から全テーブルを取得して「項目TB」シートの `Table` 領域に表示する。

```sql
SELECT テーブルNo, 関連装置, テーブル名, DBテーブル名, 説明, DB
FROM テーブル一覧
ORDER BY 関連装置, DBテーブル名
```

#### `テーブル一覧修正登録()`

**処理概要**: 「項目TB」シートでアクティブ行のテーブルを「Table登録」シートに読み込む。新規の場合は最大テーブルNo+1 を設定する。

#### `テーブル登録()`

**処理概要**: 「Table登録」シートの入力内容を Access DB の `テーブル一覧` および `項目一覧` テーブルに INSERT/UPDATE する。変更有無（`HenKou1`/`HenKou2`）に応じて処理を分岐する。

```sql
-- テーブル一覧への登録
INSERT/UPDATE テーブル一覧 SET (テーブルNo, テーブル名, DBテーブル名, DB, 関連装置, 説明)

-- 項目一覧の既存データ削除後に再登録
DELETE FROM 項目一覧 WHERE テーブルNo = <テーブルNo>
INSERT INTO 項目一覧 (テーブルNo, 表示順, 項目名称, 識別子, キー, 属性, 桁数, 説明)
```

---

### 6.5 SQL_Execution（Oracle DB 用）

`Open_oraconDB`、`SQL_Exe`、`SQL_INSERT_UPDATE`、`SQL_Delete`、`Disp_Sheet`、`Set_Array` の共通処理。

**接続文字列**: `mpDSN = "DSN=ricdb;UID=" & Range("DBName") & ";PWD=t6101"`

> 注意: UID は `DBName` セルの値が使用される（固定 `ric` ではなく、ユーザーが指定したDB名がそのまま UID として使われる）。接続先は照射管理システム（Oracle DB）。

---

### 6.6 SQL_AccessExe（Access DB 用）

`Open_AccessDB`、`SQL_Access_Exe`、`SQL_Access_INSERT_UPDATE`、`SQL_Access_DELETE`、`Disp_Access_Sheet`、`Set_Access_Array`、`Set_Access_Close` の Access DB 専用の接続・SQL 実行処理。

**接続方式**: Microsoft.Ace.OLEDB.12.0（OLE DB プロバイダー）
**DB パス**: `\\163.59.144.156\ExRicSys\テーブル一覧.accdb`（ネットワークパス固定）

---

### 6.7 シートの保護

#### `シート保護()`

**処理概要**: 起動時に各シートに保護を設定する。ソート・フィルタは許可。セル選択制限はシートによって異なる。

---

### 6.8 画面クリア

#### `AllCls()` (Ctrl+e)

**処理概要**: 抽出結果シートのデータ・抽出設定・Table登録シートを全クリアし、初期状態に戻す。

---

### 6.9 ユーティリティ

#### `一覧登録()`

**処理概要**: 「項目TB」シートのテーブル一覧を Access DB の `テーブル一覧` テーブルに一括登録する（初期データ投入用）。

#### `シートデータコピー()` (Ctrl+b)

**処理概要**: アクティブシートのデータを「Table登録」シートにコピーする（運用開始時の一括登録用）。通常は `Exit Sub` で無効化されている。

---

## 7. ユーザーフォーム仕様

なし。（ユーザーフォームなし）。

## 8. DB 接続・外部連携

### 8.1 ODBC 接続設定

本ファイルは 2 系統の DB 接続を使用する。

| 接続方式 | 接続先 | UID | PWD | 用途 |
| --- | --- | --- | --- | --- |
| ADO + ODBC | DSN=`ricdb` | `<DBName セルの値>` | `t6101` | 各業務テーブルのデータ抽出・参照 |
| ADO + ACE.OLEDB.12.0 | `\\163.59.144.156\ExRicSys\テーブル一覧.accdb` | — | — | テーブル定義・項目一覧の管理 |

> **UID の動的設定**: Oracle 接続文字列は `"DSN=ricdb;UID=" & Range("DBName") & ";PWD=t6101"` で生成される。`DBName` セルにユーザーが入力した DB 名が UID として使用される。
> **DB サーバー IP**: 163.59.144.156（Access DB 共有フォルダ）

### 8.2 テーブル一覧（参照/更新区分付き）

> ✓ = INSERT / UPDATE / DELETE の対象テーブル（参照のみのテーブルは ✓ なし）

| ✓ | No | テーブル名 | 区分 | DB | 主な用途 | キー列 | 参照/更新列 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| ✓ | 1 | `テーブル一覧` | **参照＋更新** | Access | 抽出可能なDBテーブルのメタ情報 | `テーブルNo` | 更新: `テーブル名`, `DBテーブル名`, `DB`, `関連装置`, `説明` |
| ✓ | 2 | `項目一覧` | **参照＋更新（DELETE+INSERT）** | Access | 各テーブルのカラム定義 | `テーブルNo` | 更新: `表示順`, `項目名称`, `識別子`, `キー`, `属性`, `桁数`, `説明` |

Oracle DB 上の業務テーブル（`zaiko`, `sehmst`, `sengnr1` 等）は `テーブル一覧.accdb` に登録されているものすべてが抽出可能（参照のみ）。テーブルの一覧自体は Access DB で管理する。

### 8.3 SQL 一覧

#### 8.3.1 項目名表示（`項目名表示()` / **テーブル選択.bas**）

```sql
SELECT 表示順, '', 項目名称, 説明, 識別子, 属性, 桁数
FROM 項目一覧
WHERE テーブルNo = <テーブルNo>
ORDER BY 表示順
```

#### 8.3.2 テーブル一覧表示（`テーブル一覧表示()` / **テーブル一覧.bas**）

```sql
SELECT テーブルNo, 関連装置, テーブル名, DBテーブル名, 説明, DB
FROM テーブル一覧
ORDER BY 関連装置, DBテーブル名
```

#### 8.3.3 データ抽出（`データ抽出()` / **抽出設定.bas**）

Oracle 業務テーブルに対する動的 SELECT。表示項目・WHERE（H/I列）・ORDER BY を「抽出」シートの設定から組み立てる。UID は `DBName` セルの値。

---


### 8.4 外部ファイル連携

なし。外部ファイル連携は存在しない。

## 9. データフロー

各フローは「起点 → 処理 → 結果」の粒度で記述する。

### 9.1 起動フロー

| No | 起点 | 処理 | 結果 |
| --- | --- | --- | --- |
| 1 | 📄 ブックOpen | 📄 `Workbook_Open()` / **ThisWorkbook.cls** | 「抽出」を選択 |
| 2 | 上記 | 📊 `シート保護()` / **シートの保護.bas** | 「項目TB」「抽出」「Table登録」を保護 |
| 3 | 上記 | 📊 `AllCls()` / **画面クリア.bas** | 全シートをクリア |
| 4 | 上記 | 📊 `MaxDataN` = 30000 | 件数上限を設定 |
| 5 | 上記 | 🗄️ `テーブル一覧表示()` / **テーブル一覧.bas** | Access `テーブル一覧` を SELECT |
| 6 | 上記 | 📊 「項目TB」`Table` 領域に書込み | テーブル一覧を表示 |

#### ツリー図（補助）

```
（ブックOpen）
└─ 📄 Workbook_Open              [ThisWorkbook.cls]
   ├─ 📊 シート保護               [シートの保護.bas]
   ├─ 📊 AllCls                   [画面クリア.bas]
   ├─ 📊 MaxDataN = 30000
   └─ 🗄️ テーブル一覧表示         [テーブル一覧.bas]
      └─ 🗄️ Access テーブル一覧 SELECT → 📊「項目TB」Table に書込み
```

### 9.2 テーブル選択・項目表示フロー

| No | 起点 | 処理 | 結果 |
| --- | --- | --- | --- |
| 1 | 📊（選択 Click） | 📄 `テーブル指定()` / **テーブル選択.bas** | 選択行のテーブルを確定 |
| 2 | 上記 | 📊 `T_TableNo` / `ReadDB` / `DB_FileName` / `DBName` / `F_Setumei` 書込み | 「抽出」シートへ転記 |
| 3 | 上記 | 🗄️ `項目名表示()` / **テーブル選択.bas** | Access `項目一覧` を SELECT |
| 4 | 上記 | 📊 「抽出」`TyusyutuTB` に書込み | 項目一覧を表示 |

#### ツリー図（補助）

```
（選択 Click）
└─ 📄 テーブル指定                [テーブル選択.bas]
   ├─ 📊 T_TableNo / ReadDB / DB_FileName / DBName / F_Setumei 書込み
   └─ 🗄️ 項目名表示               [テーブル選択.bas]
      └─ 🗄️ Access 項目一覧 SELECT → 📊「抽出」TyusyutuTB に書込み
```

### 9.3 データ抽出フロー

| No | 起点 | 処理 | 結果 |
| --- | --- | --- | --- |
| 1 | 📊 「抽出」B列を「する」、H/I に条件、J にソート | 📄 抽出条件の確定 | WHERE / SELECT / ORDER BY の材料 |
| 2 | 📊（抽出 Click） | 📄 `データ抽出()` / **抽出設定.bas** | SQL 組立を開始 |
| 3 | 上記 | 📄 WHERE / SELECT / ORDER BY を動的生成 | SQL 文字列 |
| 4 | 上記 | 🗄️ `Disp_Sheet()` / **SQL_Execution.bas** | Oracle（`DSN=ricdb`, UID=`DBName`）から SELECT |
| 5 | 上記 | 📊 「抽出結果」12行目〜に表示 | DATE 型に書式設定 |
| 6 | [30000件超] | 🖥️ 警告 MsgBox | 件数超過を通知 |
| 7 | 🖥️「新しいBookに記録しますか？」 | 📄 `新しいBookに記録()` / **抽出設定.bas** | 抽出結果を新規ブックへコピー |

#### ツリー図（補助）

```
（抽出 Click）
└─ 📄 データ抽出                  [抽出設定.bas]
   ├─ 📊 B/H/I/J 列から条件読取
   ├─ 📄 WHERE / SELECT / ORDER BY 動的生成
   ├─ 🗄️ Disp_Sheet              [SQL_Execution.bas]（Oracle SELECT）
   │   └─ 📊「抽出結果」12行目〜に表示
   ├─ [30000件超] 🖥️ 警告 MsgBox
   └─ 🖥️「新しいBookに記録しますか？」
      └─ 📄 新しいBookに記録      [抽出設定.bas]
```

### 9.4 テーブル定義登録・修正フロー

| No | 起点 | 処理 | 結果 |
| --- | --- | --- | --- |
| 1 | 📊（テーブル設定の登録・修正 Click） | 📄 `テーブル一覧修正登録()` / **テーブル一覧.bas** | 新規 / 修正を分岐 |
| 2 | [新規] | 📄 テーブルNo を最大値+1 に設定 | 「Table登録」へ遷移 |
| 3 | [修正] | 🗄️ Access から既存定義を読込 | 「Table登録」に表示 |
| 4 | 📊（登録 Click） | 📄 `テーブル登録()` / **テーブル一覧.bas** | 入力チェック |
| 5 | 上記 | 🗄️ Access `テーブル一覧` / `項目一覧` を INSERT/UPDATE | 定義を保存 |
| 6 | 上記 | 🗄️ `テーブル一覧表示()` → 📊 「項目TB」を更新 | 一覧を再表示 |

#### ツリー図（補助）

```
（テーブル設定の登録・修正 Click）
└─ 📄 テーブル一覧修正登録        [テーブル一覧.bas]
   ├─ [新規] 📄 テーブルNo = 最大+1 → 📊「Table登録」
   └─ [修正] 🗄️ Access 既存定義 → 📊「Table登録」
      └─ 📊（登録 Click）
         └─ 📄 テーブル登録       [テーブル一覧.bas]
            ├─ 🗄️ Access テーブル一覧 / 項目一覧 INSERT/UPDATE
            └─ 🗄️ テーブル一覧表示 → 📊「項目TB」更新
```

---

## 10. セキュリティ注意事項


| No | カテゴリ | 内容 | リスク |
| --- | --- | --- | --- |
| 1 | 認証情報ハードコード | DSN=`ricdb`, UID=`<DBName セル>` , PWD=`t6101` が VBA ソースに平文記載 | 中：VBAエディタで閲覧可能 |
| 2 | 社内アドレス露出 | VBA および Access DB パスに DB サーバー IP がハードコード | 低：社内アドレスの露出 |

## スコープ外（本仕様書に含まないもの）

- セル書式（色・罫線・フォント）
- 条件付き書式、グラフ・画像、印刷設定

必要な場合は Excel 画面のスクリーンショットで補完してください。
