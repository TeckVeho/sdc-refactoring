# ExRIC3線量不足報告書 仕様書

> **対象ファイル**: ExRIC3線量不足報告書.xlsm
> **ファイル種別**: .xlsm（マクロ付き）
> **用途**: RIC3号機で線量不足が発生した際の追加照射指示書（工程確認・追加照射指示書）を作成・印刷する
> **VBA プロジェクト**: モジュール 10 本（.bas 6 / .cls 4 / .frm 0）
> **外部連携**: DSN=ricdb（ODBC経由 ADO接続）、UID=ric / PWD=t6101
> **解析日**: 2026-06-29（excel-to-md スキルによる自動解析）

---

## 凡例（本仕様書の表記ルール）

本仕様書では、保守時の判別を容易にするため、以下の表記ルールを使用します。

### 用語規約

| 用語 | 意味 |
| --- | --- |
| EXメニュー | ExRicSys フォルダに配置される VBA ファイル群（Ex*.xlsm）の総称 |
| `ExRicSys` | 配置フォルダ名。初出時は〈EXメニュー配置フォルダ〉と注記 |

### 表記規則

| 種別 | 表記 | 例 |
| --- | --- | --- |
| モジュール（.bas / .cls） | **太字** | **スタート処理.bas** |
| ユーザーフォーム（.frm） | **太字** | **SoutiSenntaku.frm** |
| プロシージャ / イベント | `コード体()` | `生産情報開始処理()` ※Markdown 制約上、斜体ではなくコード体で統一 |
| シート名 | 「」 | 「未処理品一覧」 |
| セル参照 | `コード体` | `$W$3` |
| 名前付き範囲 | `コード体` | `RangeName` |
| DB テーブル / カラム | `コード体` | `TableName` / `column` |
| ユーザー操作 | （操作名） | （実行ボタン Click） |
| 主要マーク | ✓ | ✓ = 保守時に最初に確認すべき項目 |

### データフロー 場所マーク（9章）

9章のデータフロー（テーブル・ツリー図）では、処理が行われる場所を以下のアイコンで区別します。

| アイコン | 種別 | 意味 |
| --- | --- | --- |
| 📊 | シート操作 | ワークシート上のセル書込み・読取り・表示変更 |
| 🖥️ | 画面操作 | ユーザーフォーム（.frm）の表示・入力・操作 |
| 🗄️ | DB操作 | DB への SELECT / INSERT / UPDATE / DELETE |
| 📄 | VBA内部処理 | 変数計算・条件分岐など、画面・シートに直接関与しない処理 |

### ✓（主要マーク）の判定基準

✓ は **保守時に最初に確認すべき項目** を示します。
判定基準は対象の種類ごとに以下のとおりです。

| 章 | 対象 | ✓ の判定基準 |
| --- | --- | --- |
| 1.1 | シート | ユーザーが直接操作する、または VBA が動的に表示/非表示を切り替える |
| 1.2 | ユーザーフォーム | ユーザー入力を受け付ける、または業務フローの起点となる |
| 1.3 / 6.0 | VBA モジュール | ① ユーザー操作の起点 ② DB I/O を含む ③ 他モジュールから呼び出される ④ コード行数上位 25% のいずれか |
| 2 | セル / 名前付き範囲 | VBA から `Range()` で代入または参照され、業務ロジックに直結する |
| 3 | 名前付き範囲 | VBA から `Range()` で代入または参照され、業務ロジックに直結する |
| 5 | ボタン / コントロール | DB 更新・画面遷移・計算実行など副作用のある操作を起動する |
| 6.0（全プロシージャ） | プロシージャ | ① ユーザー操作の起点（Click イベント等） ② DB I/O を実行 ③ 他モジュールから呼び出される Public のいずれか |
| 7 | フォームコントロール | ユーザー入力を受け付ける、またはイベントで業務処理を起動する |
| 8.2 | DB テーブル | INSERT / UPDATE / DELETE の対象（参照のみのテーブルは ✓ なし） |

---

## 目次

1. [ファイル構成](#1-ファイル構成)
2. [シート詳細](#2-シート詳細)
3. [名前付き範囲一覧](#3-名前付き範囲一覧)
4. [数式一覧](#4-数式一覧)
5. [ボタン・マクロ対応](#5-ボタンマクロ対応)
6. [VBA モジュール仕様](#6-vba-モジュール仕様)
7. [ユーザーフォーム仕様](#7-ユーザーフォーム仕様)
8. [DB 接続・外部連携](#8-db-接続外部連携)
9. [データフロー](#9-データフロー)
10. [セキュリティ注意事項](#10-セキュリティ注意事項)

---

## 1. ファイル構成

### 1.1 シート一覧

> ✓ = ユーザーが直接操作する、または VBA が動的に表示/非表示を切り替えるシート

| ✓ | No | シート名 | 最大行 | 最大列 | 保存時 Visible | VBA による動的切替 |
| --- | --- | --- | --- | --- | --- | --- |
| ✓ | 1 | 報告書 | 39 | 60 (BH) | visible | シート保護 ON/OFF 切替あり |
|  | 2 | 試験成績書用紙 | 61 | 31 (AE) | visible | — |
|  | 3 | 改訂履歴 | 112 | 6 (F) | visible | `ブック保存()` 完了後に Select |

### 1.2 ユーザーフォーム一覧

> ✓ = ユーザー入力を受け付ける、または業務フローの起点となるフォーム

ユーザーフォームは存在しない。

### 1.3 VBA モジュール一覧

> ✓ = ユーザー操作の起点 / DB I/O を含む / 他モジュールから呼び出される / コード行数上位 25%
> 全モジュールを列挙し、✓ 基準に該当するものにマークを付ける。

| ✓ | No | モジュール | 種別 | プロシージャ数 | 主な役割 |
| --- | --- | --- | --- | --- | --- |
| ✓ | 1 | **ThisWorkbook.cls** | .cls | 5 | 起動/終了/印刷前イベント・入力チェック |
| ✓ | 2 | **Sheet1.cls** | .cls | 1 | 「報告書」シートイベント（セル変更検知） |
|  | 3 | **Sheet5.cls** | .cls | 0 | 「試験成績書用紙」シート（コードなし） |
|  | 4 | **Sheet2.cls** | .cls | 0 | 「改訂履歴」シート（コードなし） |
| ✓ | 5 | **初期化.bas** | .bas | 3 | 画面初期設定・クリア処理 |
| ✓ | 6 | **データ抽出SQL.bas** | .bas | 6 | DB からの線量不足データ抽出・表示 |
| ✓ | 7 | **SQL_Execution.bas** | .bas | 6 | ADO/ODBC 汎用 DB 接続・SQL 実行エンジン |
|  | 8 | **印刷.bas** | .bas | 1 | 印刷範囲設定 |
|  | 9 | **Debug用.bas** | .bas | 1 | デバッグ用イベント復旧 |
| ✓ | 10 | **改定履歴.bas** | .bas | 1 | サーバーへのブック保存 |

> **Sheet*.cls の記載ルール**: コードネーム（Sheet1, Sheet5, Sheet2）と対応するシートタブ名を
> 必ず「」で囲んで紐づける。コードがある場合は「<シート名>」シートイベント、
> コードがない（`Option Explicit` のみ）場合は「<シート名>」シート（コードなし）と記載する。

---

## 2. シート詳細

### 2.0 シート可視性一覧

| No | シート | VBA による非表示化 | 表示するタイミング | 非表示にするタイミング | 制御プロシージャ |
| --- | --- | --- | --- | --- | --- |
| 1 | 報告書 | — | — | — | — |
| 2 | 試験成績書用紙 | — | — | — | — |
| 3 | 改訂履歴 | — | — | — | — |

> 以下の各シートのレイアウト構造表における ✓ = VBA から `Range()` で代入または参照され、業務ロジックに直結するセル

### 2.1 報告書

**目的**: RIC3号機の線量不足報告書（工程確認・追加照射指示書）の帳票。DB から取得した測定データ・追加照射計画を表示し、印刷用帳票として出力する。

#### 非表示行・列

| 種類 | 対象 | 備考 |
| --- | --- | --- |
| 非表示行 | 行 34〜39（6行） | VBA 作業領域（VLOOKUP 用インデックス行・DB データ中間格納） |
| 非表示列 | AO〜BH 列 | マスタテーブル格納域（製品目的コード、素子異常、線量計種類、社員データ、設定値） |

#### レイアウト構造

```
行 1: ヘッダー（様式番号・品質記録区分・作成日）
行 2: タイトル「３号機線量不足報告書（工程確認･追加照射指示書）」
行 3: 追加照射回数表示・データ件数上限警告
行 5: セクション見出し「３号機測定データ」「ﾁｪｯｸ(測定者）」
行 6〜7: 照射管理番号・測定日・測定者・再測定者・測定線量・製品・指定ﾊﾟｽ等
行 8〜13: 追加照射対象製品一覧（受付番号・RIC-NO・指定線量・下限・上限・実パス・会社情報）×5件
行 15〜16: 線量計番号・線量計種類・厚さ・計算式コード
行 17〜22: 追加照射データ明細（数式で自動計算：追加線量・追加パス数等）×5件
行 24: 警告メッセージ行（測定データ不適切 / 追加パス数不一致警告）
行 30: 注記「本紙は照射計画作成後作業指図書に貼付すること」
行 34〜36: 非表示作業域（VLOOKUP インデックス・中間データ取得行）
```

| ✓ | No | セル | 名前付き範囲 | 種別 | 実態（値/数式/VBA代入） | 業務的意味 |
| --- | --- | --- | --- | --- | --- | --- |
| ✓ | 1 | `$A$1` | `Debug` | 手動設定 | 空欄 or 任意値 | デバッグモード判定フラグ（空＝本番、値あり＝デバッグ） |
| ✓ | 2 | `$B$7` | `SenkNo` | VBA代入 | ドロップダウン（入力規則リスト） | 照射管理番号（線量計番号の選択。全フローの起点） |
| ✓ | 3 | `$G$7` | `SokDate` | VBA代入 | `線量不足データ抽出()` で書込み | 測定日 |
| ✓ | 4 | `$K$7` | `SoktCD` | VBA代入 | `線量不足データ抽出()` で書込み | 測定者名 |
| ✓ | 5 | `$O$7` | `SaiSoku` | 手動設定 | ユーザー入力 | 再測定者（印刷前チェック対象） |
| ✓ | 6 | `$S$7` | `SokutSN` | VBA代入 | `線量不足データ抽出()` で書込み | 測定線量値 |
| ✓ | 7 | `$W$7` | `Tani` | VBA代入 | `線量不足データ抽出()` で書込み | 製品目的（医療機器/一般品/試験照射） |
| ✓ | 8 | `$AA$7` | `Pass` | VBA代入 | `線量不足データ抽出()` で書込み | 指定パス数 |
| ✓ | 9 | `$AD$7` | `SikiKodo` | VBA代入 | `線量不足データ抽出()` で書込み | 計算式コード |
| ✓ | 10 | `$AG$7` | `Sosi` | 手動設定 | ユーザー入力 | 素子異常有無（印刷前チェック対象） |
| ✓ | 11 | `$AJ$7` | `Atusa` | VBA代入 | `線量不足データ抽出()` で書込み | 素子厚さ（mm） |
| ✓ | 12 | `$E$3` | `Tuika` | VBA代入 | `線量不足データ抽出()` で書込み | 追加照射回数（N回目） |
| ✓ | 13 | `$B$9:$AN$13` | `Data2` | VBA代入 | `線量不足データ抽出()` で各セルに書込み | 追加照射対象製品情報5件分 |
| ✓ | 14 | `$G$7:$AN$7` | `Data1` | VBA代入 | `線量不足データ抽出()` で書込み | 3号機測定データ1行分 |
| ✓ | 15 | `$G$16` | `SEnKind` | VBA代入 | ドロップダウン選択 → `Worksheet_Change` 検知 | 線量計種類（入力時に計算式コード自動取得） |
| ✓ | 16 | `$O$16` | `AtusaT` | 手動設定 | ユーザー入力 | 厚さ（印刷前チェック対象） |
| ✓ | 17 | `$W$16` | `Keisask` | VBA代入 | `計算式コード取得()` で書込み | 有効計算式コード |
| ✓ | 18 | `$BG$3` | `SebkNoTB` | VBA代入 | `線量不足線量計()` で書込み | 線量不足データ件数 |
| ✓ | 19 | `$BG$5:$BG$36` | `SyainTB` | DB読込 | `社員データ()` で DB → シート格納 | 有資格社員名リスト |
| ✓ | 20 | `$BC$10:$BC$12` | `SenSyu` | DB読込 | `線量計種類()` で DB → シート格納 | 線量計種類マスタ |
| ✓ | 21 | `$BC$3:$BD$6` | `mokuteki` | 設定値 | 固定値（1:医療機器, 2:一般品, 3:試験照射） | 製品目的コード変換テーブル |
| ✓ | 22 | `$BC$21` | `TuikaMin` | 設定値 | 固定値 `5` | 線量計高・低線量切替閾値（kGy） |
|  | 23 | `$BH$3` | — | 設定値 | 固定値 `30` | 追加照射データ最大件数 |
| ✓ | 24 | `$B$9` | `UkeNo1` | VBA代入 | `線量不足データ抽出()` で書込み | 受付番号（1件目） |
| ✓ | 25 | `$G$9` | `RicNoS1` | VBA代入 | `線量不足データ抽出()` で書込み | 開始RIC-NO（1件目） |
| ✓ | 26 | `$K$9` | `RicNoE1` | VBA代入 | `線量不足データ抽出()` で書込み | 終了RIC-NO（1件目） |
| ✓ | 27 | `$O$9` | `SiteiSN1` | VBA代入 | `線量不足データ抽出()` で書込み | 指定線量（1件目） |
| ✓ | 28 | `$S$9` | `Kagen1` | VBA代入 | `線量不足データ抽出()` で書込み | 下限線量（1件目） |
| ✓ | 29 | `$W$9` | `Jyoug1` | VBA代入 | `線量不足データ抽出()` で書込み | 上限線量（1件目） |
| ✓ | 30 | `$AD$9` | `KaiCD1` | VBA代入 | `線量不足データ抽出()` で書込み | 会社コード（1件目） |
| ✓ | 31 | `$AG$9` | `KaiName1` | VBA代入 | `線量不足データ抽出()` で書込み | 会社名（1件目） |
| ✓ | 32 | `$AA$9` | `JituP1` | VBA代入 | `線量不足データ抽出()` で書込み | 実パス数（1件目） |

> 2〜5件目（`UkeNo2`〜`UkeNo5`, `RicNoS2`〜`RicNoS5` 等）は1件目と同構造で行 10〜13 に配置。

### 2.2 試験成績書用紙

**目的**: 「３号機線量不足報告書」作成プログラムのテスト仕様書・結果報告書。開発時の品質管理文書として使用。

#### 非表示行・列

なし。

#### レイアウト構造

```
行 2: タイトル「「３号機線量不足報告書」作成プログラムテスト仕様書／結果報告書」
行 10: サブタイトル「機能追加（項目No4)に伴うテスト」
行 12〜13: 列見出し（No / 検査項目 / 検査手順 / 確認方法 / 判定）
行 15〜52: テスト項目（起動時 / 画面表示 / 印刷 / 追加機能 / 終了）
行 55: 判定結果行
行 57〜59: 確認日・検証担当者
```

| ✓ | No | セル | 名前付き範囲 | 種別 | 実態（値/数式/VBA代入） | 業務的意味 |
| --- | --- | --- | --- | --- | --- | --- |
|  | 1 | `$F$2` | — | 設定値 | テスト仕様書タイトル | テスト文書識別 |
|  | 2 | `$A$15`〜`$A$52` | — | 設定値 | テスト項目番号 1〜5 | テスト番号 |

### 2.3 改訂履歴

**目的**: 様式 G3-08 の改訂履歴を管理する。版数・日付・改訂内容・登録者を記録。

#### 非表示行・列

なし。

#### レイアウト構造

```
行 1: タイトル「様式 G3-08の改訂履歴」
行 3: 列見出し（様式名 / 様式番号）
行 4: 様式情報（３号機線量不足報告書 / G03-08）
行 5: 列見出し（版数 / 制定・改定日 / 改訂箇所・理由 / 登録者）
行 6〜112: 改訂履歴データ（版 0〜16、1998年〜2025年）
```

| ✓ | No | セル | 名前付き範囲 | 種別 | 実態（値/数式/VBA代入） | 業務的意味 |
| --- | --- | --- | --- | --- | --- | --- |
| ✓ | 1 | `$A$6:$D$112` | `Rireki` | 設定値 | 改訂履歴データ領域 | 版番号の MAX 値で様式番号に反映 |
| ✓ | 2 | `$A$4` | `YousikiName` | 設定値 | "３号機線量不足報告書" | 様式名称 |
|  | 3 | `$D$4` | `YousikiNo` | 設定値 | " G03-08" | 様式番号 |

---

## 3. 名前付き範囲一覧


> ✓ = VBA から `Range()` で代入または参照され、業務ロジックに直結する名前付き範囲
> 全件を列挙し、✓ 基準に該当するものにマークを付ける。

| ✓ | No | 名前 | 参照先 | 業務的意味 |
| --- | --- | --- | --- | --- |
| ✓ | 1 | `SenkNo` | 報告書!$B$7 | 照射管理番号（全操作の起点キー） |
| ✓ | 2 | `SokDate` | 報告書!$G$7 | 測定日（DB取得値を表示） |
| ✓ | 3 | `SoktCD` | 報告書!$K$7 | 測定者コード→名前 |
| ✓ | 4 | `SaiSoku` | 報告書!$O$7 | 再測定者（ユーザー入力・印刷前チェック） |
| ✓ | 5 | `SokutSN` | 報告書!$S$7 | 測定線量（追加線量計算の基準値） |
| ✓ | 6 | `Tani` | 報告書!$W$7 | 製品目的名称 |
| ✓ | 7 | `Pass` | 報告書!$AA$7 | 指定パス数 |
| ✓ | 8 | `SikiKodo` | 報告書!$AD$7 | 計算式コード |
| ✓ | 9 | `Sosi` | 報告書!$AG$7 | 素子異常有無（印刷前チェック） |
| ✓ | 10 | `Atusa` | 報告書!$AJ$7 | 素子厚さ（DB 取得） |
| ✓ | 11 | `Debug` | 報告書!$A$1 | デバッグモードフラグ |
| ✓ | 12 | `Data1` | 報告書!$G$7:$AN$7 | 測定データ1行格納域（クリア対象） |
| ✓ | 13 | `Data2` | 報告書!$B$9:$AN$13 | 照射計画データ5行格納域（クリア対象） |
| ✓ | 14 | `SEnKind` | 報告書!$G$16 | 線量計種類（ドロップダウン入力→計算式取得） |
| ✓ | 15 | `AtusaT` | 報告書!$O$16 | 厚さ入力欄（印刷前チェック） |
| ✓ | 16 | `Keisask` | 報告書!$W$16 | 有効計算式コード（VBA で自動設定） |
| ✓ | 17 | `Tuika` | 報告書!$E$3 | 追加照射回数（N回目） |
| ✓ | 18 | `TuikaMin` | 報告書!$BC$21 | 線量計高低切替閾値（5 kGy） |
| ✓ | 19 | `SebkNoTB` | 報告書!$BG$3 | 線量不足データ件数 |
| ✓ | 20 | `SyainTB` | 報告書!$BG$5:$BG$36 | 有資格社員名リスト（DB取得） |
| ✓ | 21 | `SenSyu` | 報告書!$BC$10:$BC$12 | 線量計種類マスタ（DB取得） |
| ✓ | 22 | `MokuCD` | 報告書!$BC$3 | 製品目的コード参照開始セル |
|  | 23 | `mokuteki` | 報告書!$BC$3:$BD$6 | 製品目的コード⇔名称変換テーブル |
|  | 24 | `MokuSeu` | 報告書!#REF! | （無効参照・未使用） |
| ✓ | 25 | `UkeNo1` | 報告書!$B$9 | 受付番号（1件目） |
| ✓ | 26 | `UkeNo2` | 報告書!$B$10 | 受付番号（2件目） |
| ✓ | 27 | `UkeNo3` | 報告書!$B$11 | 受付番号（3件目） |
| ✓ | 28 | `UkeNo4` | 報告書!$B$12 | 受付番号（4件目） |
| ✓ | 29 | `UkeNo5` | 報告書!$B$13 | 受付番号（5件目） |
| ✓ | 30 | `RicNoS1` | 報告書!$G$9 | 開始RIC-NO（1件目） |
| ✓ | 31 | `RicNoS2` | 報告書!$G$10 | 開始RIC-NO（2件目） |
| ✓ | 32 | `RicNoS3` | 報告書!$G$11 | 開始RIC-NO（3件目） |
| ✓ | 33 | `RicNoS4` | 報告書!$G$12 | 開始RIC-NO（4件目） |
| ✓ | 34 | `RicNoS5` | 報告書!$G$13 | 開始RIC-NO（5件目） |
| ✓ | 35 | `RicNoE1` | 報告書!$K$9 | 終了RIC-NO（1件目） |
| ✓ | 36 | `RicNoE2` | 報告書!$K$10 | 終了RIC-NO（2件目） |
| ✓ | 37 | `RicNoE3` | 報告書!$K$11 | 終了RIC-NO（3件目） |
| ✓ | 38 | `RicNoE4` | 報告書!$K$12 | 終了RIC-NO（4件目） |
| ✓ | 39 | `RicNoE5` | 報告書!$K$13 | 終了RIC-NO（5件目） |
| ✓ | 40 | `SiteiSN1` | 報告書!$O$9 | 指定線量（1件目） |
| ✓ | 41 | `SiteiSN2` | 報告書!$O$10 | 指定線量（2件目） |
| ✓ | 42 | `SiteiSN3` | 報告書!$O$11 | 指定線量（3件目） |
| ✓ | 43 | `SiteiSN4` | 報告書!$O$12 | 指定線量（4件目） |
| ✓ | 44 | `SiteiSN5` | 報告書!$O$13 | 指定線量（5件目） |
| ✓ | 45 | `Kagen1` | 報告書!$S$9 | 下限線量（1件目） |
| ✓ | 46 | `Kagen2` | 報告書!$S$10 | 下限線量（2件目） |
| ✓ | 47 | `Kagen3` | 報告書!$S$11 | 下限線量（3件目） |
| ✓ | 48 | `Kagen4` | 報告書!$S$12 | 下限線量（4件目） |
| ✓ | 49 | `Kagen5` | 報告書!$S$13 | 下限線量（5件目） |
| ✓ | 50 | `Jyoug1` | 報告書!$W$9 | 上限線量（1件目） |
| ✓ | 51 | `Jyoug2` | 報告書!$W$10 | 上限線量（2件目） |
| ✓ | 52 | `Jyoug3` | 報告書!$W$11 | 上限線量（3件目） |
| ✓ | 53 | `Jyoug4` | 報告書!$W$12 | 上限線量（4件目） |
| ✓ | 54 | `Jyoug5` | 報告書!$W$13 | 上限線量（5件目） |
| ✓ | 55 | `KaiCD1` | 報告書!$AD$9 | 会社コード（1件目） |
| ✓ | 56 | `KaiCD2` | 報告書!$AD$10 | 会社コード（2件目） |
| ✓ | 57 | `KaiCD3` | 報告書!$AD$11 | 会社コード（3件目） |
| ✓ | 58 | `KaiCD4` | 報告書!$AD$12 | 会社コード（4件目） |
| ✓ | 59 | `KaiCD5` | 報告書!$AD$13 | 会社コード（5件目） |
| ✓ | 60 | `KaiName1` | 報告書!$AG$9 | 会社名（1件目） |
| ✓ | 61 | `KaiName2` | 報告書!$AG$10 | 会社名（2件目） |
| ✓ | 62 | `KaiName3` | 報告書!$AG$11 | 会社名（3件目） |
| ✓ | 63 | `KaiName4` | 報告書!$AG$12 | 会社名（4件目） |
| ✓ | 64 | `KaiName5` | 報告書!$AG$13 | 会社名（5件目） |
| ✓ | 65 | `JituP1` | 報告書!$AA$9 | 実パス数（1件目） |
| ✓ | 66 | `JituP2` | 報告書!$AA$10 | 実パス数（2件目） |
| ✓ | 67 | `JituP3` | 報告書!$AA$11 | 実パス数（3件目） |
| ✓ | 68 | `JituP4` | 報告書!$AA$12 | 実パス数（4件目） |
| ✓ | 69 | `JituP5` | 報告書!$AA$13 | 実パス数（5件目） |
|  | 70 | `Rireki` | 改訂履歴!$A$6:$D$112 | 改訂履歴データ領域 |
|  | 71 | `YousikiName` | 改訂履歴!$A$4 | 様式名称 |
|  | 72 | `YousikiNo` | 改訂履歴!$D$4 | 様式番号 |

---


### 3.1 データの入力規則

なし。

## 4. 数式一覧

| シート | 数式件数 | 備考 |
| --- | --- | --- |
| 報告書 | 56 | 追加照射計算・VLOOKUP 中間データ取得 |
| 試験成績書用紙 | 0 | — |
| 改訂履歴 | 0 | — |

### 4.1 報告書

| セル | 数式 | 説明 |
| --- | --- | --- |
| `$B$1` | `="様式 G3-08("&MAX(改訂履歴!A6:A112)&")"` | 最新版数を様式番号に自動反映 |
| `$M$3` | `=IF(SebkNoTB>BH3,"追加照射データが多過ぎるため管理者に連絡","")` | データ件数上限（30件）超過警告 |
| `$G$15` | `=RIGHT(SenkNo,4)` | 照射管理番号から線量計番号（下4桁）を抽出 |
| `$B$18` | `=IF(UkeNo1="","",UkeNo1)` | 追加照射明細の受付番号表示（1件目） |
| `$G$18` | `=RicNoS1` | 開始RIC-NO 参照（1件目） |
| `$K$18` | `=RicNoE1` | 終了RIC-NO 参照（1件目） |
| `$O$18` | `=IF(B18="","",Kagen1-SokutSN)` | 追加必要線量＝下限線量−測定線量（1件目） |
| `$S$18` | `=O18` | 追加線量（下限基準）の複写（1件目） |
| `$W$18` | `=IF(B18="","",W9-$S$7)` | 上限基準の追加可能線量（1件目） |
| `$AA$18` | `=IF(B18="","",ROUNDUP((O18/($S$7/AA9)),0))` | 追加パス数＝追加必要線量÷(測定線量/実パス)切上（1件目） |
| `$AD$18` | `=KaiName1` | 会社名参照（1件目） |
| `$B$19`〜`$AD$22` | （同構造で2〜5件目） | UkeNo2〜5, RicNoS2〜5 等を参照 |
| `$M$24` | `=IF(OR(AA18<0,AA19<0,AA20<0,AA21<0,AA22<0),"測定データが不適切","")` | 追加パス数が負になる異常検知 |
| `$S$24` | `=IF(MAX(AA18:AC22)<>MIN(AA18:AC22),"追加ﾊﾟｽ数異なるため課長に報告のこと","")` | 混載品の追加パス数不一致警告 |
| `$A$36`〜`$AX$36` | `=IF(ISERROR(VLOOKUP(SenkNo,Data,N,FALSE)),"",VLOOKUP(SenkNo,Data,N,FALSE))` | 中間作業行：DB データを `Data` 範囲から VLOOKUP で取得（列番号 N は行34の値） |

---

## 5. ボタン・マクロ対応

> ✓ = DB 更新・画面遷移・計算実行など副作用のある操作を起動するボタン

### 5.1 シート上のボタン（Form Control / 描画図形）

Form Control ボタンは存在しない。ただし以下の描画図形にマクロが割り当てられている。

| ✓ | No | シート | 図形テキスト | マクロ | 動作 |
| --- | --- | --- | --- | --- | --- |
| ✓ | 1 | 報告書 | （図形・テキストなし） | `CTRCls()` | 線量計番号クリア → `クリア()` でデータ全消去 |
| ✓ | 2 | 改訂履歴 | "BOOK 内容変更して改訂履歴に入力後ここをクリックしてExメニューを再起動" | `ブック保存()` | `ExSeihinJ` から保存先取得 → サーバーに SaveAs |

また、操作は以下のイベントでも起動される：
- セル `$B$7`（`SenkNo`）への入力規則ドロップダウン選択 → `Worksheet_Change` イベント
- セル `$G$16`（`SEnKind`）への入力 → `Worksheet_Change` イベント
- 印刷操作 → `Workbook_BeforePrint` イベント

### 5.1 b ショートカットキー

| No | マクロ名 | ショートカット | 処理概要 |
| --- | --- | --- | --- |
| 1 | `クリア()` / `CTRCls()` | **Ctrl+E** | 画面クリア（データ範囲消去 → シート保護再設定） |

### 5.2 ユーザーフォーム上のボタン（サマリ）

なし。ユーザーフォームは存在しない。### 5.3 CommandBar に動的追加されるボタン（アドイン風）

なし。CommandBar ボタンは存在しない。

---

## 6. VBA モジュール仕様

### 6.0 全プロシージャ一覧


> ✓ = ユーザー操作の起点（Click イベント等） / DB I/O を実行 / 他モジュールから呼び出される Public
> 全プロシージャを列挙し、✓ 基準に該当するものにマークを付ける。

| ✓ | No | モジュール | プロシージャ | スコープ | 種別 | 概要 |
| --- | --- | --- | --- | --- | --- | --- |
| ✓ | 1 | **ThisWorkbook.cls** | `Workbook_Open()` | Private | Event | ブック起動時に `画面初期設定()` を呼出し |
| ✓ | 2 | **ThisWorkbook.cls** | `Workbook_BeforeClose()` | Private | Event | デバッグモード判定し終了制御 |
| ✓ | 3 | **ThisWorkbook.cls** | `Workbook_BeforePrint()` | Private | Event | 未入力チェック・線量計種類チェック・印刷範囲設定 |
|  | 4 | **ThisWorkbook.cls** | `未入力チェック()` | Public | Private Function | 再測定者・素子異常・線量計種類・厚さの入力有無判定 |
|  | 5 | **ThisWorkbook.cls** | `線量計種類チェック()` | Public | Private Sub | 線量計の高/低線量用が適切か確認し警告 |
| ✓ | 6 | **Sheet1**（「報告書」） | `Worksheet_Change()` | Private | Event | 「報告書」セル変更検知（B7:照射管理番号→データ抽出、G16:線量計種類→計算式取得） |
| ✓ | 7 | **初期化.bas** | `画面初期設定()` | Public | Sub | クリア→DB初期データ取得（線量不足線量計・社員・種類）→シート保護 |
| ✓ | 8 | **初期化.bas** | `クリア()` | Public | Sub | 全データ領域のクリアと入力規則削除 |
| ✓ | 9 | **初期化.bas** | `CTRCls()` | Public | Sub | `SenkNo` クリア後に `クリア()` 呼出し |
| ✓ | 10 | **データ抽出SQL.bas** | `線量不足線量計()` | Public | Sub | DB から線量不足の照射管理番号リストを取得し入力規則に設定 |
| ✓ | 11 | **データ抽出SQL.bas** | `社員データ()` | Public | Sub | DB から有資格社員データを取得しシートに格納 |
| ✓ | 12 | **データ抽出SQL.bas** | `線量計種類()` | Public | Sub | DB から線量計種類マスタを取得しシートに格納 |
| ✓ | 13 | **データ抽出SQL.bas** | `計算式コード取得()` | Public | Sub | 選択された線量計種類に対応する計算式コードを DB から取得 |
| ✓ | 14 | **データ抽出SQL.bas** | `線量不足データ抽出()` | Public | Sub | 照射実績(SYOUJ2)と計画(SYOUKJ3)から詳細データを取得し表示 |
| ✓ | 15 | **データ抽出SQL.bas** | `製品目的()` | Public | Function | 製品目的コードから名称を変換（`mokuteki` テーブル参照） |
| ✓ | 16 | **SQL_Execution.bas** | `Open_oraconDB()` | Public | Sub | ADO+ODBC で DB 接続を開く |
| ✓ | 17 | **SQL_Execution.bas** | `SQL_Exe()` | Public | Sub | SQL 文を Execute し結果セット取得 |
| ✓ | 18 | **SQL_Execution.bas** | `SQL_INSERT_UPDATE()` | Public | Sub | キー有無判定で INSERT/UPDATE を切替実行 |
| ✓ | 19 | **SQL_Execution.bas** | `SQL_Delete()` | Public | Sub | 指定テーブルの DELETE 実行 |
| ✓ | 20 | **SQL_Execution.bas** | `Disp_Sheet()` | Public | Sub | SQL 結果をシートに直接書込み |
| ✓ | 21 | **SQL_Execution.bas** | `Set_Array()` | Public | Sub | SQL 結果を配列に格納 |
|  | 22 | **印刷.bas** | `印刷範囲の設定()` | Public | Sub | 報告書シートの印刷範囲を B1:AN30 に設定 |
|  | 23 | **Debug用.bas** | `DebugCom()` | Public | Sub | イベント有効化（デバッグ時復旧用） |
| ✓ | 24 | **改定履歴.bas** | `ブック保存()` | Public | Sub | DB から保存先フォルダを取得し `SaveAs` で保存 |

---

### 6.1 ThisWorkbook.cls

ブックの起動・終了イベントを受け、後続処理へ委譲する。

| No | プロシージャ | 処理フロー | 主要SQL |
| --- | --- | --- | --- |
| 1 | `Workbook_Open()` | ブック起動時に `画面初期設定()` を呼出し | なし |
| 2 | `Workbook_BeforeClose()` | デバッグモード判定し終了制御 | なし |
| 3 | `Workbook_BeforePrint()` | 未入力チェック・線量計種類チェック・印刷範囲設定 | なし |
| 4 | `未入力チェック()` | 再測定者・素子異常・線量計種類・厚さの入力有無判定 | なし |
| 5 | `線量計種類チェック()` | 線量計の高/低線量用が適切か確認し警告 | なし |

## 7. ユーザーフォーム仕様

ユーザーフォームは存在しない。

---

## 8. DB 接続・外部連携

### 8.1 ODBC 接続設定

| DSN 名 | UID | PWD | 用途 |
| --- | --- | --- | --- |
| `ricdb` | `ric` | `t6101` | RIC 照射管理システム DB（本番） |

> **備考**: コメントアウトされた代替接続: `UID=rich` / `PWD=t6101`（履歴データ用）

### 8.2 テーブル一覧（参照/更新区分付き）

> ✓ = INSERT / UPDATE / DELETE の対象テーブル（参照のみのテーブルは ✓ なし）

| ✓ | No | テーブル名 | 区分 | 主な用途 | キー列 | 参照/更新列 |
| --- | --- | --- | --- | --- | --- | --- |
|  | 1 | `RIC.SYOUJ2` | 参照 | 照射実績データ取得 | `UNO`, `SENKNO`, `TUIKAFLG` | `SESDATE`, `SOKUTCD`, `SOKUTSN`, `PASS`, `KEISASK`, `ATUSA`, `JITUNO`, `SENHFLG` |
|  | 2 | `RIC.SYOUKJ3` | 参照 | 照射計画データ取得（5製品分） | `SYKNO`, `SYOUSH_F` | `UNO1`〜`5`, `FRICNO1`〜`5`, `LRICNO1`〜`5`, `SITEISN1`〜`5`, `KAGENSN1`〜`5`, `JYOUGSN1`〜`5`, `KAISYACD1`〜`5`, `KAINAME1`〜`5` |
|  | 3 | `SHAINMST` | 参照 | 線量測定有資格社員マスタ | `SHANO` | `SHANAME`, `HSHIKA`, `LSHIKA` |
|  | 4 | `SENKIND` | 参照 | 線量計種類マスタ | `SENSYU` | `KKODE`, `DISPNO` |
|  | 5 | `KEICODE` | 参照 | 計算式コードマスタ | `KEISASK` | `YFLG1` |
|  | 6 | `RIC.ZAIKO` | 参照 | 在庫データ（照射単位情報） | `UNO` | `TANI` |
|  | 7 | `ExSeihinJ` | 参照 | ブック保存先フォルダ取得 | `FILENAME` | `FOLDER` |

> 本ファイルでは DB への INSERT / UPDATE / DELETE は実行していない（参照のみ）。
> ただし `SQL_Execution.bas` には汎用の `SQL_INSERT_UPDATE()` / `SQL_Delete()` プロシージャが定義されており、他ブックから呼び出される可能性がある。
>
> **「キー列」の定義**: JOIN 条件または UPDATE/DELETE の WHERE 句で使用される列を示す。

### 8.3 SQL 一覧

#### 8.3.1 線量不足の照射管理番号リスト取得（`線量不足線量計()` / **データ抽出SQL.bas**）

```sql
SELECT DISTINCT sk.SYKNO, sk.uno1, sk.uno2, sk.uno3, sk.uno4, sk.uno5
FROM RIC.SYOUJ2 s, RIC.SYOUKJ3 sk
WHERE s.UNO = sk.UNO1 AND s.FRICNO = sk.FRICNO1
  AND sk.SYOUSH_F = '2'
  AND (s.SENHFLG = '2' OR s.SENHFLG = '4')
  AND s.syouflg = '1' AND s.seshflg = '1'
ORDER BY sk.sykno
```

#### 8.3.2 照射実績データ取得（`線量不足データ抽出()` / **データ抽出SQL.bas**）

```sql
SELECT S.SESDATE, sha.shaname,
       TO_NUMBER(S.SOKUTSN), TO_NUMBER(za.tani), TO_NUMBER(S.PASS),
       S.KEISASK, TO_NUMBER(S.ATUSA), TO_NUMBER(S.JITUNO),
       TO_NUMBER(TUIKAFLG)
FROM SHAINMST SHA, RIC.SYOUJ2 S, RIC.ZAIKO ZA
WHERE s.sokutcd = sha.shano
  AND za.uno = s.uno
  AND (s.senhflg = '2' OR s.senhflg = '4')
  AND s.senkno = '<線量計番号下4桁>'
ORDER BY s.tuikaflg DESC
```

#### 8.3.3 照射計画データ取得（`線量不足データ抽出()` / **データ抽出SQL.bas**）

```sql
SELECT TO_NUMBER(UNO1), FRICNO1, LRICNO1, SITEISN1, KAGENSN1, JYOUGSN1, kaisyacd1, KAINAME1,
       TO_NUMBER(UNO2), FRICNO2, LRICNO2, SITEISN2, KAGENSN2, JYOUGSN2, kaisyacd2, KAINAME2,
       TO_NUMBER(UNO3), FRICNO3, LRICNO3, SITEISN3, KAGENSN3, JYOUGSN3, kaisyacd3, KAINAME3,
       TO_NUMBER(UNO4), FRICNO4, LRICNO4, SITEISN4, KAGENSN4, JYOUGSN4, kaisyacd4, KAINAME4,
       TO_NUMBER(UNO5), FRICNO5, LRICNO5, SITEISN5, KAGENSN5, JYOUGSN5, kaisyacd5, KAINAME5
FROM RIC.SYOUKJ3
WHERE SYKNO = '<照射管理番号>' AND SYOUSH_F = '2'
```

#### 8.3.4 有資格社員データ取得（`社員データ()` / **データ抽出SQL.bas**）

```sql
SELECT TRIM(SHANAME)
FROM SHAINMST
WHERE HSHIKA = '1' AND LSHIKA = '1' AND shano > '1000'
ORDER BY SHANO
```

#### 8.3.5 線量計種類マスタ取得（`線量計種類()` / **データ抽出SQL.bas**）

```sql
SELECT sensyu FROM senkind ORDER BY dispno
```

#### 8.3.6 計算式コード取得（`計算式コード取得()` / **データ抽出SQL.bas**）

```sql
SELECT KKODE FROM SENKIND WHERE sensyu = '<選択線量計種類>'
SELECT KEISASK FROM KEICODE WHERE YFLG1 = '1'
```

#### 8.3.7 ブック保存先フォルダ取得（`ブック保存()` / **改定履歴.bas**）

```sql
SELECT folder FROM ExSeihinJ WHERE filename = '<ThisWorkbook.Name>'
```

### 8.4 外部ファイル連携

| ファイル | パス | ファイル名 | 処理 | 備考 |
| --- | --- | --- | --- | --- |
| ブック保存先 | `ExSeihinJ.folder` | `<ThisWorkbook.Name>` | `ActiveWorkbook.SaveAs` | サーバー上の管理フォルダにブックを上書き保存 |

#### ブック保存先詳細

- **出力元プロシージャ**: `ブック保存()` / **改定履歴.bas**
- **パス生成ロジック**（VBAソースより）:
  ```
  mySql = "SELECT folder FROM ExSeihinJ WHERE filename='" & ThisWorkbook.Name & "'"
  ActiveWorkbook.SaveAs Filename:=myData(0, 0) & ThisWorkbook.Name
  ```
- **出力例**: `<DBから取得したフォルダパス>\ExRIC3線量不足報告書.xlsm`
- **フォルダ不在時**: エラーハンドリングなし（`On Error Resume Next` で例外を吸収し「保存しませんでした。」を表示）

---

## 9. データフロー

各フローは「起点 → 処理 → 結果」の粒度で記述する。

### 9.1 ブック起動フロー

| No | 起点 | 処理 | 結果 |
| --- | --- | --- | --- |
| 1 | 📄 `Workbook_Open` イベント | 📄 `画面初期設定()` / **初期化.bas** 呼出し | 初期化開始 |
| 2 | 上記 | 📊 `クリア()` / **初期化.bas** | 全データ領域・入力規則をクリア |
| 3 | 上記 | 🗄️ `線量不足線量計()` / **データ抽出SQL.bas** | DB から線量不足管理番号を取得 |
| 4 | 上記 | 📊 `SenkNo` に入力規則リスト設定 | ドロップダウン選択肢を生成 |
| 5 | 上記 | 🗄️ `社員データ()` / **データ抽出SQL.bas** | DB から有資格社員リスト取得 |
| 6 | 上記 | 📊 `SyainTB` 領域にシート書込み | 社員名を非表示列に格納 |
| 7 | 上記 | 🗄️ `線量計種類()` / **データ抽出SQL.bas** | DB から線量計種類マスタ取得 |
| 8 | 上記 | 📊 `SenSyu` 領域にシート書込み | 種類名を非表示列に格納 |
| 9 | 上記 | 📊 シート保護設定（UIOnly） | ユーザー操作可能な保護状態に |

#### ツリー図（補助）

```
（ブック起動）
└─ 📄 Workbook_Open                    [ThisWorkbook.cls]
   └─ 📄 画面初期設定()                 [初期化.bas]
      ├─ 📊 クリア()                    [初期化.bas]（全領域初期化）
      ├─ 🗄️ 線量不足線量計()            [データ抽出SQL.bas]
      │   ├─ 🗄️ Set_Array()            [SQL_Execution.bas]（管理番号取得）
      │   ├─ 🗄️ Set_Array()            [SQL_Execution.bas]（追加照射有効判定）
      │   └─ 📊 SenkNo 入力規則設定
      ├─ 🗄️ 社員データ()               [データ抽出SQL.bas]
      │   └─ 🗄️ Disp_Sheet()           [SQL_Execution.bas]（シート書込み）
      ├─ 🗄️ 線量計種類()               [データ抽出SQL.bas]
      │   └─ 🗄️ Disp_Sheet()           [SQL_Execution.bas]（シート書込み）
      └─ 📊 ActiveSheet.Protect         （UI保護設定）
```

### 9.2 照射管理番号選択フロー

| No | 起点 | 処理 | 結果 |
| --- | --- | --- | --- |
| 1 | 📊 ユーザーが `$B$7`（`SenkNo`）でドロップダウン選択 | 📄 `Worksheet_Change()` / **Sheet1.cls** 検知 | セル変更イベント発火 |
| 2 | 上記 | 🗄️ `線量不足データ抽出()` / **データ抽出SQL.bas** | DB から照射実績取得 |
| 3 | 上記 | 📊 `Data1` 領域に書込み | 測定日・測定者・線量等を表示 |
| 4 | 上記 | 🗄️ 照射計画(SYOUKJ3)取得 | 5製品分の計画データ |
| 5 | 上記 | 📊 `Data2` 領域に書込み | 受付番号・RIC-NO・線量・会社情報 |
| 6 | 上記 | 📊 数式が自動再計算 | 追加線量・追加パス数が算出される |
| 7 | [条件分岐] | 📊 `$M$24`: 追加パス数が負 → "測定データが不適切" 表示 | 異常データ警告 |
| 8 | [条件分岐] | 📊 `$S$24`: パス数不一致 → "追加パス数異なるため課長に報告" 表示 | 混載品警告 |

#### ツリー図（補助）

```
（照射管理番号選択）
└─ 📊 SenkNo セル変更
   └─ 📄 Worksheet_Change()             [Sheet1.cls]
      └─ 🗄️ 線量不足データ抽出()        [データ抽出SQL.bas]
         ├─ 🗄️ Set_Array()              [SQL_Execution.bas]（SYOUJ2 照射実績取得）
         ├─ 📊 Data1 書込み             （測定日・測定者・線量等）
         ├─ 📊 Tuika 書込み             （追加回数）
         ├─ 🗄️ Set_Array()              [SQL_Execution.bas]（SYOUKJ3 照射計画取得）
         ├─ 📊 Data2 書込み             （5件分の製品情報）
         └─ 📊 数式自動再計算            （O18〜AA22: 追加線量・パス数）
            ├─ [追加パス < 0] 📊 M24: 警告表示
            └─ [パス数不一致] 📊 S24: 課長報告指示
```

### 9.3 線量計種類選択フロー

| No | 起点 | 処理 | 結果 |
| --- | --- | --- | --- |
| 1 | 📊 ユーザーが `$G$16`（`SEnKind`）を入力 | 📄 `Worksheet_Change()` / **Sheet1.cls** 検知 | セル変更イベント発火 |
| 2 | 上記 | 🗄️ `計算式コード取得()` / **データ抽出SQL.bas** | SENKIND から KKODE 取得 |
| 3 | 上記 | 🗄️ KEICODE テーブルから有効計算式検索 | RIC3用の計算式コード特定 |
| 4 | 上記 | 📊 `Keisask` に計算式コード書込み | `$W$16` に設定 |

#### ツリー図（補助）

```
（線量計種類選択）
└─ 📊 SEnKind セル変更
   └─ 📄 Worksheet_Change()             [Sheet1.cls]
      └─ 🗄️ 計算式コード取得()          [データ抽出SQL.bas]
         ├─ 🗄️ Set_Array()              [SQL_Execution.bas]（SENKIND → KKODE）
         ├─ 🗄️ Set_Array()              [SQL_Execution.bas]（KEICODE → 有効コード）
         ├─ 📄 Mid(code,2,1)="3" チェック（RIC3号機判定）
         └─ 📊 Keisask 書込み
```

### 9.4 印刷フロー

| No | 起点 | 処理 | 結果 |
| --- | --- | --- | --- |
| 1 | 📊 ユーザーが印刷操作 | 📄 `Workbook_BeforePrint()` / **ThisWorkbook.cls** | イベント発火 |
| 2 | 上記 | 📄 `未入力チェック()` | 再測定者・素子異常・線量計種類・厚さの入力確認 |
| 3 | [未入力あり] | 📄 MsgBox で警告、印刷続行 Yes/No | ユーザー確認 |
| 4 | [全入力済] | 📄 `線量計種類チェック()` | 高/低線量用の適合性チェック |
| 5 | [不適合] | 📄 MsgBox で確認 | キャンセル可能 |
| 6 | 上記 | 📊 `印刷範囲の設定()` / **印刷.bas** | B1:AN30, 1ページに収まるよう設定 |

#### ツリー図（補助）

```
（印刷操作）
└─ 📄 Workbook_BeforePrint()            [ThisWorkbook.cls]
   ├─ 📄 未入力チェック()               [ThisWorkbook.cls]
   │   ├─ [未入力あり] 📄 MsgBox → Cancel 可能
   │   └─ [全入力済]
   │       └─ 📄 線量計種類チェック()    [ThisWorkbook.cls]
   │           ├─ [低線量用で高線量値] 📄 MsgBox 確認
   │           └─ [高線量用で低線量値] 📄 MsgBox 確認
   └─ 📊 印刷範囲の設定()              [印刷.bas]（B1:AN30）
```

### 9.5 ブック保存フロー

| No | 起点 | 処理 | 結果 |
| --- | --- | --- | --- |
| 1 | 📄 ユーザーが `ブック保存()` を実行 | 🗄️ `ExSeihinJ` から保存先取得 | フォルダパス決定 |
| 2 | 上記 | 📄 MsgBox で確認 | Yes/No |
| 3 | 上記 | 📊 `CTRCls()` で全クリア | データ保護 |
| 4 | 上記 | 📄 `SaveAs` でサーバーに保存 | ファイル永続化 |

#### ツリー図（補助）

```
（ブック保存）
└─ 📄 ブック保存()                      [改定履歴.bas]
   ├─ 🗄️ Set_Array()                   [SQL_Execution.bas]（ExSeihinJ → folder）
   ├─ 📄 MsgBox 確認
   ├─ 📊 CTRCls()                      [初期化.bas]（データクリア）
   └─ 📄 ActiveWorkbook.SaveAs          （サーバーパスに保存）
```

---

## 10. セキュリティ注意事項


| No | カテゴリ | 内容 | リスク |
| --- | --- | --- | --- |
| 1 | 認証情報ハードコード | DSN=`ricdb`, UID=`ric`, PWD=`t6101` が VBA ソースに平文記載 | 中：VBAエディタで閲覧可能 |
| 2 | ファイル操作 | ブックの上書き保存を実行 | 中：保存先の上書き |

---

## スコープ外（本仕様書に含まないもの）

- セル書式（色・罫線・フォント）
- 条件付き書式、グラフ・画像、印刷設定
- マージセル詳細（レイアウト確認は Excel 画面参照）

必要な場合は Excel 画面のスクリーンショットで補完してください。
