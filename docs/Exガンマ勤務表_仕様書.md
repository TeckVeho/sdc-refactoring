# Exガンマ勤務表 仕様書

> **対象ファイル**: Exガンマ勤務表.xlsm
> **ファイル種別**: .xlsm（マクロ付き）
> **用途**: ガンマ線照射施設の勤務計画表。シフト記号によるガンマ勤務・トイレ掃除当番の計画作成、DB への記録・読込、社員マスタ管理を行う
> **VBA プロジェクト**: モジュール 16 本（.bas 7 / .cls 7 / .frm 3）※ユーザーフォーム含むが UserForm1 は空
> **外部連携**: Access DB（`\\163.59.144.156\ExRicSys\ガンマ勤務計画DB.accdb`）、DSN=ricdb（Oracle）
> **解析日**: 2026-06-29（excel-to-md スキルによる自動解析）

---

## 凡例（本仕様書の表記ルール）

本仕様書では、保守時の判別を容易にするため、以下の表記ルールを使用します。

### 用語規約

| 用語 | 意味 |
|---|---|
| EXメニュー | ExRicSys フォルダに配置される VBA ファイル群（Ex*.xlsm）の総称 |
| `ExRicSys` | 配置フォルダ名〈EXメニュー配置フォルダ〉（`\\163.59.144.156\ExRicSys\`） |

### 表記規則

| 種別 | 表記 | 例 |
|---|---|---|
| モジュール（.bas / .cls） | **太字** | **初期設定.bas** |
| ユーザーフォーム（.frm） | **太字** | **Form資格.frm** |
| プロシージャ / イベント | `コード体()` | `スタート()` ※Markdown 制約上、斜体ではなくコード体で統一 |
| シート名 | 「」 | 「勤務計画」 |
| セル参照 | `コード体` | `$G$7` |
| 名前付き範囲 | `コード体` | `S_Day` |
| DB テーブル / カラム | `コード体` | `勤務予定表` / `社員番号` |
| ユーザー操作 | （操作名） | （記録ボタン Click） |
| 主要マーク | ✓ | ✓ = 保守時に最初に確認すべき項目 |

### データフロー 場所マーク（9章）

| アイコン | 種別 | 意味 |
|---|---|---|
| 📊 | シート操作 | ワークシート上のセル書込み・読取り・表示変更 |
| 🖥️ | 画面操作 | ユーザーフォーム（.frm）の表示・入力・操作 |
| 🗄️ | DB操作 | DB への SELECT / INSERT / UPDATE / DELETE |
| 📄 | VBA内部処理 | 変数計算・条件分岐など、画面・シートに直接関与しない処理 |

### ✓（主要マーク）の判定基準

| 章 | 対象 | ✓ の判定基準 |
|---|---|---|
| 1.1 | シート | ユーザーが直接操作する、または VBA が動的に表示/非表示を切り替える |
| 1.2 | ユーザーフォーム | ユーザー入力を受け付ける、または業務フローの起点となる |
| 1.3 / 6.0 | VBA モジュール | ① ユーザー操作の起点 ② DB I/O を含む ③ 他モジュールから呼び出される ④ コード行数上位 25% のいずれか |
| 2 | セル / 名前付き範囲 | VBA から `Range()` で代入または参照され、業務ロジックに直結する |
| 3 | 名前付き範囲 | VBA から `Range()` で代入または参照され、業務ロジックに直結する |
| 5 | ボタン / コントロール | DB 更新・画面遷移・計算実行など副作用のある操作を起動する |
| 6.0 | プロシージャ | ① ユーザー操作の起点（Click イベント等） ② DB I/O を実行 ③ 他モジュールから呼び出される Public のいずれか |
| 7 | フォームコントロール | ユーザー入力を受け付ける、またはイベントで業務処理を起動する |
| 8.2 | DB テーブル | INSERT / UPDATE / DELETE の対象（参照のみのテーブルは ✓ なし） |

---

## 目次

1. [ファイル構成](#1-ファイル構成)
2. [シート詳細](#2-シート詳細)
3. [名前付き範囲一覧](#3-名前付き範囲一覧)
4. [数式一覧](#4-数式一覧)
5. [ボタン・マクロ対応](#5-ボタンマクロ対応)
6. [VBA モジュール仕様](#6-vba-モジュール仕様)
7. [ユーザーフォーム仕様](#7-ユーザーフォーム仕様)
8. [DB 接続・外部連携](#8-db-接続外部連携)
9. [データフロー](#9-データフロー)
10. [セキュリティ注意事項](#10-セキュリティ注意事項)

---

## 1. ファイル構成

### 1.1 シート一覧（全 7 件）

> ✓ = ユーザーが直接操作する、または VBA が動的に表示/非表示を切り替えるシート

| ✓ | シート名 | 最大行 | 最大列 | 保存時 Visible | VBA による動的切替 |
|---|---|---|---|---|---|
| ✓ | 勤務計画 | 101 | 260 (IZ) | visible | 行/列の動的表示非表示あり |
| ✓ | トイレ掃除 | 78 | 40 (AN) | visible | 行/列の動的表示非表示あり |
|   | 再入室表 | 41 | 41 (AO) | visible | — |
| ✓ | 社員データ | 46 | 42 (AP) | visible | 列 B:C, K:Q の非表示制御あり |
|   | トイレチェック表 | 38 | 22 (V) | visible | — |
| ✓ | 勤務計画印刷用 | 40 | 236 (IB) | visible | 印刷時に動的切替 |
| ✓ | 使用方法 | 56 | 3 (C) | visible | 設定値保持（DebugF / PasWard） |

### 1.2 ユーザーフォーム一覧（全 3 件）

> ✓ = ユーザー入力を受け付ける、または業務フローの起点となるフォーム

| ✓ | フォーム名 | コントロール数 | 主用途 |
|---|---|---|---|
| ✓ | **Form資格.frm** | 8 | 起動時の資格認証（社員番号＋パスワード入力） |
| ✓ | **Form社員登録.frm** | 11 | 社員の追加／移動登録 |
|   | **UserForm1.frm** | 1 | 注意事項表示（空マクロ） |

### 1.3 VBA モジュール一覧（全 16 件）

> ✓ = ユーザー操作の起点 / DB I/O を含む / 他モジュールから呼び出される / コード行数上位 25%
> 全モジュールを列挙し、✓ 基準に該当するものにマークを付ける。

| ✓ | モジュール | 種別 | プロシージャ数 | 主な役割 |
|---|---|---|---|---|
| ✓ | **ThisWorkbook.cls** | .cls | 4 | 起動/終了/印刷/保存イベント |
| ✓ | **Sheet1.cls** | .cls | 7 | 「勤務計画」シートイベント |
|   | **Sheet2.cls** | .cls | 0 | 「勤務計画印刷用」シート（コードなし） |
| ✓ | **Sheet3.cls** | .cls | 3 | 「トイレ掃除」シートイベント |
|   | **Sheet4.cls** | .cls | 0 | 「再入室表」シート（コードなし） |
|   | **Sheet5.cls** | .cls | 0 | 「トイレチェック表」シート（コードなし） |
| ✓ | **Sheet6.cls** | .cls | 3 | 「社員データ」シートイベント |
|   | **Sheet7.cls** | .cls | 0 | 「使用方法」シート（コードなし） |
| ✓ | **初期設定.bas** | .bas | 2 | 起動処理・画面初期化 |
| ✓ | **共通変数.bas** | .bas | 0（宣言のみ） | Public 変数・定数定義 |
| ✓ | **表作成_勤務Read.bas** | .bas | 12 | 表体裁・氏名読込・勤務データ読込 |
| ✓ | **勤務データ記録.bas** | .bas | 2 | 勤務計画の DB 記録 |
| ✓ | **社員_勤務データ.bas** | .bas | 9 | 社員テーブル・勤務時間・回覧先の読書 |
| ✓ | **SQL_AccessExe.bas** | .bas | 8 | Access DB 接続・SQL 実行エンジン |
| ✓ | **SQL_Execution.bas** | .bas | 6 | Oracle(ricdb) DB 接続・SQL 実行エンジン |
|   | **印刷設定.bas** | .bas | 1 | 印刷パラメータ設定ユーティリティ |
|   | **Debug用.bas** | .bas | 2 | デバッグ用画面表示・イベント復元 |
| ✓ | **コマンド表示_非表示.bas** | .bas | 1 | 資格に応じたボタン表示/非表示制御 |
|   | **Module1.bas** | .bas | 1 | 注意事項フォーム表示 |

---

## 2. シート詳細

### 2.0 シート可視性一覧

| シート | VBA による非表示化 | 表示するタイミング | 非表示にするタイミング | 制御プロシージャ |
|---|---|---|---|---|
| 勤務計画 | あり（行/列単位） | `表体裁()` で日付入力後 | `画面の初期設定()` で行 2〜5, 9, 40, 46〜61 非表示 / 列 B:F, AR:BM 非表示 | **表作成_勤務Read.bas** / **初期設定.bas** |
| トイレ掃除 | あり（行/列単位） | `表体裁()` で日付入力後 | `画面の初期設定()` で行 2, 4〜9, 72〜75 非表示 / 列 B:F 非表示 | **表作成_勤務Read.bas** / **初期設定.bas** |
| 社員データ | あり（列単位） | — | `画面の初期設定()` で列 B:C, K:Q 非表示 | **初期設定.bas** |
| 勤務計画印刷用 | — | 印刷時選択 | — | **ThisWorkbook.cls** |
| 再入室表 | — | — | — | — |
| トイレチェック表 | — | — | — | — |
| 使用方法 | — | — | — | — |

> 以下の各シートのレイアウト構造表における ✓ = VBA から `Range()` で代入または参照され、業務ロジックに直結するセル

### 2.1 勤務計画

**目的**: ガンマ線照射施設の月間勤務シフトを作成・表示する中核シート。社員ごとの日別勤務記号を管理し、DB に記録する。

#### 非表示行・列

| 種類 | 対象 | 備考 |
|---|---|---|
| 非表示行 | 2〜5（4行） | ヘッダ補助情報 |
| 非表示行 | 9（1行） | 休日暦（○マーク行） |
| 非表示行 | 40（1行） | 区切り |
| 非表示行 | 46〜61（16行） | 勤務記号マスタ・集計領域 |
| 非表示列 | B:F（5列） | 社員番号・表示順（内部管理列） |
| 非表示列 | AR:BM（22列） | 勤務時間集計・日数カウント |

#### レイアウト構造

```
行 6: 表示年月日・情報メッセージ
行 7: 期間始〜期間終・出勤日数・労働時間
行 8: 勤務記号元（コピー元エリア）— VBA で非表示制御
行 9: 休日暦（○印）
行 10: 日付行・集計ヘッダ（超勤時間/労働時間/氏名）
行 11: 列ヘッダ（社員番号/表示順/氏名＼曜日）+ 勤務時間記号ラベル
行 12〜39: データ本体（社員×日の勤務記号マトリクス）
行 40: 区切り（非表示）
行 41〜45: 夜勤・号機配置情報
行 46〜: 勤務記号定義テーブル（Ric1/Ric2/Ric3/記号/人数）
```

| ✓ | セル | 名前付き範囲 | 種別 | 実態（値/数式/VBA代入） | 業務的意味 |
|---|---|---|---|---|---|
| ✓ | `$G$7` | `S_Day` | 手動設定 | 「年月日入力」または日付値 | 勤務計画の期間開始日（11日固定） |
| ✓ | `$I$7` | `E_day` | 数式 | `=DateAdd("m",1,S_Day)-1`（VBA代入） | 勤務計画の期間終了日 |
| ✓ | `$S$6` | `Infometion` | VBA代入 | `記録Main()` で「記録中」表示 | 処理状態メッセージ |
| ✓ | `$AC$6:$AO$7` | `MsgCell` | VBA代入 | 資格に応じたガイドメッセージ | ユーザー向け操作案内 |
| ✓ | `$H$8:$AL$8` | `KinmuMoto` | 数式/VBA | `=社員データ!T7`〜`T33` の参照 | 勤務記号コピー元行 |
| ✓ | `$H$9:$AL$11` | `K_Yasumi` | VBA代入 | `表体裁_日付()` で休日暦をDB読込 | 休日カレンダー・列ヘッダ |
| ✓ | `$E$12:$G$39` | `K_Simei` | VBA代入 | `表体裁_氏名_作成中ガンマ()` でDB読込 | 社員番号・表示順・氏名列 |
| ✓ | `$H$12:$AL$39` | `Kinnmu1` | 手動設定/VBA代入 | コピー＆ペーストまたはDB読込 | 勤務記号本体（日×社員） |
| ✓ | `$E$12:$AL$39` | `TB_Kinmu` | VBA代入 | `表体裁_氏名_作成中ガンマ()` | 社員情報＋勤務記号の全体範囲 |
| ✓ | `$E$12:$AP$39` | `KinmuHyou` | VBA代入 | 背景色クリア対象 | 勤務表全体範囲 |
| ✓ | `$AN$8` | `SyaName` | VBA代入 | `Form資格.frm` で認証後に氏名設定 | ログインユーザー氏名表示 |
|   | `$N$7` | — | 数式 | `=IFERROR(E_day*1-S_Day*1+1-AM11,"")` | 出勤日数 |
|   | `$R$7` | — | 数式 | `=IFERROR(N7*8,"")` | 労働時間（出勤日数×8） |
|   | `$AM$11` | — | 数式 | `=IF(S_Day="","",COUNTIF(H9:AL9,"○"))` | 休日数カウント |
|   | `$AO$12`〜`$AO$39` | — | 数式 | `=BD12*11+BJ12*8` | 個人別労働時間計算 |

### 2.2 トイレ掃除

**目的**: トイレ掃除当番のシフトを管理するシート。勤務計画と同期間で社員別に割当を行う。

#### 非表示行・列

| 種類 | 対象 | 備考 |
|---|---|---|
| 非表示行 | 2（1行） | 補助情報 |
| 非表示行 | 4〜9（6行） | 日付計算・コピー元 |
| 非表示行 | 72〜75（4行） | 予備行 |
| 非表示列 | B:F（5列） | 社員番号・表示順（内部管理列） |

#### レイアウト構造

```
行 3: 日付表示（ST_Day / ToireMoto）
行 7: 式コピー元（Siki2）
行 10〜11: ヘッダ
行 12〜71: データ本体（2行ごと: 偶数行=社員名、奇数行=当番記号）
```

| ✓ | セル | 名前付き範囲 | 種別 | 実態 | 業務的意味 |
|---|---|---|---|---|---|
| ✓ | `$K$3` | `ST_Day` | VBA代入 | 期間開始日 | トイレ掃除の期間開始日 |
| ✓ | `$AD$3:$AH$3` | `ToireMoto` | 設定値 | コピー元記号 | トイレ掃除記号のコピー元 |
| ✓ | `$H$7:$AL$7` | `Siki2` | 設定値 | 数式テンプレート | 日付行の式コピー元 |
| ✓ | `$E$12:$G$71` | `T_Simei` | VBA代入 | `表体裁_氏名_作成中トイレ()` | 社員番号・表示順・氏名列 |
| ✓ | `$H$12:$AL$71` | `Toire1` | 手動設定/VBA代入 | コピー＆ペーストまたはDB読込 | トイレ掃除当番記号本体 |
| ✓ | `$E$12:$AL$71` | `Toire_TB` | VBA代入 | `表体裁()` で全体クリア/表示 | トイレ掃除テーブル全体範囲 |

### 2.3 再入室表

**目的**: ガンマ照射室への再入室記録を管理する紙帳票テンプレート。VBA による制御はなし。

#### 非表示行・列

なし。

#### レイアウト構造

```
行 3: タイトル・日付ヘッダ
行 4〜41: 再入室記録本体（氏名・時刻・確認欄）
```

VBA から直接操作されるセル/名前付き範囲はなし。印刷帳票テンプレートとして使用。

### 2.4 社員データ

**目的**: 社員マスタ情報・勤務時間テーブル・回覧先を管理するデータシート。DB の読書き対象。

#### 非表示行・列

| 種類 | 対象 | 備考 |
|---|---|---|
| 非表示列 | B:C（2列） | 行番号・変更有無判定用 |
| 非表示列 | K:Q（7列） | 社員テーブル比較用（SyainTB2） |

#### レイアウト構造

```
行 2〜3: シートタイトル
行 4〜5: 表示切替ドロップダウン（Hyouji）/ 期間始終
行 6: 列ヘッダ
行 7〜46: 社員テーブル（SyainTB1 / SyainTB2）
行 7〜12: 回覧先（Kairann）
列 T〜X: 勤務時間テーブル（KinmuTB / KinmuTime）
```

| ✓ | セル | 名前付き範囲 | 種別 | 実態 | 業務的意味 |
|---|---|---|---|---|---|
| ✓ | `$D$4` | `Hyouji` | 手動設定 | 「有効社員のみ」等 | 社員表示フィルタ条件 |
| ✓ | `$D$7:$J$46` | `SyainTB1` | DB読込/手動設定 | `社員テーブル呼出()` で読込 | 社員マスタ表示テーブル（編集対象） |
| ✓ | `$K$7:$Q$46` | `SyainTB2` | VBA代入 | 登録後にTB1コピー | 社員マスタ比較用テーブル |
| ✓ | `$E$7:$J$46` | `SyainSika` | 手動設定 | 社員番号入力でイベント発火 | 社員資格入力範囲 |
| ✓ | `$F$7:$F$46` | `Simei` | VBA代入/手動 | 社員名列 | 氏名表示列（保護） |
| ✓ | `$C$7:$C$46` | `HenUMU` | 数式 | 変更有無判定式 | 編集有無フラグ列 |
| ✓ | `$D$14:$D$46` | `Jyunnjyo` | VBA代入 | 表示順の自動再割付 | 表示順管理列 |
| ✓ | `$T$5` | `KikanS` | VBA代入 | `Range("S_Day")` 代入 | 勤務時間テーブル期間始 |
| ✓ | `$W$5` | `KikanE` | VBA代入 | 期間終日付 | 勤務時間テーブル期間終 |
| ✓ | `$T$7:$X$33` | `KinmuTime` | DB読込 | `勤務時間テーブル読込()` | 勤務記号→勤務時間対応表 |
| ✓ | `$T$31:$W$33` | `KinmuTB` | DB読込 | `勤務時間テーブル読込()` | 勤務時間テーブル（記号番号>24） |
| ✓ | `$AC$7:$AC$12` | `Kairann` | DB読込 | `回覧先読込()` | 回覧先1〜6 |
| ✓ | `$C$5` | `Siki1` | 設定値 | 変更有無判定式のテンプレート | 数式コピー元 |

### 2.5 トイレチェック表

**目的**: トイレの清掃チェック表。印刷して使用する紙帳票テンプレート。

#### 非表示行・列

なし。

#### レイアウト構造

```
行 2〜21: チェック表本体（日付・担当者・チェック項目）
```

VBA による直接操作はないが、印刷パラメータ設定の対象シート。

### 2.6 勤務計画印刷用

**目的**: 「勤務計画」シートの印刷用コピー。印刷時に切り替えて使用する。

#### 非表示行・列

なし。

#### レイアウト構造

「勤務計画」シートと同一構造。印刷時に `Workbook_BeforePrint` で動的に印刷範囲を設定。

### 2.7 使用方法

**目的**: システム設定値の保持シート（パスワード・デバッグフラグ・DB パス）。ユーザーには表示されない運用。

#### 非表示行・列

なし。

#### レイアウト構造

| ✓ | セル | 名前付き範囲 | 種別 | 実態 | 業務的意味 |
|---|---|---|---|---|---|
| ✓ | `$A$1` | `DebugF` | 手動設定 | 空欄/1/* | デバッグフラグ |
| ✓ | `$C$13` | `DBFolder` | VBA代入 | `mpDBName` の値 | DB ファイルパス表示 |
|   | `$C$45` | `DBForder` | 設定値 | パス | DB フォルダパス（予備） |
| ✓ | `$C$53` | `PasWard` | 設定値 | パスワード文字列 | シート保護パスワード |

---

## 3. 名前付き範囲一覧

全 **35** 件。

> ✓ = VBA から `Range()` で代入または参照され、業務ロジックに直結する名前付き範囲
> 全件を列挙し、✓ 基準に該当するものにマークを付ける。

| ✓ | 名前 | 参照先 | 業務的意味 |
|---|---|---|---|
| ✓ | `S_Day` | 勤務計画!$G$7 | 勤務計画期間の開始日（11日） |
| ✓ | `E_day` | 勤務計画!$I$7 | 勤務計画期間の終了日 |
| ✓ | `Kinnmu1` | 勤務計画!$H$12:$AL$39 | 勤務記号本体（入力エリア） |
| ✓ | `KinmuMoto` | 勤務計画!$H$8:$AL$8 | 勤務記号コピー元行 |
| ✓ | `K_Simei` | 勤務計画!$E$12:$G$39 | 勤務計画の社員名列 |
| ✓ | `K_Yasumi` | 勤務計画!$H$9:$AL$11 | 休日暦・列ヘッダ |
| ✓ | `TB_Kinmu` | 勤務計画!$E$12:$AL$39 | 社員情報＋勤務記号の全体範囲 |
| ✓ | `KinmuHyou` | 勤務計画!$E$12:$AP$39 | 勤務表全体（背景色制御） |
| ✓ | `Infometion` | 勤務計画!$S$6 | 処理状態メッセージ表示 |
| ✓ | `MsgCell` | 勤務計画!$AC$6 | ユーザー向けガイドメッセージ |
| ✓ | `SyaName` | 勤務計画!$AN$8 | ログインユーザー氏名 |
|   | `KibouTate` | 勤務計画!$H$58 | 希望休み縦方向集計用 |
| ✓ | `Toire1` | トイレ掃除!$H$12:$AL$71 | トイレ掃除記号本体（入力エリア） |
| ✓ | `ToireMoto` | トイレ掃除!$AD$3:$AH$3 | トイレ掃除記号コピー元 |
| ✓ | `T_Simei` | トイレ掃除!$E$12:$G$71 | トイレ掃除の社員名列 |
| ✓ | `Toire_TB` | トイレ掃除!$E$12:$AL$71 | トイレ掃除テーブル全体範囲 |
| ✓ | `ST_Day` | トイレ掃除!$K$3 | トイレ掃除の期間開始日 |
| ✓ | `Siki2` | トイレ掃除!$H$7:$AL$7 | 式テンプレート行 |
| ✓ | `SyainTB1` | 社員データ!$D$7:$J$46 | 社員マスタ表示テーブル |
| ✓ | `SyainTB2` | 社員データ!$K$7:$Q$46 | 社員マスタ比較用テーブル |
| ✓ | `SyainSika` | 社員データ!$E$7:$J$46 | 社員資格入力範囲 |
| ✓ | `Simei` | 社員データ!$F$7:$F$46 | 氏名列（保護） |
| ✓ | `HenUMU` | 社員データ!$C$7:$C$46 | 変更有無フラグ列 |
| ✓ | `Jyunnjyo` | 社員データ!$D$14:$D$46 | 表示順管理列 |
| ✓ | `KinmuTime` | 社員データ!$T$7:$X$33 | 勤務記号→時間対応マスタ |
| ✓ | `KinmuTB` | 社員データ!$T$31:$W$33 | 勤務時間テーブル（DB読書） |
| ✓ | `KikanS` | 社員データ!$T$5 | 勤務時間テーブル期間始 |
| ✓ | `KikanE` | 社員データ!$W$5 | 勤務時間テーブル期間終 |
| ✓ | `Kairann` | 社員データ!$AC$7:$AC$12 | 回覧先（6名分） |
|   | `Siki1` | 社員データ!$C$5 | 変更判定式テンプレート |
| ✓ | `Hyouji` | 社員データ!$D$4 | 社員表示フィルタ条件 |
| ✓ | `DBFolder` | 使用方法!$C$13 | DB ファイルパス表示 |
|   | `DBForder` | 使用方法!$C$45 | DB パス（予備） |
| ✓ | `DebugF` | 使用方法!$A$1 | デバッグフラグ |
| ✓ | `PasWard` | 使用方法!$C$53 | シート保護パスワード |

---

## 4. 数式一覧

| シート | 数式件数 | 備考 |
|---|---|---|
| 勤務計画 | 約 100+ | 勤務記号参照・時間計算・日数集計 |
| トイレ掃除 | 数件 | 日付連動 |
| 社員データ | 数件 | 変更有無判定 |
| 勤務計画印刷用 | 勤務計画と同一 | 印刷用複製 |

### 4.1 勤務計画

| セル | 数式 | 説明 |
|---|---|---|
| `$G$6` | `=IF(OR(S_Day="年月日入力",S_Day=""),"------",S_Day)` | 年月日表示制御 |
| `$N$7` | `=IFERROR(E_day*1-S_Day*1+1-AM11,"")` | 出勤日数（全日数−休日数） |
| `$R$7` | `=IFERROR(N7*8,"")` | 労働時間（出勤日数×8時間） |
| `$H$8`〜`$AL$8` | `=社員データ!T33&""`〜`=社員データ!T7&""` | 勤務記号を社員データから参照 |
| `$AM$11` | `=IF(S_Day="","",COUNTIF(H9:AL9,"○"))` | 休日数カウント |
| `$AM$12`〜`$AM$39` | `=IFERROR(BF12+BH12,"")` | 個人別休日数合計 |
| `$AN$12`〜`$AN$39` | `=IF(G12="","",BD12*3)` | 超勤時間（11h勤務日数×3） |
| `$AO$12`〜`$AO$39` | `=BD12*11+BJ12*8` | 労働時間（11h×日数 + 8h×日数） |
| `$AP$12`〜`$AP$39` | `=G12` | 氏名表示（集計列用） |
| `$AR$10`〜`$BM$10` | `=VLOOKUP(AR11,KinmuTime,5,FALSE)` | 記号ごとの勤務時間参照 |
| `$AR$11`〜`$BM$11` | `=社員データ!T16`等 | 記号名参照 |
| `$BD$12`〜`$BD$39` | `=COUNTIF(H12:AL12,AR$11)+...` | 11時間勤務日数カウント |
| `$BJ$12`〜`$BJ$39` | `=COUNTIF(...)` | 8時間勤務日数カウント |

---

## 5. ボタン・マクロ対応

### 5.1 シート上のボタン（Form Control）

> ✓ = DB 更新・画面遷移・計算実行など副作用のある操作を起動するボタン

| ✓ | シート | ボタンラベル | 割り当てマクロ | 動作概要 |
|---|---|---|---|---|
| ✓ | 勤務計画 | 画面初期化 | `スタート()` | 画面初期化＋資格認証フォーム表示 |
| ✓ | 勤務計画 | 記録 | `記録Main()` | 勤務計画データをDBに記録 |
|   | 勤務計画 | 注意事項 | `注意事項()` | UserForm1（注意事項）表示 |
| ✓ | トイレ掃除 | 記録 | `記録Main()` | トイレ掃除当番データをDBに記録 |
| ✓ | 社員データ | 社員登録 | `社員登録()` | 社員マスタデータをDBに登録 |
| ✓ | 社員データ | 呼出 | `社員テーブル呼出()` | 社員マスタをDBから読込表示 |
| ✓ | 社員データ | 勤務時間登録 | `勤務時間テーブル記録()` | 勤務時間テーブルをDBに記録 |
| ✓ | 社員データ | 回覧先登録 | `回覧先書込()` | 回覧先をDBに記録 |
| ✓ | 社員データ | 呼出（勤務時間） | `勤務時間テーブル読込()` | 勤務時間テーブルをDBから読込 |
| ✓ | 社員データ | 呼出（回覧先） | `回覧先読込()` | 回覧先をDBから読込 |

### 5.2 ユーザーフォーム上のボタン（サマリ）

| ✓ | フォーム | コントロール | キャプション | イベント | 呼び出すプロシージャ | 動作概要 |
|---|---|---|---|---|---|---|
| ✓ | **Form資格.frm** | ComKakunin | 確認 | Click | `ComKakunin_Click()` → `資格()` | 社員番号＋PWで資格認証し権限設定 |
|   | **Form資格.frm** | ComCancel | キャンセル | Click | `ComCancel_Click()` | ブック閉じる |
| ✓ | **Form社員登録.frm** | CommandTuika | 追加/移動 | Click | `CommandTuika_Click()` | 社員をDBに追加/移動登録 |
|   | **Form社員登録.frm** | CommandCancel | キャンセル | Click | `CommandCancel_Click()` | フォーム閉じる |

### 5.3 CommandBar に動的追加されるボタン（アドイン風）

本ファイルには CommandBar に動的追加されるボタンは存在しない。

---

## 6. VBA モジュール仕様

### 6.0 全プロシージャ一覧

全 **60** 件。

> ✓ = ユーザー操作の起点（Click イベント等） / DB I/O を実行 / 他モジュールから呼び出される Public
> 全プロシージャを列挙し、✓ 基準に該当するものにマークを付ける。

| ✓ | モジュール | プロシージャ | 種別 | 概要 |
|---|---|---|---|---|
| ✓ | **ThisWorkbook.cls** | `Workbook_Open()` | Event | ブック起動時に `スタート()` 呼出 |
| ✓ | **ThisWorkbook.cls** | `Workbook_BeforeClose()` | Event | 終了時に未記録確認・保存促進 |
| ✓ | **ThisWorkbook.cls** | `Workbook_BeforePrint()` | Event | シート別印刷パラメータ設定 |
|   | **ThisWorkbook.cls** | `Workbook_BeforeSave()` | Event | DebugF以外は保存禁止 |
| ✓ | **Sheet1.cls** | `Worksheet_Activate()` | Event | 「勤務計画」アクティブ時の行表示制御 |
|   | **Sheet1.cls** | `Worksheet_BeforeDoubleClick()` | Event | ダブルクリックで黄色ハイライト |
| ✓ | **Sheet1.cls** | `Worksheet_BeforeRightClick()` | Event | 右クリックでセル消去（権限制御付） |
| ✓ | **Sheet1.cls** | `Worksheet_Change()` | Event | 年月日入力時に表体裁・DB読込実行 |
|   | **Sheet1.cls** | `Worksheet_Deactivate()` | Event | 空（処理なし） |
| ✓ | **Sheet1.cls** | `Worksheet_SelectionChange()` | Event | セル選択変更でコピー＆ペースト制御 |
| ✓ | **Sheet1.cls** | `myActivCell()` | Sub | 勤務記号のコピー元→先ペースト処理 |
|   | **Sheet1.cls** | `Kibou()` | Function | 希望休み検出判定 |
| ✓ | **Sheet3.cls** | `Worksheet_BeforeRightClick()` | Event | 「トイレ掃除」右クリックで消去 |
| ✓ | **Sheet3.cls** | `Worksheet_SelectionChange()` | Event | セル選択変更でコピー＆ペースト制御 |
| ✓ | **Sheet3.cls** | `myActivCell()` | Sub | トイレ掃除記号のコピー＆ペースト処理 |
| ✓ | **Sheet6.cls** | `Worksheet_Change()` | Event | 「社員データ」社員番号入力で照射管理DB照会 |
| ✓ | **Sheet6.cls** | `SyainRead()` | Sub | 社員番号→社員名DB検索 |
|   | **Sheet6.cls** | `XSyainRead()` | Sub | 社員番号→社員名DB検索（別版） |
| ✓ | **初期設定.bas** | `スタート()` | Sub | 初期設定実行＋Form資格表示＋回覧先読込 |
| ✓ | **初期設定.bas** | `画面の初期設定()` | Sub | 全シートの保護・表示・クリア処理 |
| ✓ | **表作成_勤務Read.bas** | `表体裁()` | Sub | 日付変更時の全体表作成処理 |
| ✓ | **表作成_勤務Read.bas** | `表体裁_日付()` | Sub | 日付行・暦の設定（ExYasumiX DB参照） |
| ✓ | **表作成_勤務Read.bas** | `表体裁_氏名_作成中トイレ()` | Sub | トイレ掃除対象社員をDB読込・表示 |
| ✓ | **表作成_勤務Read.bas** | `表体裁_氏名_作成中ガンマ()` | Sub | ガンマ勤務対象社員をDB読込・表示 |
| ✓ | **表作成_勤務Read.bas** | `ガンマ勤務過去表示()` | Sub | 過去の勤務データをDB読込・表示 |
| ✓ | **表作成_勤務Read.bas** | `トイレ当番過去表示()` | Sub | 過去のトイレ当番データをDB読込・表示 |
| ✓ | **表作成_勤務Read.bas** | `行列表示_非表示()` | Sub | 社員数に応じた行/列非表示制御 |
| ✓ | **表作成_勤務Read.bas** | `最終行列取得()` | Sub | 有効最終行/列の取得 |
| ✓ | **表作成_勤務Read.bas** | `勤務表保護解除()` | Sub | 資格に応じた入力行の保護解除 |
| ✓ | **表作成_勤務Read.bas** | `勤務記号()` | Sub | 資格に応じた記号表示書式制御 |
| ✓ | **勤務データ記録.bas** | `記録Main()` | Sub | 記録処理のメイン（確認→勤務記録呼出） |
| ✓ | **勤務データ記録.bas** | `勤務記録()` | Sub | 勤務計画＋トイレ掃除のDB記録実行 |
| ✓ | **勤務データ記録.bas** | `記録()` | Sub | 勤務/トイレの個別DB記録（INSERT/UPDATE） |
| ✓ | **社員_勤務データ.bas** | `社員_勤務時間_回覧先呼出()` | Sub | 社員＋勤務時間＋回覧先の一括呼出 |
| ✓ | **社員_勤務データ.bas** | `社員テーブル呼出()` | Sub | ガンマ入出荷勤務テーブルからシートに読込 |
| ✓ | **社員_勤務データ.bas** | `社員登録()` | Sub | 社員テーブル書込の実行制御 |
|   | **社員_勤務データ.bas** | `UP()` | Sub | 社員表示順を上に移動 |
|   | **社員_勤務データ.bas** | `Down()` | Sub | 社員表示順を下に移動 |
| ✓ | **社員_勤務データ.bas** | `社員テーブル書込()` | Function | 変更行をDBに登録（INSERT/UPDATE） |
|   | **社員_勤務データ.bas** | `DataChek()` | Function | 入力チェック（重複・空欄検出） |
| ✓ | **社員_勤務データ.bas** | `勤務時間テーブル記録()` | Sub | 勤務時間テーブルをDBに記録 |
| ✓ | **社員_勤務データ.bas** | `勤務時間テーブル読込()` | Sub | 勤務時間テーブルをDBから読込 |
| ✓ | **社員_勤務データ.bas** | `回覧先読込()` | Sub | 回覧先テーブルをDBから読込 |
| ✓ | **社員_勤務データ.bas** | `回覧先書込()` | Sub | 回覧先テーブルをDBに記録 |
| ✓ | **SQL_AccessExe.bas** | `Open_AccessDB()` | Sub | Access DB 接続確立 |
| ✓ | **SQL_AccessExe.bas** | `SQL_Access_Exe()` | Sub | SQL 実行（Access） |
| ✓ | **SQL_AccessExe.bas** | `SQL_Access_INSERT_UPDATE()` | Sub | 存在チェック付き INSERT or UPDATE |
| ✓ | **SQL_AccessExe.bas** | `SQL_Access_INSERT()` | Function | INSERT文生成 |
| ✓ | **SQL_AccessExe.bas** | `SQL_Access_UPDATE()` | Function | UPDATE文生成 |
| ✓ | **SQL_AccessExe.bas** | `SQL_Access_Delete()` | Sub | DELETE 実行 |
| ✓ | **SQL_AccessExe.bas** | `Disp_Access_Sheet()` | Sub | SELECT結果をシートに直接表示 |
| ✓ | **SQL_AccessExe.bas** | `Set_Access_Array()` | Sub | SELECT結果を配列に格納 |
|   | **SQL_AccessExe.bas** | `Set_Access_Close()` | Sub | DB 接続クローズ |
| ✓ | **SQL_Execution.bas** | `Open_oraconDB()` | Sub | Oracle(ricdb) DB 接続確立 |
| ✓ | **SQL_Execution.bas** | `SQL_Exe()` | Sub | SQL 実行（Oracle） |
| ✓ | **SQL_Execution.bas** | `SQL_INSERT_UPDATE()` | Sub | 存在チェック付き INSERT or UPDATE（Oracle） |
| ✓ | **SQL_Execution.bas** | `SQL_Delete()` | Sub | DELETE 実行（Oracle） |
| ✓ | **SQL_Execution.bas** | `Disp_Sheet()` | Sub | SELECT結果をシートに表示（Oracle） |
| ✓ | **SQL_Execution.bas** | `Set_Array()` | Sub | SELECT結果を配列に格納（Oracle） |
|   | **印刷設定.bas** | `印刷パラメータ設定()` | Sub | 印刷設定のユーティリティ |
|   | **Debug用.bas** | `EventTrue()` | Sub | イベント有効化・カーソル復元 |
|   | **Debug用.bas** | `DEUG画面()` | Sub | デバッグ用全表示モード |
| ✓ | **コマンド表示_非表示.bas** | `コマンド表示非表示設定()` | Sub | 資格に応じたボタン表示/非表示・保護制御 |
|   | **Module1.bas** | `注意事項()` | Sub | UserForm1（注意事項）表示 |
| ✓ | **Form資格.frm** | `ComCancel_Click()` | Event | キャンセル→ブック閉じる |
| ✓ | **Form資格.frm** | `ComKakunin_Click()` | Event | 確認ボタン→資格認証実行 |
|   | **Form資格.frm** | `UserForm_Activate()` | Event | 初期値設定（9999/閲覧） |
| ✓ | **Form資格.frm** | `資格()` | Sub | DB照会で社員資格判定・PW検証 |
| ✓ | **Form社員登録.frm** | `CommandCancel_Click()` | Event | キャンセル→フォーム閉じる |
| ✓ | **Form社員登録.frm** | `CommandTuika_Click()` | Event | 社員追加/移動のDB登録処理 |
| ✓ | **Form社員登録.frm** | `TextSyainID_AfterUpdate()` | Event | 社員番号入力後に社員呼出 |
| ✓ | **Form社員登録.frm** | `社員呼出()` | Sub | 照射管理システムから社員名取得 |
|   | **Form社員登録.frm** | `UserForm_Activate()` | Event | 初期値クリア |

---

## 7. ユーザーフォーム仕様

> ✓ = ユーザー入力を受け付ける、またはイベントで業務処理を起動するコントロール

### 7.1 Form資格.frm（資格認証フォーム）

**目的**: ブック起動時に社員番号とパスワードで認証を行い、操作資格レベル（作成資格/本人のみ/閲覧）を決定する。
**フォームキャプション**: "社員確認"
**モードレス表示**: ShowModal=True

#### コントロール一覧

| ✓ | コントロール | 種別 | キャプション | 親フレーム | 用途 |
|---|---|---|---|---|---|
| ✓ | TextBox1 | TextBox | — | — | 社員番号入力 |
| ✓ | TextPW | TextBox | — | — | パスワード入力 |
| ✓ | ComKakunin | CommandButton | 確認 | — | 資格認証実行 |
|   | ComCancel | CommandButton | キャンセル | — | ブック終了 |
|   | Label1 | Label | 社員番号 | — | ラベル |
|   | Label2 | Label | パスワード | — | ラベル |
|   | Label3 | Label | — | — | 説明ラベル |
|   | Label4 | Label | — | — | 社員名表示または説明 |

#### イベント一覧

| ✓ | イベント | 動作概要 |
|---|---|---|
| ✓ | `ComKakunin_Click()` | `ガンマ入出荷勤務` テーブルで資格確認 → `shainmst` でPW検証 → `mpSikaku` 設定 → `コマンド表示非表示設定()` 呼出 |
|   | `ComCancel_Click()` | フォーム閉じてブック終了 |
|   | `UserForm_Activate()` | mpSyainID="9999", mpSikaku=9 に初期化 |

### 7.2 Form社員登録.frm（社員追加/移動フォーム）

**目的**: 新規社員の追加、または既存社員の表示順移動を行う。ガンマ勤務/トイレ掃除対象者チェックボックスで対象区分を指定。
**フォームキャプション**: "社員追加の社員登録"
**モードレス表示**: ShowModal=True

#### コントロール種別サマリ

| 種別 | 件数 | 内訳 |
|---|---|---|
| Label | 3 | Label1（社員番号）, Label2（社員名）, Label3 |
| TextBox | 1 | TextSyainID（社員番号入力） |
| CommandButton | 2 | CommandTuika（追加/移動）, CommandCancel（キャンセル） |
| CheckBox | 2 | CheckGannma（ガンマ勤務対象）, CheckToire（トイレ掃除対象） |
| OptionButton | 2 | OptionButton1（上に追加）, OptionButton2（下に追加） |
| Label（表示用） | 1 | LabelSyaName（社員名表示） |

#### コントロール一覧

| ✓ | コントロール | 種別 | キャプション | 親フレーム | 用途 |
|---|---|---|---|---|---|
| ✓ | TextSyainID | TextBox | — | — | 社員番号入力（AfterUpdate で DB 照会） |
| ✓ | CommandTuika | CommandButton | 追加/移動 | — | 社員追加/移動のDB登録実行 |
|   | CommandCancel | CommandButton | キャンセル | — | フォーム閉じる |
| ✓ | CheckGannma | CheckBox | ガンマ勤務対象 | — | ガンマ勤務表対象者フラグ |
| ✓ | CheckToire | CheckBox | トイレ掃除対象 | — | トイレ掃除対象者フラグ |
|   | OptionButton1 | OptionButton | 上に追加 | — | 追加位置指定（上） |
|   | OptionButton2 | OptionButton | 下に追加 | — | 追加位置指定（下） |
|   | LabelSyaName | Label | — | — | DB照会結果の社員名表示 |
|   | Label1 | Label | 社員番号 | — | ラベル |
|   | Label2 | Label | 社員名 | — | ラベル |
|   | Label3 | Label | — | — | 説明ラベル |

#### イベント一覧

| ✓ | イベント | 動作概要 |
|---|---|---|
| ✓ | `TextSyainID_AfterUpdate()` | 社員番号入力後に `社員呼出()` → `shainmst` から社員名取得、`ガンマ入出荷勤務` で既登録チェック |
| ✓ | `CommandTuika_Click()` | `ガンマ入出荷勤務` に INSERT/UPDATE → 表示順再採番 → `勤務予定表` に登録 → `表体裁()` 再実行 |
|   | `CommandCancel_Click()` | フォーム閉じる |
|   | `UserForm_Activate()` | 初期値クリア・チェックボックス ON |

### 7.3 UserForm1.frm（注意事項フォーム）

**目的**: 注意事項を表示する読み取り専用フォーム。コードは空。

#### コントロール一覧

| ✓ | コントロール | 種別 | キャプション | 親フレーム | 用途 |
|---|---|---|---|---|---|
|   | Label1 | Label | 注意事項テキスト | — | 注意事項の表示 |

#### イベント一覧

イベントコードなし。

---

## 8. DB 接続・外部連携

### 8.1 ODBC 接続設定

本ファイルは 2 系統の DB 接続を使用する。

| 接続方式 | 接続先 | UID | PWD | 用途 |
|---|---|---|---|---|
| ADO + ACE.OLEDB.12.0 | `\\163.59.144.156\ExRicSys\ガンマ勤務計画DB.accdb` | — | — | 勤務計画・社員マスタの読書き |
| ADO + ODBC | DSN=ricdb | ric | t6101 | 照射管理システム（Oracle）の参照 |

> **DB サーバー IP**: 163.59.144.156（Access DB 共有フォルダ）

### 8.2 テーブル一覧（参照/更新区分付き）

> ✓ = INSERT / UPDATE / DELETE の対象テーブル（参照のみのテーブルは ✓ なし）

| ✓ | テーブル名 | 区分 | DB | 主な用途 | キー列 | 参照/更新列 |
|---|---|---|---|---|---|---|
| ✓ | `勤務予定表` | **参照＋更新（DELETE+INSERT/UPDATE）** | Access | 勤務シフト計画データ | `職場`, `勤務期間始`, `社員番号` | 更新: `日1`〜`日31`, `表示順`（`記録()` / `CommandTuika_Click()` から INSERT/UPDATE/DELETE） |
| ✓ | `ガンマ入出荷勤務` | **参照＋更新** | Access | 社員マスタ（勤務表用） | `社員番号` | 更新: `表示順`, `社員名`, `ガンマ勤務表対象者`, `トイレ掃除対象者`, `作成資格`, `有効無効`（`社員テーブル書込()` / `CommandTuika_Click()` から INSERT/UPDATE） |
| ✓ | `労働時間表` | **参照＋更新** | Access | 勤務記号別労働時間定義 | `番号`, `期間始` | 更新: `記号`, `装置１`, `装置２`, `装置３`, `勤務時間`, `期間終`（`勤務時間テーブル記録()` から INSERT/UPDATE） |
| ✓ | `回覧先` | **参照＋更新** | Access | 勤務表回覧先設定 | `ID=1`（固定） | 更新: `回覧１`〜`回覧６`（`回覧先書込()` から INSERT/UPDATE） |
|   | `ExYasumiX` | 参照 | Access | 休日カレンダーマスタ | `KYUUJITU1` | 参照: `KYUUJITU1`（`表体裁_日付()` で休日判定に使用） |
|   | `shainmst` | 参照 | Oracle(ricdb) | 照射管理システム社員マスタ | `shano` | 参照: `shaname`, `hshika`, `shask`（資格認証・社員名取得） |

### 8.3 主要 SQL（抜粋）

```sql
-- 勤務予定表への記録（INSERT or UPDATE）
-- キー: 職場='ガンマ' AND 勤務期間始=<日付数値> AND 社員番号='<番号>'
INSERT INTO 勤務予定表 (職場,勤務期間始,社員番号,表示順,日1,...,日31)
VALUES('ガンマ',<日付>,<社員番号>,<表示順>,<日1>,...,<日31>)
-- or
UPDATE 勤務予定表 SET 日1=<値>,...,日31=<値>
WHERE 職場='ガンマ' AND 勤務期間始=<日付> AND 社員番号='<番号>'
```

```sql
-- 休日カレンダー読込
SELECT KYUUJITU1 FROM ExYasumiX
WHERE KYUUJITU1>=<期間始> AND KYUUJITU1<=<期間終>
ORDER BY KYUUJITU1
```

```sql
-- ガンマ勤務対象社員読込（作成中）
SELECT CINT(社員番号),表示順,社員名 FROM ガンマ入出荷勤務
WHERE 有効無効='有効' AND ガンマ勤務表対象者='有効'
ORDER BY 表示順
```

```sql
-- 過去の勤務データ読込
SELECT CINT(k.社員番号),ガンマ入出荷勤務.表示順,ガンマ入出荷勤務.社員名,
       k.日1,...,k.日31
FROM 勤務予定表 k,ガンマ入出荷勤務
WHERE k.勤務期間始=<日付> AND k.職場='ガンマ'
  AND k.社員番号=ガンマ入出荷勤務.社員番号
ORDER BY ガンマ入出荷勤務.表示順
```

```sql
-- 資格認証（Oracle ricdb）
SELECT 作成資格,社員名,有効無効 FROM ガンマ入出荷勤務
WHERE 社員番号='<社員番号>'

SELECT shask FROM shainmst WHERE shano='<社員番号>'
```

```sql
-- 社員登録チェック（Oracle ricdb → 照射管理システム）
SELECT TRIM(REPLACE(shaname,'　','')),hshika FROM shainmst
WHERE shano='<社員番号>'

SELECT COUNT(*) FROM ガンマ入出荷勤務 WHERE 社員番号='<社員番号>'
```

### 8.4 外部ファイル連携

外部ファイル連携は存在しない。

---

## 9. データフロー

場所マークの凡例: 📊 シート操作 / 🖥️ 画面操作 / 🗄️ DB操作 / 📄 VBA内部処理

### 9.1 起動フロー

| # | 起点 | 処理 | 結果 |
|---|---|---|---|
| 1 | 📄 ブックを開く | 📄 `Workbook_Open()` / **ThisWorkbook.cls** | `スタート()` 呼出 |
| 2 | 上記 | 📊 `画面の初期設定()` / **初期設定.bas** | 全シートの保護・表示初期化・セルクリア |
| 3 | 上記 | 🖥️ **Form資格.frm** 表示 | ユーザーに社員番号＋PW 入力促進 |
| 4 | 🖥️ 確認ボタン Click | 🗄️ `資格()` / **Form資格.frm** | `ガンマ入出荷勤務`＋`shainmst` で認証 |
| 5 | 上記 | 📄 `コマンド表示非表示設定()` / **コマンド表示_非表示.bas** | mpSikaku に応じたボタン表示制御 |
| 6 | 上記 | 🗄️ `回覧先読込()` / **社員_勤務データ.bas** | 回覧先テーブルから読込→「社員データ」シートに表示 |

#### ツリー図（補助）

```
（ブックを開く）
└─ 📄 Workbook_Open                [ThisWorkbook.cls]
   └─ 📄 スタート                   [初期設定.bas]
      ├─ 📊 画面の初期設定          [初期設定.bas]（全シート保護・初期化）
      ├─ 🖥️ Form資格.Show          [Form資格.frm]
      │   └─ （確認ボタン Click）
      │       ├─ 🗄️ 資格            [Form資格.frm]（ガンマ入出荷勤務 + shainmst 照会）
      │       └─ 📊 コマンド表示非表示設定  [コマンド表示_非表示.bas]
      └─ 🗄️ 回覧先読込              [社員_勤務データ.bas]（回覧先テーブル SELECT）
```

### 9.2 勤務計画表示フロー（日付入力時）

| # | 起点 | 処理 | 結果 |
|---|---|---|---|
| 1 | 📊 `$G$7`（S_Day）に日付入力 | 📄 `Worksheet_Change()` / **Sheet1.cls** | 11日でない場合エラー |
| 2 | 上記 | 📊 `表体裁()` / **表作成_勤務Read.bas** | 表全体のクリア＋再構成 |
| 3 | 上記 | 🗄️ `表体裁_日付()` / **表作成_勤務Read.bas** | `ExYasumiX` から休日読込→暦セット |
| 4 | 上記 | 🗄️ `表体裁_氏名_作成中ガンマ()` | `ガンマ入出荷勤務`＋`勤務予定表` から読込 |
| 5 | 上記 | 🗄️ `表体裁_氏名_作成中トイレ()` | `ガンマ入出荷勤務`＋`勤務予定表` から読込 |
| 6 | 上記 | 📊 `行列表示_非表示()` | 社員数に応じた行非表示 |
| 7 | 上記 | 📊 `勤務表保護解除()` | 資格に応じた入力行の保護解除 |
| 8 | 上記 | 🗄️ `勤務時間テーブル読込()` / **社員_勤務データ.bas** | `労働時間表` から読込 |
| 9 | 上記 | 📊 `勤務記号()` | 記号表示書式の設定 |

#### ツリー図（補助）

```
（S_Day に日付入力）
└─ 📊 Worksheet_Change             [Sheet1.cls]
   ├─ 📊 表体裁                     [表作成_勤務Read.bas]
   │   ├─ 🗄️ 表体裁_日付           [表作成_勤務Read.bas]（ExYasumiX SELECT）
   │   ├─ [E_day < Date の場合]
   │   │   ├─ 🗄️ トイレ当番過去表示  （勤務予定表 SELECT）
   │   │   └─ 🗄️ ガンマ勤務過去表示  （勤務予定表 SELECT）
   │   ├─ [E_day >= Date の場合]
   │   │   ├─ 🗄️ 表体裁_氏名_作成中ガンマ  （ガンマ入出荷勤務 + 勤務予定表 SELECT）
   │   │   └─ 🗄️ 表体裁_氏名_作成中トイレ  （ガンマ入出荷勤務 + 勤務予定表 SELECT）
   │   ├─ 📊 行列表示_非表示
   │   └─ 📊 最終行列取得
   ├─ 🗄️ 勤務時間テーブル読込       [社員_勤務データ.bas]（労働時間表 SELECT）
   └─ 📊 勤務記号                   [表作成_勤務Read.bas]
```

### 9.3 勤務記録フロー（DB更新）

| # | 起点 | 処理 | 結果 |
|---|---|---|---|
| 1 | 📊（記録ボタン Click） | 📄 `記録Main()` / **勤務データ記録.bas** | 確認ダイアログ表示 |
| 2 | 上記 | 📄 `勤務記録()` / **勤務データ記録.bas** | 日付チェック → Infometion="記録中" |
| 3 | 上記（資格=1） | 🗄️ `勤務時間テーブル記録()` / **社員_勤務データ.bas** | `労働時間表` に INSERT/UPDATE |
| 4 | 上記 | 🗄️ `記録("トイレ")` / **勤務データ記録.bas** | `勤務予定表`（職場='トイレ'）に INSERT/UPDATE |
| 5 | 上記 | 🗄️ `記録("ガンマ")` / **勤務データ記録.bas** | `勤務予定表`（職場='ガンマ'）に INSERT/UPDATE |
| 6 | 上記 | 📄 mpRecordF = True | 記録済フラグ設定 |

#### ツリー図（補助）

```
（記録ボタン Click）
└─ 📄 記録Main                      [勤務データ記録.bas]
   └─ 📄 勤務記録                   [勤務データ記録.bas]
      ├─ [mpSikaku = 1 の場合]
      │   ├─ 🗄️ 勤務時間テーブル記録  [社員_勤務データ.bas]（労働時間表 INSERT/UPDATE）
      │   ├─ 🗄️ 記録("トイレ")        [勤務データ記録.bas]（勤務予定表 INSERT/UPDATE）
      │   └─ 🗄️ 記録("ガンマ")        [勤務データ記録.bas]（勤務予定表 INSERT/UPDATE）
      └─ [mpSikaku = 0 の場合]
          ├─ 🗄️ 記録("トイレ")        （mpKirokuF=True の場合のみ）
          └─ 🗄️ 記録("ガンマ")        （本人行のみ INSERT/UPDATE）
```

### 9.4 社員登録フロー

| # | 起点 | 処理 | 結果 |
|---|---|---|---|
| 1 | 📊（社員登録ボタン Click） | 📄 `社員登録()` / **社員_勤務データ.bas** | 確認ダイアログ表示 |
| 2 | 上記 | 📄 `DataChek()` / **社員_勤務データ.bas** | 入力チェック（重複・空欄） |
| 3 | 上記 | 🗄️ `社員テーブル書込()` / **社員_勤務データ.bas** | 変更行を `ガンマ入出荷勤務` に INSERT/UPDATE |
| 4 | 上記 | 🗄️ `社員テーブル呼出()` / **社員_勤務データ.bas** | 最新データを再読込 |

#### ツリー図（補助）

```
（社員登録ボタン Click）
└─ 📄 社員登録                       [社員_勤務データ.bas]
   ├─ 📄 DataChek                   [社員_勤務データ.bas]（入力チェック）
   ├─ 🗄️ 社員テーブル書込           [社員_勤務データ.bas]（ガンマ入出荷勤務 INSERT/UPDATE）
   └─ 🗄️ 社員テーブル呼出           [社員_勤務データ.bas]（ガンマ入出荷勤務 SELECT）
```

### 9.5 社員追加/移動フロー（Form社員登録）

| # | 起点 | 処理 | 結果 |
|---|---|---|---|
| 1 | 🖥️ TextSyainID に社員番号入力 | 🗄️ `社員呼出()` / **Form社員登録.frm** | `shainmst` から社員名取得 + 既登録判定 |
| 2 | 🖥️ 追加/移動ボタン Click | 🗄️ `CommandTuika_Click()` / **Form社員登録.frm** | `ガンマ入出荷勤務` に INSERT/UPDATE |
| 3 | 上記 | 🗄️ 表示順再採番 | 全社員の表示順を連番で再UPDATE |
| 4 | 上記 | 🗄️ `勤務予定表` に登録/削除 | 当月分の勤務予定を追加/削除 |
| 5 | 上記 | 📊 `表体裁()` 再実行 | 表の再描画 |

#### ツリー図（補助）

```
（社員番号入力 → 追加/移動ボタン Click）
└─ 🖥️ Form社員登録.frm
   ├─ 🗄️ 社員呼出                   （shainmst SELECT / ガンマ入出荷勤務 SELECT）
   └─ 🗄️ CommandTuika_Click
      ├─ 🗄️ SQL_Access_INSERT_UPDATE  （ガンマ入出荷勤務 INSERT/UPDATE）
      ├─ 🗄️ 表示順再採番             （ガンマ入出荷勤務 UPDATE × N件）
      ├─ 🗄️ 勤務予定表登録/削除      （勤務予定表 INSERT/UPDATE/DELETE）
      └─ 📊 表体裁                   [表作成_勤務Read.bas]（再描画）
```

---

## 10. セキュリティ注意事項

olevba 解析結果:

| 種別 | キーワード | 内容 |
|---|---|---|
| AutoExec | `Workbook_Open` | ファイルを開くと自動実行 |
| AutoExec | `Workbook_BeforeClose` | ファイルを閉じると自動実行 |
| AutoExec | `ComCancel_Click` | ActiveX オブジェクトのイベントで実行 |
| AutoExec | `Worksheet_Change` | ActiveX オブジェクトのイベントで実行 |
| Suspicious | `Open` | ファイルを開く可能性 |
| Suspicious | `Command` | PowerShell コマンドを実行する可能性（誤検出: ADO Command オブジェクト） |
| Suspicious | `Call` | DLL 呼出の可能性（誤検出: VBA の Call ステートメント） |
| Suspicious | `Chr` | 文字列難読化の可能性（改行 `Chr(13)` に使用） |
| Suspicious | Hex Strings | 16進エンコード文字列検出（フォーム内バイナリ） |
| Suspicious | Base64 Strings | Base64エンコード文字列検出（フォーム内バイナリ） |
| IOC | `163.59.144.156` | IPv4 アドレス（Access DB 共有サーバー） |

> **注意**: DB 接続文字列（DSN=ricdb, UID=ric, PWD=t6101）および Access DB パスが VBA に平文で埋め込まれています。外部共有時はマスキング・権限分離を推奨します。
> シート保護パスワードも `使用方法!$C$53`（`PasWard`）に平文で保持されています。

---

## スコープ外（本仕様書に含まないもの）

- セル書式（色・罫線・フォント）
- 条件付き書式、グラフ・画像、印刷設定

必要な場合は Excel 画面のスクリーンショットで補完してください。
