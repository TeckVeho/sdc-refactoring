# Ex装置運転状況 仕様書

> **対象ファイル**: Ex装置運転状況.xlsm
> **ファイル種別**: .xlsm（マクロ付き）
> **用途**: 照射装置（1号機・2号機・3号機）の運転時間・停止時間・稼働率を集計・グラフ表示し、1号機のイベント別集計・詳細表示を行う装置運転状況管理ツール
> **VBA プロジェクト**: モジュール 7 本（.bas 4 / .cls 3 / .frm 0）
> **外部連携**: DSN=ricdb（Oracle）、DB接続先 UID=ric / PWD=t6101
> **解析日**: 2026-06-29（excel-to-md スキルによる自動解析）

---

## 凡例（本仕様書の表記ルール）

本仕様書では、保守時の判別を容易にするため、以下の表記ルールを使用します。

### 用語規約

| 用語 | 意味 |
| --- | --- |
| EXメニュー | ExRicSys フォルダに配置される VBA ファイル群（Ex*.xlsm）の総称 |
| `ExRicSys` | 配置フォルダ名。初出時は〈EXメニュー配置フォルダ〉と注記 |

### 表記規則

| 種別 | 表記 | 例 |
| --- | --- | --- |
| モジュール（.bas / .cls） | **太字** | **SQL_Execution.bas** |
| ユーザーフォーム（.frm） | **太字** | — |
| プロシージャ / イベント | `コード体()` | `装置稼働時間表示()` ※Markdown 制約上、斜体ではなくコード体で統一 |
| シート名 | 「」 | 「稼働時間」 |
| セル参照 | `コード体` | `$C$4` |
| 名前付き範囲 | `コード体` | `KaisiDay` |
| DB テーブル / カラム | `コード体` | `sengnr1` / `sdate` |
| ユーザー操作 | （操作名） | （実行ボタン Click） |
| 主要マーク | ✓ | ✓ = 保守時に最初に確認すべき項目 |

### データフロー 場所マーク（9章）

9章のデータフロー（テーブル・ツリー図）では、処理が行われる場所を以下のアイコンで区別します。

| アイコン | 種別 | 意味 |
| --- | --- | --- |
| 📊 | シート操作 | ワークシート上のセル書込み・読取り・表示変更 |
| 🖥️ | 画面操作 | ユーザーフォーム（.frm）の表示・入力・操作 |
| 🗄️ | DB操作 | DB への SELECT / INSERT / UPDATE / DELETE |
| 📄 | VBA内部処理 | 変数計算・条件分岐など、画面・シートに直接関与しない処理 |

### ✓（主要マーク）の判定基準

✓ は **保守時に最初に確認すべき項目** を示します。
判定基準は対象の種類ごとに以下のとおりです。

| 章 | 対象 | ✓ の判定基準 |
| --- | --- | --- |
| 1.1 | シート | ユーザーが直接操作する、または VBA が動的に表示/非表示を切り替える |
| 1.3 / 6.0 | VBA モジュール | ① ユーザー操作の起点 ② DB I/O を含む ③ 他モジュールから呼び出される ④ コード行数上位 25% のいずれか |
| 2 | セル / 名前付き範囲 | VBA から `Range()` で代入または参照され、業務ロジックに直結する |
| 3 | 名前付き範囲 | VBA から `Range()` で代入または参照され、業務ロジックに直結する |
| 5 | ボタン / コントロール | DB 更新・画面遷移・計算実行など副作用のある操作を起動する |
| 6.0（全プロシージャ） | プロシージャ | ① ユーザー操作の起点（Click イベント等） ② DB I/O を実行 ③ 他モジュールから呼び出される Public のいずれか |
| 8.2 | DB テーブル | INSERT / UPDATE / DELETE の対象（参照のみのテーブルは ✓ なし） |

---

## 目次

1. [ファイル構成](#1-ファイル構成)
2. [シート詳細](#2-シート詳細)
3. [名前付き範囲一覧](#3-名前付き範囲一覧)
4. [数式一覧](#4-数式一覧)
5. [ボタン・マクロ対応](#5-ボタンマクロ対応)
6. [VBA モジュール仕様](#6-vba-モジュール仕様)
7. [ユーザーフォーム仕様](#7-ユーザーフォーム仕様)
8. [DB 接続・外部連携](#8-db-接続外部連携)
9. [データフロー](#9-データフロー)
10. [セキュリティ注意事項](#10-セキュリティ注意事項)

---

## 1. ファイル構成

### 1.1 シート一覧

> ✓ = ユーザーが直接操作する、または VBA が動的に表示/非表示を切り替えるシート

| ✓ | No | シート名 | 最大行 | 最大列 | 保存時 Visible | VBA による動的切替 |
| --- | --- | --- | --- | --- | --- | --- |
| ✓ | 1 | 稼働時間 | 1472 | 7 (G) | visible | グラフ・ボタンの表示/非表示を制御 |
| ✓ | 2 | 1号機集計表 | 10005 | 13 (M) | visible | Protect/Unprotect 制御 |

### 1.2 ユーザーフォーム一覧

なし。ユーザーフォームは存在しない。

### 1.3 VBA モジュール一覧

> ✓ = ユーザー操作の起点 / DB I/O を含む / 他モジュールから呼び出される / コード行数上位 25%
> 全モジュールを列挙し、✓ 基準に該当するものにマークを付ける。

| ✓ | No | モジュール | 種別 | プロシージャ数 | 主な役割 |
| --- | --- | --- | --- | --- | --- |
| ✓ | 1 | **ThisWorkbook.cls** | .cls | 2 | 起動/終了イベント |
|  | 2 | **Sheet1.cls** | .cls | 0 | 「1号機集計表」シート（コードなし） |
| ✓ | 3 | **Sheet2.cls** | .cls | 1 | 「稼働時間」シートイベント |
| ✓ | 4 | **SQL_Execution.bas** | .bas | 6 | DB接続・SQL実行・シート表示の共通処理 |
| ✓ | 5 | **運転時間.bas** | .bas | 12 | 装置稼働時間の計算・グラフデータ生成 |
| ✓ | 6 | **イベント集計.bas** | .bas | 3 | 1号機イベント別集計・詳細表示 |
| ✓ | 7 | **クリア.bas** | .bas | 2 | 初期化処理・スタート画面設定 |
|  | 8 | **終了処理.bas** | .bas | 2 | ブック終了・クリア処理 |

> **Sheet*.cls の記載ルール**: コードネーム（Sheet1, Sheet2）と対応するシートタブ名を
> 必ず「」で囲んで紐づける。コードがある場合は「<シート名>」シートイベント、
> コードがない（`Option Explicit` のみ）場合は「<シート名>」シート（コードなし）と記載する。

**コードネーム対応表**:
- Sheet1（VBA内部名）→ 「1号機集計表」（タブ名）— xl/worksheets/sheet2.xml
- Sheet2（VBA内部名）→ 「稼働時間」（タブ名）— xl/worksheets/sheet1.xml

---

## 2. シート詳細

### 2.0 シート可視性一覧

| No | シート | VBA による非表示化 | 表示するタイミング | 非表示にするタイミング | 制御プロシージャ |
| --- | --- | --- | --- | --- | --- |
| 1 | 稼働時間 | — | — | — | — |
| 2 | 1号機集計表 | — | — | — | — |

> 以下の各シートのレイアウト構造表における ✓ = VBA から `Range()` で代入または参照され、業務ロジックに直結するセル

### 2.1 稼働時間

**目的**: 照射装置3台（1号機・2号機・3号機）の運転時間・停止時間・稼働率を期間指定で集計し、1日指定時は分単位のトレンドグラフを表示する。

#### 非表示行・列

なし。

#### レイアウト構造

```
行 1: （空）
行 2: タイトル行（C2: 「装置運転実績（照射／貯蔵トレンドは１日指定時）」）
行 3: 列見出し（集計開始日 / 集計終了日 / 総時間）
行 4: 入力・計算結果行（開始日・終了日・総時間）
行 5: 列見出し（時間 / 1号機 / 2号機 / 3号機）
行 6: 運転時間（VBA 書込み）
行 7: 停止時間（VBA 書込み）
行 8: 稼働率（数式計算）
行 9〜29: （空）
行 30: グラフデータ列見出し（N0 / 時刻 / 1号機 / 2号機 / 3号機 / ダミー）
行 31〜1471: グラフデータ本体（1分刻み 0:00〜24:00、VBA が G_Data に書込み）
行 1472: 末尾ダミー値（E1472=2.2）
```

| ✓ | No | セル | 名前付き範囲 | 種別 | 実態（値/数式/VBA代入） | 業務的意味 |
| --- | --- | --- | --- | --- | --- | --- |
| ✓ | 1 | `$C$4` | `KaisiDay` | 手動設定 | ユーザー入力（日付） | 集計開始日 |
| ✓ | 2 | `$D$4` | `SyuuryouDay` | VBA代入/手動 | `初期画面()` でクリア、`装置稼働時間表示()` で設定 | 集計終了日 |
| ✓ | 3 | `$E$4` | `TotalTime` | VBA代入 | `装置稼働時間表示()` で算出値書込み | 集計期間の総時間（時間単位） |
| ✓ | 4 | `$D$6:$F$7` | `SyousyaTime` | VBA代入 | `Ric1Kadou()` `Ric2Kadou()` `Ric3Kadou()` で書込み | 各号機の運転時間・停止時間 |
|  | 5 | `$D$8` | — | 数式 | `=IF(D6="","",D6/TotalTime)` | 1号機稼働率 |
|  | 6 | `$E$8` | — | 数式 | `=IF(E6="","",E6/TotalTime)` | 2号機稼働率 |
|  | 7 | `$F$8` | — | 数式 | `=IF(F6="","",F6/TotalTime)` | 3号機稼働率 |
| ✓ | 8 | `$D$31:$F$1471` | `G_Data` | VBA代入 | `R1Graph()` `R2Graph()` `R3Graph()` で書込み | 1分刻みグラフデータ（1号/2号/3号） |
|  | 9 | `$B$30:$C$1471` | — | 設定値 | 0〜1440 の連番 / 0:00:00〜24:00:00 | グラフのX軸（時刻ラベル） |
|  | 10 | `$G$31` | — | 設定値 | 3.4 | グラフY軸のダミースケール値 |

### 2.2 1号機集計表

**目的**: 1号機の線源昇降回数をイベント別に集計し、イベント詳細（日付・時刻・タイマー・イベント内容・照射時間）を一覧表示する。

#### 非表示行・列

| 種類 | 対象 | 備考 |
| --- | --- | --- |
| 非表示列 | C列 | イベントコード（DB から取得した数値/文字コード。B列の VLOOKUP 元） |

#### レイアウト構造

```
行 1: （空）
行 2: タイトル行（B2: 「１号機線源昇降回数等集計」）
行 3: 列見出し（開始日 / 終了日）
行 4: 入力行（Yori=B4, Made=D4）
行 5: 列見出し（イベント / 回数）+ 詳細ヘッダ列見出し
行 6〜11: イベント別集計結果（VBA 書込み）+ イベントコードマスタ（E6:F11）
行 12: 合計行（数式）
行 13〜10005: 詳細表示領域（H:M列、VBA 書込み）
```

| ✓ | No | セル | 名前付き範囲 | 種別 | 実態（値/数式/VBA代入） | 業務的意味 |
| --- | --- | --- | --- | --- | --- | --- |
| ✓ | 1 | `$B$4` | `Yori` | 手動設定 | ユーザー入力（日付） | 集計開始日 |
| ✓ | 2 | `$D$4` | `Made` | 手動設定 | ユーザー入力（日付） | 集計終了日 |
| ✓ | 3 | `$C$6:$D$11` | `RIC1Kekka` | VBA代入 | `イベント別集計()` で書込み | イベント別集計結果（コード＋回数） |
|  | 4 | `$E$6:$F$11` | `EvetList` | 設定値 | 固定マスタ | イベントコードと名称の対応表 |
|  | 5 | `$D$6` | `Event` | VBA代入 | `イベント別集計()` で書込み | 先頭イベントの回数 |
|  | 6 | `$B$6:$B$11` | — | 数式 | `=IF(C6="","",VLOOKUP(C6,$E$6:$F$11,2,FALSE))` | イベント名称（VLOOKUPで変換） |
|  | 7 | `$D$12` | — | 数式 | `=SUM(D6:D11)` | イベント回数合計 |
| ✓ | 8 | `$H:$M` | — | VBA代入 | `詳細表示()` で書込み | イベント詳細一覧（日付/時刻/タイマー/イベント/内容/照射時間） |
|  | 9 | `$F$6` | — | 設定値 | 貯蔵中 | イベントコード 0 の名称 |
|  | 10 | `$F$7` | — | 設定値 | 照射開始 | イベントコード 1 の名称 |
|  | 11 | `$F$8` | — | 設定値 | 昇降開始 | イベントコード 2 の名称 |
|  | 12 | `$F$9` | — | 設定値 | 移動照射 | イベントコード 3 の名称 |
|  | 13 | `$F$10` | — | 設定値 | ＰＣ Stop | イベントコード E の名称 |
|  | 14 | `$F$11` | — | 設定値 | PC Start | イベントコード S の名称 |

---

## 3. 名前付き範囲一覧


> ✓ = VBA から `Range()` で代入または参照され、業務ロジックに直結する名前付き範囲
> 全件を列挙し、✓ 基準に該当するものにマークを付ける。

| ✓ | No | 名前 | 参照先 | 業務的意味 |
| --- | --- | --- | --- | --- |
| ✓ | 1 | `KaisiDay` | 稼働時間!$C$4 | 集計開始日入力セル。`装置稼働時間表示()` で日付バリデーション・SQL条件に使用 |
| ✓ | 2 | `SyuuryouDay` | 稼働時間!$D$4 | 集計終了日。`装置稼働時間表示()` で範囲判定・当日フラグに使用 |
| ✓ | 3 | `TotalTime` | 稼働時間!$E$4 | 総時間（時間単位）。稼働率計算の分母として数式・VBA双方で参照 |
| ✓ | 4 | `SyousyaTime` | 稼働時間!$D$6:$F$7 | 各号機の運転/停止時間書込み先。`Ric1Kadou()` 等で行列指定の基準に使用 |
| ✓ | 5 | `G_Data` | 稼働時間!$D$31:$F$1471 | グラフ用分刻みデータ書込み先。`R1Graph()` `R2Graph()` `R3Graph()` で参照 |
| ✓ | 6 | `Yori` | '1号機集計表'!$B$4 | 1号機集計の開始日入力。`イベント別集計()` `詳細表示()` で参照 |
| ✓ | 7 | `Made` | '1号機集計表'!$D$4 | 1号機集計の終了日入力。`イベント別集計()` `詳細表示()` で参照 |
| ✓ | 8 | `RIC1Kekka` | '1号機集計表'!$C$6:$D$11 | イベント別集計結果の書込み先。`初期画面()` でクリア |
|  | 9 | `Event` | '1号機集計表'!$D$6 | 先頭イベントの回数セル |
|  | 10 | `EvetList` | '1号機集計表'!$E$6:$F$11 | イベントコード⇔名称の対応マスタ（VLOOKUP参照元） |

---


### 3.1 データの入力規則

なし。

## 4. 数式一覧

| シート | 数式件数 | 備考 |
| --- | --- | --- |
| 稼働時間 | 3 | 稼働率の計算 |
| 1号機集計表 | 7 | イベント名称変換（VLOOKUP）＋合計 |

### 4.1 稼働時間

| セル | 数式 | 説明 |
| --- | --- | --- |
| `$D$8` | `=IF(D6="","",D6/TotalTime)` | 1号機稼働率（運転時間÷総時間） |
| `$E$8` | `=IF(E6="","",E6/TotalTime)` | 2号機稼働率 |
| `$F$8` | `=IF(F6="","",F6/TotalTime)` | 3号機稼働率 |

### 4.2 1号機集計表

| セル | 数式 | 説明 |
| --- | --- | --- |
| `$B$6` | `=IF(C6="","",VLOOKUP(C6,$E$6:$F$11,2,FALSE))` | イベントコードから名称を逆引き |
| `$B$7` | `=IF(C7="","",VLOOKUP(C7,$E$6:$F$11,2,FALSE))` | 同上 |
| `$B$8` | `=IF(C8="","",VLOOKUP(C8,$E$6:$F$11,2,FALSE))` | 同上 |
| `$B$9` | `=IF(C9="","",VLOOKUP(C9,$E$6:$F$11,2,FALSE))` | 同上 |
| `$B$10` | `=IF(C10="","",VLOOKUP(C10,$E$6:$F$11,2,FALSE))` | 同上 |
| `$B$11` | `=IF(C11="","",VLOOKUP(C11,$E$6:$F$11,2,FALSE))` | 同上 |
| `$D$12` | `=SUM(D6:D11)` | イベント回数合計 |

---

## 5. ボタン・マクロ対応

> ✓ = DB 更新・画面遷移・計算実行など副作用のある操作を起動するボタン

### 5.1 シート上のボタン（Form Control）

| ✓ | No | シート | ボタンラベル | 割り当てマクロ | 動作概要 |
| --- | --- | --- | --- | --- | --- |
| ✓ | 1 | 稼働時間 | 実行 | `装置稼働時間表示()` | 入力された期間に対して3台の装置稼働時間を集計・表示する |
| ✓ | 2 | 稼働時間 | 前日表示 | `前日表示()` | 開始日を1日前にして再集計する（グラフ表示時のみ表示） |
| ✓ | 3 | 稼働時間 | 翌日表示 | `翌日表示()` | 開始日を1日後にして再集計する（グラフ表示時のみ表示） |
| ✓ | 4 | 1号機集計表 | 詳細表示 | `詳細表示()` | 指定期間のイベント詳細をDBから取得し一覧表示する |
| ✓ | 5 | 1号機集計表 | 回数集計 | `イベント別集計()` | 指定期間のイベント回数をGROUP BYで集計表示する |

### 5.2 ショートカットキー

| No | マクロ名 | ショートカット | 処理概要 |
| --- | --- | --- | --- |
| 1 | `スタート()` | **Ctrl+E** | 画面初期化（`初期画面()` を呼び出し、両シートのデータをクリアして保護を再設定） |

### 5.3 ユーザーフォーム上のボタン（サマリ）

なし。ユーザーフォームは存在しない。

---

### 5.4 CommandBar に動的追加されるボタン

なし。

## 6. VBA モジュール仕様

### 6.0 全プロシージャ一覧


> ✓ = ユーザー操作の起点（Click イベント等） / DB I/O を実行 / 他モジュールから呼び出される Public
> 全プロシージャを列挙し、✓ 基準に該当するものにマークを付ける。

| ✓ | No | モジュール | プロシージャ | スコープ | 種別 | 概要 |
| --- | --- | --- | --- | --- | --- | --- |
| ✓ | 1 | **ThisWorkbook.cls** | `Workbook_Open()` | Private | Event | ブック起動時に `スタート()` を呼び出して初期画面を設定 |
|  | 2 | **ThisWorkbook.cls** | `Workbook_BeforeClose()` | Private | Event | 保存確認を抑制してブックを閉じる |
|  | 3 | **Sheet1**（「1号機集計表」） | — | — | — | コードなし（Option Explicit のみ） |
| ✓ | 4 | **Sheet2**（「稼働時間」） | `Worksheet_Change()` | Private | Event | `KaisiDay` 変更時に `SyuuryouDay` を自動設定 |
| ✓ | 5 | **SQL_Execution.bas** | `Open_oraconDB()` | Public | Sub | DSN=ricdb で ADODB.Connection を開く |
| ✓ | 6 | **SQL_Execution.bas** | `SQL_Exe()` | Public | Sub | 任意のSQL文を Execute して Recordset に格納 |
| ✓ | 7 | **SQL_Execution.bas** | `SQL_INSERT_UPDATE()` | Public | Sub | テーブルにレコードが存在すれば UPDATE、なければ INSERT |
| ✓ | 8 | **SQL_Execution.bas** | `SQL_Delete()` | Public | Sub | 指定テーブルから WHERE 条件で DELETE |
| ✓ | 9 | **SQL_Execution.bas** | `Disp_Sheet()` | Public | Sub | SQL結果をシートの指定位置に CopyFromRecordset で貼り付け |
| ✓ | 10 | **SQL_Execution.bas** | `Set_Array()` | Public | Sub | SQL結果を2次元配列 myData() に格納 |
| ✓ | 11 | **運転時間.bas** | `装置稼働時間表示()` | Public | Sub | 入力バリデーション後、3台の稼働時間計算を呼び出す（ボタン起点） |
| ✓ | 12 | **運転時間.bas** | `前日表示()` | Public | Sub | 開始日を1日前にして再集計（ボタン起点） |
| ✓ | 13 | **運転時間.bas** | `翌日表示()` | Public | Sub | 開始日を1日後にして再集計（ボタン起点） |
| ✓ | 14 | **運転時間.bas** | `Ric1Kadou()` | Public | Sub | 1号機（sengnr1）の照射時間を算出 |
| ✓ | 15 | **運転時間.bas** | `Ric2Kadou()` | Public | Sub | 2号機（kyouj2）の停止時間を算出し照射時間を逆算 |
| ✓ | 16 | **運転時間.bas** | `Ric3Kadou()` | Public | Sub | 3号機（sengnr3）のビット演算で照射状態を判定し時間算出 |
|  | 17 | **運転時間.bas** | `R1Graph()` | Public | Sub | 1号機のグラフデータを分刻みで `G_Data` 列Dに書込み |
|  | 18 | **運転時間.bas** | `R2Graph()` | Public | Sub | 2号機のグラフデータを分刻みで `G_Data` 列Eに書込み |
|  | 19 | **運転時間.bas** | `R3Graph()` | Public | Sub | 3号機のグラフデータを分刻みで `G_Data` 列Fに書込み |
|  | 20 | **運転時間.bas** | `ExHenkan()` | Public | Function | 10進数を2進数文字列に変換（3号機のイベントビット判定用） |
|  | 21 | **運転時間.bas** | `ChengTime()` | Public | Function | "yyyyMMdd HHmmss" 形式文字列を Date 型に変換 |
| ✓ | 22 | **イベント集計.bas** | `イベント別集計()` | Public | Sub | sengnr1 から期間内イベントを GROUP BY 集計（ボタン起点） |
| ✓ | 23 | **イベント集計.bas** | `詳細表示()` | Public | Sub | sengnr1 から全イベントを取得しシートに展開（ボタン起点） |
|  | 24 | **イベント集計.bas** | `Hours()` | Public | Function | 日付文字列+時刻文字列を Date 型に変換 |
| ✓ | 25 | **クリア.bas** | `スタート()` | Public | Sub | 初期画面設定の起点（`Workbook_Open` から呼ばれる） |
|  | 26 | **クリア.bas** | `初期画面()` | Public | Sub | 両シートの入力・結果をクリアし保護を設定 |
|  | 27 | **終了処理.bas** | `Bookを閉じる()` | Public | Sub | 保存確認なしでブックを閉じる |
|  | 28 | **終了処理.bas** | `クリア()` | Public | Sub | `Kekka` `Yori` `Made` をクリア |

---

### 6.1 ThisWorkbook.cls

ブックの起動・終了イベントを受け、後続処理へ委譲する。

| No | プロシージャ | 処理フロー | 主要SQL |
| --- | --- | --- | --- |
| 1 | `Workbook_Open()` | ブック起動時に `スタート()` を呼び出して初期画面を設定 | なし |
| 2 | `Workbook_BeforeClose()` | 保存確認を抑制してブックを閉じる | なし |

## 7. ユーザーフォーム仕様

ユーザーフォームは存在しない。

---

## 8. DB 接続・外部連携

### 8.1 ODBC 接続設定

| DSN 名 | UID | PWD | 用途 |
| --- | --- | --- | --- |
| `ricdb` | `ric` | `t6101` | 照射管理データベースへの接続（ADODB + ODBC） |

### 8.2 テーブル一覧（参照/更新区分付き）

> ✓ = INSERT / UPDATE / DELETE の対象テーブル（参照のみのテーブルは ✓ なし）

| ✓ | No | テーブル名 | 区分 | 主な用途 | キー列 | 参照/更新列 |
| --- | --- | --- | --- | --- | --- | --- |
|  | 1 | `sengnr1` | 参照 | 1号機線源昇降イベントデータ | `sdate`, `stime` | `sdate`, `stime`, `timer`, `event` |
|  | 2 | `kyouj2` | 参照 | 2号機運転データ（回転速度記録） | `rectime` | `rectime`, `realspd` |
|  | 3 | `sengnr3` | 参照 | 3号機線源昇降イベントデータ | `sdate` | `sdate`, `sekitime`, `event` |

> **「キー列」の定義**: JOIN 条件または UPDATE/DELETE の WHERE 句で使用される列を示す。

### 8.3 SQL 一覧

#### 8.3.1 1号機：期間内イベント別集計（`イベント別集計()` / **イベント集計.bas**）

```sql
SELECT event, COUNT(*)
FROM sengnr1
WHERE sdate >= '{開始日}' AND sdate <= '{終了日}'
GROUP BY event ORDER BY event
```

#### 8.3.2 1号機：期間内全イベント詳細（`詳細表示()` / **イベント集計.bas**）

```sql
SELECT sdate 日付, stime 時刻, timer タイマー, event イベント
FROM sengnr1
WHERE sdate >= '{開始日}' AND sdate <= '{終了日}'
```

#### 8.3.3 1号機：開始日未満の最大タイムスタンプ（`Ric1Kadou()` / **運転時間.bas**）

```sql
SELECT MAX(sdate||stime) FROM sengnr1
WHERE sdate||stime < '{開始日時}'
```

#### 8.3.4 1号機：移動照射時間合計（`Ric1Kadou()` / **運転時間.bas**）

```sql
SELECT SUM(TO_NUMBER(timer)) FROM sengnr1
WHERE TO_NUMBER(timer) < 24
  AND sdate||stime >= '{開始日時}' AND sdate||stime < '{終了日時}'
```

#### 8.3.5 2号機：期間内レコード数（`Ric2Kadou()` / **運転時間.bas**）

```sql
SELECT COUNT(*) FROM kyouj2
WHERE rectime >= '{開始日時}' AND rectime < '{終了日時}'
```

#### 8.3.6 2号機：開始日前の最大時刻＋期間データ取得（`Ric2Kadou()` / **運転時間.bas**）

```sql
SELECT RECTIME, REALSPD FROM KYOUJ2
WHERE rectime >= '{前日最大}' AND rectime <= '{終了日時}'
ORDER BY rectime
```

#### 8.3.7 3号機：集計開始時・終了時のタイマー（`Ric3Kadou()` / **運転時間.bas**）

```sql
SELECT MAX(sdate) FROM sengnr3
WHERE sdate < TO_DATE('{日時}', 'yyyy/mm/dd hh24:mi:ss')
```

#### 8.3.8 3号機：該当データ（`Ric3Kadou()` / **運転時間.bas**）

```sql
SELECT sdate, sekitime, event FROM sengnr3
WHERE sdate >= TO_DATE('{開始}', 'yyyy/mm/dd hh24:mi:ss')
  AND sdate <= TO_DATE('{終了}', 'yyyy/mm/dd hh24:mi:ss')
ORDER BY sdate
```

### 8.4 外部ファイル連携

なし。外部ファイル連携は存在しない。

---

## 9. データフロー

各フローは「起点 → 処理 → 結果」の粒度で記述する。

### 9.1 ブック起動〜初期画面表示

| No | 起点 | 処理 | 結果 |
| --- | --- | --- | --- |
| 1 | 📄 ブックOpen | 📄 `Workbook_Open()` / **ThisWorkbook.cls** | `スタート()` を呼び出し |
| 2 | 上記 | 📄 `スタート()` / **クリア.bas** | イベント無効化して `初期画面()` 呼び出し |
| 3 | 上記 | 📊 `初期画面()` / **クリア.bas** | 「1号機集計表」: Yori/Made/RIC1Kekka/H:M クリア → Protect |
| 4 | 上記 | 📊 `初期画面()` / **クリア.bas** | 「稼働時間」: KaisiDay/SyuuryouDay/SyousyaTime/G_Data/TotalTime クリア → グラフ/ボタン非表示 → Protect |
| 5 | 上記 | 📊 | 「稼働時間」を選択し `KaisiDay` にカーソル配置 |

#### ツリー図（補助）

```
（ブック起動）
└─ 📄 Workbook_Open              [ThisWorkbook.cls]
   └─ 📄 スタート                 [クリア.bas]（イベント無効化）
      └─ 📊 初期画面              [クリア.bas]
         ├─ 📊 「1号機集計表」クリア＋Protect
         ├─ 📊 「稼働時間」クリア＋グラフ/ボタン非表示＋Protect
         └─ 📊 「稼働時間」Select → KaisiDay.Select
```

### 9.2 装置稼働時間集計（実行ボタン）

| No | 起点 | 処理 | 結果 |
| --- | --- | --- | --- |
| 1 | 📊 ユーザーが `KaisiDay` に日付入力 | 📊 `Worksheet_Change()` / **Sheet2.cls** | `SyuuryouDay` に同日を自動設定 |
| 2 | 📊 （実行ボタン Click） | 📄 `装置稼働時間表示()` / **運転時間.bas** | 入力バリデーション（空・日付形式・範囲チェック） |
| 3 | 上記 | 📊 `装置稼働時間表示()` | `TotalTime` に総時間を書込み、グラフ表示可否を判定 |
| 4 | 上記 | 📊 `装置稼働時間表示()` | 1日指定時: ボタン2(前日)/ボタン3(翌日)/グラフ表示; 複数日: 非表示 |
| 5 | 上記 | 🗄️ `Ric1Kadou()` / **運転時間.bas** | sengnr1 から期間データ取得 → 照射時間・停止時間を算出 |
| 6 | 上記 | 📊 `Ric1Kadou()` | `SyousyaTime` 列D に運転/停止時間書込み |
| 7 | 上記 | 📊 `R1Graph()` / **運転時間.bas** | G_Data 列D にグラフデータ書込み（1日指定時のみ） |
| 8 | 上記 | 🗄️ `Ric2Kadou()` / **運転時間.bas** | kyouj2 から期間データ取得 → 停止時間合計を算出 |
| 9 | 上記 | 📊 `Ric2Kadou()` | `SyousyaTime` 列E に運転/停止時間書込み |
| 10 | 上記 | 📊 `R2Graph()` / **運転時間.bas** | G_Data 列E にグラフデータ書込み（1日指定時のみ） |
| 11 | 上記 | 🗄️ `Ric3Kadou()` / **運転時間.bas** | sengnr3 から期間データ取得 → ビット演算で照射判定・時間算出 |
| 12 | 上記 | 📊 `Ric3Kadou()` | `SyousyaTime` 列F に運転/停止時間書込み |
| 13 | 上記 | 📊 `R3Graph()` / **運転時間.bas** | G_Data 列F にグラフデータ書込み（1日指定時のみ） |

#### ツリー図（補助）

```
（実行ボタン Click）
└─ 📄 装置稼働時間表示            [運転時間.bas]
   ├─ 📊 バリデーション → TotalTime 書込み
   ├─ 📊 ボタン・グラフ表示制御
   ├─ 🗄️ Ric1Kadou               [運転時間.bas]
   │   ├─ 🗄️ Set_Array × 3       [SQL_Execution.bas]（sengnr1 照会）
   │   ├─ 📄 照射時間計算（タイマー差分＋移動照射合計）
   │   ├─ 📊 SyousyaTime(D6:D7) 書込み
   │   └─ 📊 R1Graph              [運転時間.bas]（G_Data 列D 書込み）
   ├─ 🗄️ Ric2Kadou               [運転時間.bas]
   │   ├─ 🗄️ Set_Array × 3       [SQL_Execution.bas]（kyouj2 照会）
   │   ├─ 📄 停止時間合計算出（REALSPD=0 区間の累計）
   │   ├─ 📊 SyousyaTime(E6:E7) 書込み
   │   └─ 📊 R2Graph              [運転時間.bas]（G_Data 列E 書込み）
   └─ 🗄️ Ric3Kadou               [運転時間.bas]
       ├─ 🗄️ Set_Array × 3       [SQL_Execution.bas]（sengnr3 照会）
       ├─ 📄 ビット判定（ExHenkan）→ 照射時間算出
       ├─ 📊 SyousyaTime(F6:F7) 書込み
       └─ 📊 R3Graph              [運転時間.bas]（G_Data 列F 書込み）
```

### 9.3 1号機イベント別集計（回数集計ボタン）

| No | 起点 | 処理 | 結果 |
| --- | --- | --- | --- |
| 1 | 📊 （回数集計ボタン Click） | 📄 `イベント別集計()` / **イベント集計.bas** | Yori/Made 読取り・バリデーション |
| 2 | 上記 | 📊 | `RIC1Kekka` クリア |
| 3 | 上記 | 🗄️ `Disp_Sheet()` / **SQL_Execution.bas** | sengnr1 から GROUP BY event で集計 |
| 4 | 上記 | 📊 | 「1号機集計表」行6〜にイベント別回数を書込み |

#### ツリー図（補助）

```
（回数集計ボタン Click）
└─ 📄 イベント別集計              [イベント集計.bas]
   ├─ 📊 Yori/Made 読取り
   ├─ 📊 RIC1Kekka クリア
   └─ 🗄️ Disp_Sheet              [SQL_Execution.bas]
      ├─ 🗄️ Open_oraconDB        [SQL_Execution.bas]（DB接続）
      ├─ 🗄️ SQL_Exe              [SQL_Execution.bas]（SELECT実行）
      └─ 📊 CopyFromRecordset    （「1号機集計表」行6, 列3 に書込み）
```

### 9.4 1号機イベント詳細表示（詳細表示ボタン）

| No | 起点 | 処理 | 結果 |
| --- | --- | --- | --- |
| 1 | 📊 （詳細表示ボタン Click） | 📄 `詳細表示()` / **イベント集計.bas** | Yori/Made 読取り・バリデーション |
| 2 | 上記 | 📊 | H:M列クリア |
| 3 | 上記 | 🗄️ `Set_Array()` / **SQL_Execution.bas** | sengnr1 からレコード件数確認（10000件超は中止） |
| 4 | 上記 | 🗄️ `Disp_Sheet()` / **SQL_Execution.bas** | sengnr1 から全イベントを取得しH列〜に展開 |
| 5 | 上記 | 📊 `詳細表示()` | H4にヘッダ、L列にイベント内容名称（Select Case）、M列に照射時間を書込み |

#### ツリー図（補助）

```
（詳細表示ボタン Click）
└─ 📄 詳細表示                    [イベント集計.bas]
   ├─ 📊 Yori/Made 読取り
   ├─ 📊 H:M クリア
   ├─ 🗄️ Set_Array               [SQL_Execution.bas]（件数チェック）
   │   └─ [10000件超] MsgBox → Exit
   ├─ 🗄️ Disp_Sheet              [SQL_Execution.bas]（詳細データ取得）
   │   └─ 📊 CopyFromRecordset  （H5〜）
   └─ 📊 イベント名称・照射時間計算
      ├─ 📄 Select Case（0→貯蔵中, 1→照射開始, 2→昇降開始, 3→移動照射, E→PC Stop, S→PC Start）
      └─ 📄 Hours() で時間差計算 → M列に照射時間書込み
```

### 9.5 前日/翌日表示（ナビゲーション）

| No | 起点 | 処理 | 結果 |
| --- | --- | --- | --- |
| 1 | 📊 （前日表示ボタン Click） | 📊 `前日表示()` / **運転時間.bas** | `KaisiDay` を -1 日、`SyuuryouDay` を同日に設定 |
| 2 | 上記 | 📄 | `装置稼働時間表示()` を再呼び出し（フロー 9.2 と同一） |
| 3 | 📊 （翌日表示ボタン Click） | 📊 `翌日表示()` / **運転時間.bas** | グラフ非表示時または当日表示時は何もしない |
| 4 | 上記 | 📊 | `KaisiDay` を +1 日、`SyuuryouDay` を同日に設定 → `装置稼働時間表示()` 再呼び出し |

#### ツリー図（補助）

```
（前日表示ボタン Click）
└─ 📊 前日表示                    [運転時間.bas]
   ├─ 📊 KaisiDay = KaisiDay - 1
   ├─ 📊 SyuuryouDay = KaisiDay
   └─ 📄 装置稼働時間表示        → フロー 9.2 へ

（翌日表示ボタン Click）
└─ 📊 翌日表示                    [運転時間.bas]
   ├─ [mmGrafF=False OR mmTodayF=True] → Exit
   ├─ 📊 KaisiDay = KaisiDay + 1
   ├─ 📊 SyuuryouDay = KaisiDay
   └─ 📄 装置稼働時間表示        → フロー 9.2 へ
```

---

## 10. セキュリティ注意事項


| No | カテゴリ | 内容 | リスク |
| --- | --- | --- | --- |
| 1 | 認証情報ハードコード | DSN=`ricdb`, UID=`ric`, PWD=`t6101` が VBA ソースに平文記載 | 中：VBAエディタで閲覧可能 |

---

## スコープ外（本仕様書に含まないもの）

- セル書式（色・罫線・フォント）
- 条件付き書式、グラフ・画像、印刷設定

必要な場合は Excel 画面のスクリーンショットで補完してください。
